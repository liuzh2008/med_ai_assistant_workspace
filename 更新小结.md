### 2026-03-27 12:30
#### 版本号：0.7.002，更新：
- 新增DRG HIV过滤功能：当患者无HIV相关诊断时，排除HIV相关DRG分组
- 修复catalog版DrgParsedRecord缺少drgName字段导致HIV过滤遗漏的问题

### 2026-03-26 20:25
#### 版本号：0.7.001，更新：
- 实现DRG双向严格分流策略：患者无手术时只匹配无手术DRG，患者有手术时只匹配有手术DRG
- 新增首行"/"标识检测，支持识别MAINPROCEDURES首行为"/"的DRG记录
- 更新接口文档和Javadoc注释

### 2026-03-26 19:40
#### 版本号：0.6.090，更新：
- 优化DRG目录解析：首行"/"标识检测逻辑，支持首行"/"后跟手术条目的DRG记录正确识别为无手术要求
- 使患者无手术时能正确匹配首行为"/"的DRG记录

### 2026-03-26 18:45
#### 版本号：0.6.089，更新：
- 优化DRG匹配逻辑：实现"诊断为主、手术为辅"差异化评分策略
- 新增PrimaryDiagnosisProcedureMatcher匹配过程DEBUG日志
- 添加心房颤动无手术场景测试用例
- 更新DRG目录匹配接口文档和Javadoc注释

### 2026-03-26 12:30
#### 版本号：0.6.088，更新：
- 新增Oracle序列一致性自动检查服务，定时任务创建Prompt前自动检测并修复序列落后问题，防止主键冲突产生脏数据

### 2026-03-26 10:30
#### 版本号：0.6.087，更新：
- 整理后端接口文档目录结构，按业务功能分类到9个子目录，合并4组同类文档，更新索引

### 2026-03-25 23:00
#### 版本号：v0.6.087，更新：
- 定时任务扩展：每日Prompt生成任务新增MCC筛选Prompt自动生成功能，为所有在院患者生成诊断分析、诊疗计划、病情小结和MCC分析四种Prompt
- 新增generateMccPromptForPatient方法，实现MCC筛选和Prompt生成逻辑
- TimerPromptGenerator新增MccScreeningService、DiagnosisRepository、PromptTemplateRepository依赖注入
- 创建定时任务MCC筛选Prompt生成接口文档

### 2026-03-25 22:00
#### 版本号：v0.6.086，更新：
- 新增DRG目录匹配功能：根据主要诊断和主要手术匹配DRG记录
- 新增DrgMatchingService服务类实现匹配逻辑
- 新增GET /api/drg/catalog/match接口
- 前端新增DRG费用卡片显示匹配结果
- 前端新增matchDrgRecords API函数
- 创建DRG目录匹配接口文档

### 2026-03-25 21:33
#### 版本号：v0.6.085，更新：
- 新增合并症或并发症分析历史结果卡片（DRG分析页面）
- 新增 GET /api/ai/latestPromptResult 接口
- Markdown渲染AI分析结果，自动过滤thinking标签
- 文本左对齐优化

### 2026-03-25 20:30
#### 版本号：v0.6.084，更新：
- DRG分析页面重构：删除所有标签页，仅保留"开始分析"按钮
- 新增患者医疗信息卡片，包含诊断列表和手术列表双列表
- 诊断列表支持类型切换（主要/次要），唯一主要诊断规则，自动排序
- 手术列表支持类型切换（主要/次要），唯一主要手术规则，自动排序
- 从后端API获取真实诊断和手术数据
- 新增患者诊断与手术查询接口文档

### 2026-03-25 17:48
#### 版本号：v0.6.083，更新：
- 新增 MCC 分析 Prompt 生成保存接口 POST /api/drg/mcc/generate-prompt
- 前端 MCC 预筛选改用病人实际诊断数据，修复硬编码测试数据问题
- MCC 筛选结果按相似度降序排列，显示诊断编码、名称、MCC/CC类型
- 前端生成Prompt按钮改为调用后端API保存到数据库
- 修复 drg.js 中14个API路径重复/api/前缀问题
- 修复 MCC 候选列表字段映射（mccCode/mccName）

### 2026-03-24 20:45
#### 版本号：0.6.082，更新：
- 修复 DRG AI 分析保存 Prompt 时 userId、sortNumber 为空导致的 ORA-01400 错误
- 修复全局 JSON 响应中文编码问题（WebConfig 添加 UTF-8 MessageConverter）
- 修复前端 package.json 语法错误导致启动失败

### 2026-03-24 16:42
#### 版本号：0.6.081，更新：
- 改造非计划再次手术分析模块接入医院内网数据源
- DynamicJdbcTemplateFactory 升级为 HikariCP 连接池
- RepeatOperation 模块支持医院内网直连查询
- 修复 Spring 多构造函数注入歧义问题
- 修复前端正则表达式 ESLint 错误

### 2026-03-24 17:35

#### 版本号：0.6.080，更新：

- 新增非计划再次手术分析功能（前后端），含Oracle查询层、Prompt生成管道、4个REST API、质控分析页面

### 2026-03-23 19:50
#### 版本号：0.5.079，更新：
- 修复未执行Prompt列表不显示已提交Prompt的问题（后端合并查询）
- 优化未执行Prompt点击交互，跳过无效API调用并显示提示信息

### 2026-03-23 18:49
#### 版本号：0.5.078，更新：
- 修复生产环境语音识别上传413错误（Nginx client_max_body_size + 后端 multipart 配置同步）

### 2026-03-23
#### 版本号：0.5.077，更新：
- 病历记录操作指南全面重构：新增语音识别功能、待办事项生成功能、智录系统详细说明；添加操作按钮功能表格；补充浏览器兼容性配置指南；更新数据管理机制说明；前后端版本号从0.5.076更新至0.5.077

### 2026-03-21 17:45
#### 版本号：0.5.076，更新：
- 修复生产环境语音识别Docker容器DNS解析失败（extra_hosts映射内网API代理+firewalld永久策略）

### 2026-03-21 08:21
#### 版本号：0.5.075，更新：
- 修复Android平板上"AI辅助"子菜单点击后立即收起的问题，新增触屏/桌面设备差异化交互

### 2026-03-20 10:30
#### 版本号：0.5.074，更新：
- 新增监护仪呼吸机AI OCR数据采集完整技术方案文档
- 更新未完成功能列表

### 2026-03-20 10:06
#### 版本号：0.4.073，更新：
- AI辅助界面Prompt模板新增补充信息输入对话框（"请会诊记录"、"日常对话"模板支持用户输入补充信息）

### 2026-03-20 09:12
#### 版本号：0.4.072，更新：
- 修复AI对话UI无内容显示问题（aiService.js非流式响应回调时序修复）

### 2026-03-19 14:00
#### 版本号：v0.4.071，更新：
- 语音识别与LLM整理功能解耦：语音识别完成后不再自动调用LLM整理，用户可手动点击"文字整理"按钮触发；优化语音识别按钮逻辑，有选中文本时替换选中内容，无选中文本时插入到光标位置，新插入文本自动保持选中状态；优化文字整理功能，有选中文本时仅整理选中部分，无选中文本时整理全部内容，整理后光标定位到文本末尾；创建接口文档记录功能变更
- 前后端版本号更新：前端从 0.4.070 更新至 0.4.071，后端从 0.4.070 更新至 0.4.071

### 2026-03-19 12:00
#### 版本号：v0.4.070，更新：
- 生产环境语音识别配置修复：经排查发现OneAPI网关配置错误导致语音识别返回500错误，修改deploy/main-linux-oracle/config/application.properties，将voice.recognition.base-url从http://10.120.8.85:3001改为https://dashscope.aliyuncs.com/compatible-mode/v1，更新voice.recognition.api-key为正确的阿里云DashScope API Key，使用测试音频文件验证语音识别功能恢复正常
- 前后端版本号更新：前端从 0.4.069 更新至 0.4.070，后端从 0.4.069 更新至 0.4.070

### 2026-03-18 22:00
#### 版本号：v0.4.069，更新：
- 语音识别配置外化：修改VoiceFileRecognitionController，将硬编码的DASHSCOPE_BASE_URL和ASR_MODEL改为从VoiceRecognitionProperties配置类读取；增强VoiceRecognitionProperties配置类，新增baseUrl属性，完善Javadoc注释；更新application.properties开发环境配置，添加voice.recognition.base-url配置项，模型名称统一为qwen3-asr-flash；更新录音文件识别接口文档，添加配置说明章节；支持通过配置文件切换不同的ASR服务提供商（阿里云百炼DashScope或院内私有模型）
- 前后端版本号更新：前端从 0.4.068 更新至 0.4.069，后端从 0.4.068 更新至 0.4.069

### 2026-03-18 20:00
#### 版本号：v0.4.068，更新：
- 后端自动部署接口路径修复：确认UpdateView.vue中后端自动部署按钮接口路径为`/deploy/auto-deploy-backend`，与VUE_APP_API_BASE_URL拼接后形成正确的`/api/deploy/auto-deploy-backend`路径，避免与基础URL中的`/api`重复
- 前后端版本号更新：前端从 0.4.067 更新至 0.4.068，后端从 0.4.067 更新至 0.4.068

### 2026-03-18 18:00
#### 版本号：v0.4.067，更新：
- 后端自动部署API接口开发：创建POST /api/deploy/auto-deploy-backend接口，执行auto-deploy-backend.sh脚本实现后端一键自动部署，600秒超时设置，完整的错误处理和日志记录
- 前端后端自动部署功能：在UpdateView.vue系统更新页面添加"自动部署后端"功能，调用后端自动部署接口，显示实时日志输出和状态反馈
- 接口文档创建：创建后端自动部署API接口文档，包含接口路径、功能说明、请求参数、响应格式、执行逻辑
- 前后端版本号更新：前端从 0.4.066 更新至 0.4.067，后端从 0.4.066 更新至 0.4.067

### 2026-03-18 16:00
#### 版本号：v0.4.066，更新：
- 后端自动部署脚本开发：创建auto-deploy-backend.sh实现后端一键自动部署，包含版本号+文件大小双校验防重复部署机制、自动备份与回滚功能；创建restore-backend.sh实现手动从备份恢复；更新GitHub Actions工作流将部署脚本纳入构建产物
- 后端接口文档创建：创建后端自动部署脚本接口文档，包含脚本功能说明、调用方式、执行逻辑、响应示例
- 代码注释完善：为auto-deploy-backend.sh添加详细JSDoc风格注释，包含文件说明、变量注释、函数注释
- 前后端版本号更新：前端从 0.4.065 更新至 0.4.066，后端从 0.4.065 更新至 0.4.066

### 2026-03-18 12:00
#### 版本号：v0.4.065，更新：
- 前端接口路径修复：修复UpdateView.vue中执行自动部署脚本按钮调用的接口路径中'api'重复的问题，将`/api/deploy/auto-deploy-frontend`修改为`/deploy/auto-deploy-frontend`，解决因VUE_APP_API_BASE_URL已包含/api后缀导致的/api/api路径重复错误
- 前端版本号更新：从 0.4.064 更新至 0.4.065，后端保持 0.4.065

### 2026-03-17 10:30
#### 版本号：v0.4.064，更新：
- GitHub Actions工作流优化：新增Docker镜像构建和导出步骤，生成与本地构建一致的`med-ai-main.tar`文件；优化版本号处理，从Git tag动态提取版本号并注入Docker构建参数；发布产物调整为与本地构建完全一致
- 前后端版本号更新：前端从 0.4.063 更新至 0.4.064，后端从 0.4.063 更新至 0.4.064

### 2026-03-16 22:00
#### 版本号：v0.4.063，更新：
- 自动部署脚本优化：auto-deploy-frontend.sh 修改版本号获取接口，从直接调用执行服务器改为调用主服务器 `/api/deploy/latest`，解决版本号不一致问题；优化 curl 下载进度显示格式
- 前后端版本号更新：前端从 0.4.062 更新至 0.4.063，后端从 0.4.062 更新至 0.4.063

### 2026-03-16 20:00
#### 版本号：v0.4.062，更新：
- 前端自动部署功能完善：UpdateView.vue移除"下载前端更新包"和"解压到部署目录"两个手动操作卡片，简化为单一"自动部署前端"卡片；新增 `POST /api/deploy/auto-deploy-frontend` 接口执行自动部署脚本；创建 `auto-deploy-frontend.sh` 和 `restore-frontend.sh` 脚本；添加完整JSDoc注释和接口文档
- 前后端版本号更新：前端从 0.4.061 更新至 0.4.062，后端从 0.4.061 更新至 0.4.062

### 2026-03-16 18:00
#### 版本号：v0.4.061，更新：
- GitHub Actions 前端构建修复：修复 nginx.conf 中 `host.docker.internal` 在 Linux 服务器无法解析问题，将反向代理地址改为实际后端服务器地址 `10.120.11.43`，修复部署脚本路径问题，确保 GitHub Actions 构建产物与本地构建产物完全一致
- 前后端版本号更新：前端从 0.4.060 更新至 0.4.061，后端从 0.4.058 更新至 0.4.061

### 2026-03-16 17:30
#### 版本号：v0.4.058，更新：
- GitHub Actions Workflow优化：参照前端workflow优化后端workflow配置，添加fetch-depth: 0确保获取完整Git历史，移除Maven缓存配置以避免构建问题
- 前后端版本号更新：前端从0.4.057更新至0.4.058，后端从0.4.052更新至0.4.058

### 2026-03-16 16:00
#### 版本号：v0.4.052，更新：
- 前端自动化部署功能实现：新增 `POST /api/deploy/deploy-frontend` 接口，实现文件验证后自动执行部署脚本；UpdateView.vue增强，支持查询版本自动填充、文件大小验证、一键部署；GitHub Actions工作流增强，生成与本地构建一致的完整部署包（包含Docker镜像）
- 前后端版本号更新：从0.4.051更新至0.4.052

### 2026-03-14 14:00
#### 版本号：v0.4.051，更新：
- BuildDownloadController接口增强：新增自定义下载目录支持，通过backendDownloadDir和frontendDownloadDir参数指定下载路径，实现从执行服务器下载构建文件到主服务器指定目录（如/medai/build-files）
- 前后端版本号更新：从0.4.050更新至0.4.051

### 2026-03-14 12:00
#### 版本号：v0.4.050，更新：
- Oracle数据库PGA内存超限问题修复：调整pga_aggregate_limit从4G降至2G，重启Oracle容器释放BG00进程占用的1.6GB内存，解决ORA-04036错误，生成问题修复文档
- 版本号更新：前后端版本号从0.4.049更新至0.4.050

### 2026-03-13 18:00
#### 版本号：v0.4.049，更新：
- Docker构建流程优化：修改Dockerfile采用本地JAR+Docker复制模式，移除容器内Maven构建步骤，解决阿里云Maven仓库SSL连接不稳定导致的构建失败问题，提升构建速度和稳定性
- 版本号更新：前后端版本号从0.4.048更新至0.4.049

### 2026-03-13 17:30
#### 版本号：v0.4.048，更新：
- 系统更新功能：在前端"帮助"菜单下新增"更新"子菜单（仅管理员0001可见），提供查询最新版本、下载前端ZIP、生成解压命令功能

### 2026-03-11 17:30
#### 版本号：v0.4.047，更新：
- 版本号更新：前后端版本号从0.4.046更新至0.4.047
- GitHub Actions构建修复：修复后端工作流中JAR文件版本号硬编码问题，使用通配符匹配任意版本，重新推送tag触发构建

### 2026-03-11 16:00
#### 版本号：v0.4.046，更新：
- 数据一致性诊断功能：新增Prompt数据一致性诊断与修复功能，提供诊断数据、演练修复、执行修复、状态摘要四个接口，前端在"自动执行Prompt"页面集成诊断卡片，支持检测和修复Prompt状态与PromptResult不匹配、执行服务器状态与主服务器状态不一致等问题

### 2026-03-09 23:30
#### 版本号：v0.4.045，更新：
- Prompt状态死循环与积压数据处理修复：清理执行服务器2673条积压数据，迁移11000+条"已处理"状态为"已完成"，修复Prompt ID 37337长时间未完成问题，创建问题修复文档

### 2026-03-09 20:30
#### 版本号：v0.4.045，更新：
- 智录系统触发逻辑调整：移除病历输入框获得焦点时自动弹出SmartInput的逻辑，改为在工具栏添加"智录"按钮手动触发，提升用户操作可控性

### 2026-03-09 19:20
#### 版本号：v0.4.043，更新：
- Docker构建Maven依赖下载SSL错误修复：修改Dockerfile添加阿里云镜像配置，将mirrorOf从central改为*（镜像所有仓库），解决插件下载时SSL握手失败问题

### 2026-03-07 14:30
#### 版本号：v0.4.042，更新：
- Oracle数据库IDENTITY策略主键NULL插入错误修复：为20个使用IDENTITY策略的表创建序列和触发器，解决新建病历记录和待办事项时ORA-01400错误；新增问题修复文档和SQL脚本

### 2026-03-08 17:30
#### 版本号：v0.4.041，更新：
- AI完善功能模型环境变量适配：将MedicalRecords.vue中硬编码的'inHospitalDeepseek'改为使用环境变量VUE_APP_LLM_MODEL，开发环境使用deepseek-chat，生产环境使用inHospitalDeepseek；AI调用从流式改为非流式：修改aiService.js和MedicalRecords.vue，简化响应处理逻辑

### 2026-03-09 11:20
#### 版本号：v0.4.040，更新：
- 修复执行服务器在 `--github.release.enabled=false` 时启动失败问题：`BuildFileController` 新增 `@ConditionalOnProperty(github.release.enabled=true)` 注解，与 `GitHubReleaseClientConfig` 生效条件保持一致，防止禁用 GitHub Release 功能时因缺少 `GitHubReleaseClient` Bean 导致 Spring 上下文初始化失败；完善 `BuildFileController` Javadoc 注释；更新接口文档生效条件说明

### 2026-03-08 18:00
#### 版本号：v0.4.039，更新：
- 新增主服务器构建产物下载控制器（BuildDownloadController，@Profile("main")）：提供 GET /api/deploy/latest、POST /api/deploy/download、GET /api/deploy/status 三个接口，实现主服务器从执行服务器拉取前后端构建产物并通过 Docker volume 挂载写入宿主机；支持多环境路径配置；开发环境三接口全部测试通过；新增接口文档及部署方案文档

### 2026-03-07 19:30
#### 版本号：v0.4.038，更新：
- 执行服务器 `/api/build-files/latest` 接口新增返回文件大小（size 字段）；修复 GitHub 私有仓库构建产物下载（改用 Assets API + 跟随 302 重定向）；执行服务器禁用启动时夜间同步；更新接口文档及 Javadoc 注释

### 2026-03-06 23:00
#### 版本号：v0.4.037，更新：
- DRG盈亏计算模块实现：新建DrgProfitLossService服务类提供盈亏计算功能；更新DrgProfitLossController从占位实现到完整实现；实现盈亏计算接口（盈亏金额=保险支付-实际费用）；实现保险支付查询接口；实现盈亏率计算接口；更新DRG分析接口文档

### 2026-03-06 22:20
#### 版本号：v0.4.036，更新：
- 修复DrgCatalogLoader构造函数数据库操作导致Bean创建失败问题：将初始化逻辑移到@PostConstruct方法中确保事务管理器就绪；修复DrgAiAnalysisService模板类型不匹配问题：将模板类型常量从'DRG_ANALYSIS'改为'其他'以匹配数据库记录

### 2026-03-06 18:00
#### 版本号：v0.4.035，更新：
- DRG分析功能API接口完善：创建4个新Controller（MccScreeningController、DrgAiAnalysisController、DrgSnapshotController、DrgProfitLossController）提供DRG分析各阶段的REST API接口；修复7个DTO类的Jackson反序列化问题；更新DRG分析接口文档；关闭开发服务器启动时同步配置；创建前端DRG API模块（src/api/drg.js）并添加完整JSDoc注释

### 2026-03-06 15:00
#### 版本号：v0.4.035，更新：
- 测试数据迁移与脱敏处理：通过PROD_DB_LINK迁移生产环境在院病人数据（63人）及关联医疗数据到本地开发数据库，迁移表包括PATIENTS、DIAGNOSIS、EMR_CONTENT、EXAMINATIONRESULTS、LABRESULTS、LONGTERMORDERS、MEDICAL_RECORDS、PROMPTS、PROMPTRESULT、PROMPTTEMPLATE、SURGERYNAME、TODO_ITEM共12张表，为各表添加主键约束和索引，对PATIENTS表NAME字段进行脱敏处理（保留姓氏，名字用*替代），创建数据迁移总结文档doc/测试/测试服务器/数据库/数据迁移总结.md；版本号更新：前端package.json从0.4.034更新至0.4.035，后端pom.xml从0.4.034更新至0.4.035

### 2026-03-06
#### 版本号：v0.4.034，更新：
- 部署脚本版本号显示功能：修改Dockerfile添加ARG APP_VERSION构建参数并将版本号写入镜像内/app/version.txt；修改build-and-export.sh从pom.xml读取版本号并传递；修改deploy.sh部署完成后从容器读取版本号显示；创建接口文档doc/接口/部署脚本版本号显示.md；版本号更新：前端package.json从0.4.033更新至0.4.034，后端pom.xml从0.4.033更新至0.4.034

### 2026-03-06
#### 版本号：v0.4.033，更新：
- 病历记录时间8小时偏差修复：将MedicalRecord的recordTime和modificationTime从Date改为LocalDateTime，解决JDBC时区转换导致的时间偏差问题；前后端统一使用ISO 8601格式（yyyy-MM-ddTHH:mm:ss.SSS）传输时间；前端使用正则表达式解析时间避免Date对象时区转换

### 2026-03-05
#### 版本号：v0.4.032，更新：
- 部署脚本服务自动启动增强：修改deploy/main-linux-oracle/deploy.sh和deploy/main-linux-testServer/deploy.sh，新增Prompt自动提交服务（/api/prompt-services/submission）和手术分析定时任务（/api/surgery-analysis/schedule）的自动检测和启动逻辑，部署完成后无需手动点击"启动提交服务"和"启动"按钮

### 2026-03-05
#### 版本号：v0.4.031，更新：
- Oracle数据库连接泄漏诊断增强：新增DatabaseResourceTracker工具类追踪JDBC资源，修复PromptPollingService中CLOB未关闭问题，增强DatabaseConnectionPoolMonitor监控功能，配置独立database-diagnostic.log日志文件，彻底删除已弃用的application-prod.properties文件
- 版本号更新：前端package.json从0.4.030更新至0.4.031

### 2026-03-05
#### 版本号：v0.4.030，更新：
- 待办事项分析诊断信息集成：在MedicalRecords.vue中添加diagnoses getter从Vuex获取患者诊断信息，修改generateTodo方法将诊断信息整合到病历内容中传递给后端，使LLM分析待办事项时能结合诊断信息给出更准确结果
- 版本号更新：前端package.json从0.4.029更新至0.4.030

### 2026-03-05
#### 版本号：v0.4.029，更新：
- 手术分析接口UTF-8编码修复：解决中文手术名称在数据库存储时出现乱码的问题，通过配置RestTemplate的StringHttpMessageConverter使用UTF-8编码（替代默认ISO-8859-1），并在HTTP请求头中添加Accept-Charset: UTF-8，确保AI服务返回的中文响应正确解析
- 版本号更新：前端package.json从0.4.028更新至0.4.029

### 2026-03-05
#### 版本号：v0.4.028，更新：
- 语音识别功能增强：新增语音停顿检测（5秒无语音自动停止）和录音时长限制（最大5分钟），前端使用AudioContext+AnalyserNode实时分析音频音量，后端文件上传限制从1MB提升至500MB
- 版本号更新：前端package.json从0.4.027更新至0.4.028

### 2026-03-04 22:45
#### 版本号：v0.4.027，更新：
- 语音识别功能改造：实时WebSocket改为录音后HTTP识别，前端使用MediaRecorder录制音频并解码为PCM格式，后端新增VoiceFileRecognitionController提供/api/voice/recognize-file接口，使用阿里云百炼qwen3-asr-flash模型支持长达2小时的录音文件识别
- 版本号更新：前端package.json从0.4.026更新至0.4.027

### 2026-03-04 16:30
#### 版本号：v0.4.026，更新：
- 开发环境前端API配置修复：修改`.env.development`中`VUE_APP_API_BASE_URL`从`http://192.168.110.130:8081/api`改为`http://localhost:8081/api`，解决浏览器访问内网IP超时问题（ERR_CONNECTION_TIMED_OUT），使前端直接连接本地主服务器
- 版本号更新：前端package.json从0.4.025更新至0.4.026

### 2026-03-04 15:30
#### 版本号：v0.4.026，更新：
- 测试服务器临时医嘱重复数据清理：使用SQLPlus连接100.66.1.3:1521/FREE数据库，删除LONGTERMORDERS表重复临时医嘱15,980条（按PATIENTID+ORDERNAME+ORDERDATE判重，保留每组ORDERID最小记录），清理后临时医嘱从76,074条降至60,094条，重复组从5,664组降至0组
- 创建SQLPlus操作指南文档：记录Oracle客户端配置、PowerShell+SQLPlus连接命令、PROMPTRESULT表结构检查及医嘱清理完整操作过程

### 2025-12-06 12:00
#### 版本号：v0.4.025，更新：
- openEuler测试服务器前端部署完成：升级Node.js从v12至v18.20.8，安装621个依赖包，使用npm run serve启动开发服务器，开放防火墙8080端口，前端服务成功运行在http://192.168.110.130:8080，宿主机能正常访问

### 2026-03-03 23:45
#### 版本号：v0.4.024，更新：
- 手术分析接口改用统一AI接口并改为非流式响应：重构OperationAnalysisController，从直接调用DeepSeek公网API改为通过/api/ai/response统一接口调用LLM；改用非流式响应(stream=false)简化处理逻辑；新增extractContentFromNonStreamResponse方法解析非流式JSON响应；移除StreamingResponseUtil依赖；更新JSDoc注释和接口文档

### 2026-03-03 22:30
#### 版本号：v0.4.023，更新：
- 自动查房记录模板统一：将病危每日查房记录、病重每2日查房记录、查房记录三个模板全部改为「上级医师查房记录」，promptType 由病情小结改为查房记录

### 2026-03-03 21:00
#### 版本号：v0.4.022，更新：
- 修复 openEuler 测试服务器后端 systemd 服务（StandardOutput 配置错误导致服务反复重启、Java 版本切换至 Java 21），后端服务成功上线；测试并验证轮询服务 API 全部正常；打通开发机到测试服务器三级 SSH 免密链路（100.66.1.1  100.66.1.3  192.168.110.130 ProxyJump 无需密码）；新增主服务器 SSH 连接与后端启动测试记录文档

### 2026-03-03 01:14
#### 版本号：v0.4.022，更新：
- 修复测试环境API路径双重前缀问题：在 `deploy/main-linux-testServer/config/application.properties` 中添加 `server.servlet.context-path=`（空值），覆盖已弃用的 `application-prod.properties` 中的 `/api` context-path 设置，解决测试服务器API路径变为 `/api/api/users/{id}` 的问题，使测试环境与开发/生产环境保持一致（均为 `/api/users/{id}`）

### 2026-03-02 18:30
#### 版本号：v0.4.021，更新：
- EMR记录AI完善功能开放：删除enhanceMedicalRecord方法中的EMR状态检查逻辑，允许对EMR病历进行AI完善，同时保持保存限制不变（EMR记录保存时仍提示无法回写并自动创建参考记录），提升用户对EMR记录的编辑体验

### 2026-03-02 10:52
#### 版本号：v0.4.020，更新：
- 全环境Jackson时区配置统一修复：在所有环境配置文件（开发/生产Oracle/测试/执行服务器）中添加spring.jackson.time-zone=GMT+8和spring.jackson.date-format配置，解决病历记录保存后时间差8小时问题，确保Date类型序列化/反序列化时使用东八区时区，实现前后端时间数据交互一致性

### 2026-03-02 00:00
#### 版本号：v0.4.019，更新：
- 删除冗余的PollingService服务：移除使用错误数据源的PollingService、PollingController和PollingServiceTest（共982行代码），解决生产环境ORA-00942错误日志问题，前端API调用迁移至/api/execution-server/polling-status和/api/prompts/status，保留功能更完善的PromptPollingService服务

### 2026-03-01 23:30
#### 版本号：v0.4.018，更新：
- Prompt轮询服务事务回滚异常修复：解决生产环境UnexpectedRollbackException问题，为EncryptedDataTempService.markAsSent()和markAsError()添加REQUIRES_NEW传播级别实现独立事务，在PollingService中添加try-catch异常隔离，避免执行服务器事务异常影响主事务，确保数据一致性并支持自动重试

### 2026-03-01 22:00
#### 版本号：v0.4.017，更新：
- 临时医嘱重复导入问题修复：修改LongTermOrderRepository.existsActiveOrder()判重逻辑，添加(:repeatIndicator=0 OR lto.stopTime IS NULL)条件区分临时医嘱和长期医嘱，解决临时医嘱stopTime有值导致判重失效的问题，同时保留长期医嘱停止后再次下达的业务灵活性，提升判重日志级别到INFO并添加详细参数输出

### 2026-03-01 16:45
#### 版本号：v0.4.015（后端）、v0.4.016（前端），更新：
- 病历记录时间时区问题全面修复：后端MedicalRecord实体类添加@JsonFormat注解指定GMT+8时区，前端handleRecordClick和handleEMRRecordClick方法改用本地时间手动格式化替代toISOString()，解决新建/点击病历记录时间少8小时的UTC时区转换问题

### 2026-03-01 16:30
#### 版本号：v0.4.016，更新：
- 前端病历记录日期显示时区修复：修复handleRecordClick和handleEMRRecordClick方法中使用toISOString()导致日期控件显示UTC时间的问题，改用本地时间手动格式化（年-月-日 时:分:秒），确保日期控件显示与用户当前时区一致，解决点击记录时日期少8小时的显示问题

### 2026-03-01 14:30
#### 版本号：v0.4.014，更新：
- 待办事项生成LLM调用方式优化：重构TodoGenerationService，移除PromptService和PromptTemplateRepository依赖，直接调用/api/ai/response接口，参考AI完善按钮实现方式，避免数据源混淆和复杂依赖链，简化调用逻辑，提升代码可维护性和稳定性；发现生产环境缺少todo_item表

### 2026-03-01 10:30
#### 版本号：0.4.013，更新：
- LLM调用统一抽象：在ai.js中新增analyzeLLM通用LLM分析方法作为非流式调用统一入口，重构voiceTextProcessor.js使用analyzeLLM替代直接request调用，减少代码重复约18行，符合项目LLM调用规范

### 2026-02-28 21:30
#### 版本号：0.4.012，更新：
- 病人列表加载性能优化（检查报告懒加载）：实施懒加载策略将检查报告改为按需加载（只在用户点击"检查"标签页时加载），添加完整性能监控日志系统（PatientList/MedicalRecords/LabTests/ExaminationReports/PatientView/PatientTabs/Store），病人列表加载从5-10秒降至151ms（提速97%），选中病人加载从4-17秒降至355ms（提速91-98%），避免检查报告3次重复调用节省11-51秒，用户体验显著提升

### 2026-02-28 18:41
#### 版本号：0.4.011，更新：
- 待办事项页面布局优化：实现左右分栏独立滚动（两侧各自添加overflow-y:auto），页面高度使用100vh视口高度而非固定像素值，移除卡片min-height限制实现高度自适应，移除内部滚动限制实现内容完整显示，提升用户浏览体验，更新Memory Bank记忆

### 2026-02-28 19:30
#### 版本号：0.4.010，更新：
- 前端执行服务器地址配置更正：修正.env.development和src/api/request.js中执行服务器地址从100.66.1.2:8082更新为100.66.1.3:8082，通过curl验证服务器可用性，解决前端调用执行服务器接口ERR_EMPTY_RESPONSE网络错误

### 2026-02-28 17:15
#### 版本号：0.4.009，更新：
- 执行服务器启动失败修复：移除run-execution-server.bat中的execution profile配置（仅用于生产环境需GitHub配置），修正application.properties中执行服务器配置（execution.server.host/oracle-port/api-url）从生产环境（nb.nblink.cc:16601）改为开发环境（100.66.1.3:1521），解决GitHub Release配置缺失和数据库连接错误问题

### 2026-02-28 16:30
#### 版本号：0.4.008，更新：
- 开发环境执行服务器切换：将执行服务器从100.66.1.2切换到100.66.1.3，修改application.properties、application-execution.properties和application-integration.properties三个配置文件，生产环境配置（nb.nblink.cc）保持不变，确保开发测试与生产环境完全隔离

### 2026-02-28 14:37

#### 版本号：0.4.007，更新：

- 待办事项页面“原始记录”功能实现与优化：新增前端API方法getMedicalRecordById()通过病历ID获取单条病历记录，实现点击按钮显示完整病历内容（记录类型/时间/医生/内容），修复Vue模板字符串错误使用this导致运行时错误，修复Element UI对话框点击x按钮产生cancel错误（添加.catch()捕获），优化卡片高度自适应布局解决浏览器渲染冲突，更新接口文档和JSDoc注释

### 2026-02-28 11:52
#### 版本号：0.4.006，更新：
- 患者信息工具函数提取与代码复用：将性别格式化函数从PatientInfo.vue和TodoView.vue提取到patientUtils.js，新增formatPatientGender()工具函数支持多种性别编码格式（数字/字符串/字母），净减少代码约10行，统一性别格式化逻辑，添加完整JSDoc注释，提升代码可维护性和一致性，更新Memory Bank记忆

### 2026-02-28 00:38
#### 版本号：0.4.005，更新：
- 待办事项页面按科室加载功能实现：完成前端TodoView页面病人待办列表自动加载，对接后端GET /api/medicalrecords/todos/by-date-department接口，实现按病人ID汇总待办（使用Map数据结构合并同一病人多条待办），页面加载时自动获取用户科室并生成病人卡片（按床号升序），更新getTodosByDepartment() API方法，临时禁用专业组筛选，完善JSDoc注释

### 2026-02-28 00:30
#### 版本号：0.4.004，更新：
- 根据日期和科室查询待办事项接口：新增GET /api/medicalrecords/todos/by-date-department接口，支持按日期（yyyy-MM-dd）和科室名称查询当天生成的待办事项，按床号升序排列，自动过滤已删除病历，创建280行完整接口文档，添加106行详细JSDoc注释，用于科室每日工作清单管理

### 2026-02-27 23:50
#### 版本号：0.4.003，更新：
- 待办事项查询逻辑修复：修复根据患者ID查询待办事项接口返回已删除病历记录问题，新增Repository方法findByPatientIdExcludingDeletedRecords使用JPQL JOIN过滤is_deleted=1的病历，更新Controller和接口文档，测试验证通过

### 2026-02-27 23:11
#### 版本号：0.4.001，更新：
- 新增根据病历记录ID获取待办事项接口：实现GET /api/medicalrecords/todo/{medicalRecordId}接口，返回指定病历的所有待办事项列表及数量统计，添加完整JSDoc注释（56行），创建接口文档（177行），前端版本号同步更新

### 2026-02-27 22:43
#### 版本号：0.3.016，更新：
- 病历待办事项生成功能AI调用优化：在TodoGenerationService中使用自定义简化系统提示词替代深度推理协议，新增PromptService.analyzePromptContentWithCustomSystem()方法支持自定义系统提示词，减少Token消耗和响应时间，更新接口文档，添加JSDoc注释

### 2026-02-27 16:00
#### 版本号：v0.3.018，更新：
- 病历待办事项生成功能文档完善：创建完整实施方案文档（819行）和TDD测试指南文档（1484行），涵盖需求分析、数据库设计、后端/前端实现、完整测试策略（单元+集成+E2E+性能测试）、Mock策略、CI/CD集成，为功能开发提供完整技术路线图

### 2026-02-27 14:45
#### 版本号：v0.3.017，更新：
- 确认病历时间显示修复完成：验证MedicalRecords组件createRecord()方法已正确使用本地时间手动格式化（非toISOString()），新建病历时记录时间正确显示为当前本地时间，无UTC时区偏移问题

### 2026-02-27 13:30
#### 版本号：v0.3.016，更新：
- 全面修复病历时间显示错误：修复MedicalRecords组件中fetchRecords()、handleRecordClick()、handleEMRRecordClick()三个方法的时间格式化问题，将所有toISOString()改为本地时间手动格式化，彻底解决从数据库加载病历、点击病历记录、点击EMR病历记录时显示时间比实际少8小时的问题

### 2026-02-27 12:00
#### 版本号：0.3.014，更新：
- 部署脚本增加服务自启功能：修改deploy/main-linux-oracle/deploy.sh和deploy/main-linux-testServer/deploy.sh，在健康检查通过后自动检测并启动执行服务器轮询、主服务器提交、主服务器轮询、Prompt自动生成四个核心服务，实现部署后服务自动启用无需手动干预

### 2026-02-27 11:55
#### 版本号：v0.3.013，更新：
- 修复新建病历时间显示错误：将createRecord()方法中的toISOString()改为使用本地时间手动格式化，解决UTC时间导致显示时间比实际时间少8小时的问题，确保新建病历时记录时间正确显示为当前本地时间；为createRecord()方法添加完整JSDoc注释

### 2026-02-26 23:59
#### 版本号：v0.3.012，更新：
- 病历待办事项生成功能 TDD 实施完成：按照红-绿-重构流程完成数据访问层（PatientTodoItem 实体和仓库，8 个测试）、服务层（TodoGenerationService，14 个测试）、REST API 层（AIController 增强 + PatientTodoController 新建，9 个集成测试），共 22 个单元测试全部通过，新增 5 个病历待办事项相关接口

### 2026-02-26 23:45
#### 版本号：v0.3.012，更新：
- 病历待办事项生成功能 TDD 实施指南完成：生成 1717 行完整 TDD 指南文档，包含 3 个用户故事、7 个开发任务、8 类边界场景测试、完整代码示例和质量保障体系；同时完成方案文档 4 项关键优化（Prompt 模板格式、兜底逻辑、测试用例、前端依赖版本）

### 2026-02-26 20:23
#### 版本号：0.3.011，更新：
- 修复开发环境启动失败问题（Bean定义重复）：移除GitHubReleaseProperties的@Configuration注解和GitHubReleaseClient的@Service注解，在GitHubReleaseClientConfig中添加@EnableConfigurationProperties启用配置类，解决Spring容器Bean定义重复冲突

### 2026-02-26 23:15
#### 版本号：0.3.011，更新：
- GitHub Release下载服务及配置类TDD实现：完成GitHubReleaseClient Service（使用WebClient异步下载、本地缓存机制、12个单元测试全部通过），创建GitHubReleaseProperties配置类（从环境变量注入Token和缓存路径）和GitHubReleaseClientConfig（仅execution profile生效），配置类测试优化从SpringBootTest改为纯单元测试（速度提升1000倍），完整JavaDoc注释和配置示例文件

### 2026-02-26 16:43
#### 版本号：v0.3.010，更新：
- Docker永久化配置实施及 BuildFileTestController 增强：修改执行服务器 docker-compose.yml 添加 /medai/build-files 目录挂载（支持 Linux 和 Windows），增强 BuildFileTestController 自动检测操作系统选择正确路径，生成接口文档和部署指南

### 2026-02-26 21:20
#### 版本号：v0.3.009，更新：
- GitHub自动构建与执行服务器文件下载验证：验证GitHub Actions构建能力、执行服务器网络连通性与私有仓库访问能力，实现执行服务器文件下载测试API（/api/build-files/test/{filename}），生成端到端自动化发布方案文档和验证记录文档

### 2026-02-26 16:30
#### 版本号：v0.3.008，更新：
- 修复诊断替换接口日期格式解析错误：ReplaceDiagnosisDTO添加@JsonFormat注解支持ISO 8601日期格式，解决前端使用toISOString()生成日期时的400错误

### 2026-02-25 13:50
#### 版本号：v0.3.007，更新：
- 修复Prompt轮询服务数据一致性Bug：重构事务边界确保PromptResult创建、Prompt状态更新、STATUS标记在同一事务中，增加数据一致性检查验证PromptResult存在性，实现自动恢复和补偿机制，解决97条历史问题记录

### 2026-02-24 19:45
#### 版本号：v0.3.006，更新：
- AI对话功能模型配置化改造：将硬编码的模型名称改为从环境变量读取，开发环境使用deepseek-chat生产环境使用inHospitalDeepseek，优化aiService.js错误处理逻辑正确处理字符串类型错误，添加详细调试日志

### 2026-02-24 17:30
#### 版本号：v0.3.005，更新：
- 修复前端部署端口冲突问题：部署脚本增强容器清理逻辑，使用docker-compose down --remove-orphans清理所有容器，添加8080端口占用检测并显示进程信息，同步更新生产和测试服务器部署脚本

### 2026-02-24 14:28
#### 版本号：v0.3.004，更新：
- 修复手术任务创建日期解析错误：在SurgeryTaskDTO中添加@JsonFormat注解支持纯日期字符串反序列化，添加详细JavaDoc注释，解决前端使用"yyyy-MM-dd"格式日期时的400错误

### 2026-02-24 09:15

#### 版本号：v0.3.003，更新：

- 前端构建脚本增强缓存清理逻辑：使用PowerShell强制删除.cache目录，检测并修复损坏的依赖，优化依赖安装流程，解决Windows环境下npm缓存清理失败问题

### 2026-02-23 12:15
#### 版本号：v0.3.002，更新：
- 病历记录页面布局优化：将记录类型、记录时间、记录医生放到同一行

### 2026-02-23 12:10
#### 版本号：v0.3.002，更新：
- 小屏界面功能：适配7-8英寸设备，系统菜单添加小屏模式开关，小屏模式下病人列表和Prompt模板列表默认隐藏，通过菜单控制显示，选择后自动隐藏

### 2026-02-22 23:55
#### 版本号：v0.3.001，更新：
- 语音识别后LLM文字整理功能：语音识别结束后自动调用LLM整理文字，解析修改后记录和修改原因分别显示到病历输入框和修改原因框；新增LLM模型环境变量配置；修复vue.config.js代理配置

### 2026-02-22 22:40
#### 版本号：v0.3.000，更新：
- 实时语音识别端到端集成完成：集成DashScope SDK 2.22.9，后端实现音频数据转发到阿里云百炼ASR服务（paraformer-realtime-v2），识别结果实时推送到前端病历输入框；修复Spring MVC/WebFlux冲突、CORS跨域、多数据源冲突等问题

### 2026-02-22 19:05
#### 版本号：v0.2.108，更新：
- 病历记录实时语音识别前端功能：使用AudioContext采集麦克风音频，通过WebSocket实时发送PCM数据，识别结果自动追加到病历内容

### 2026-02-22 18:40
#### 版本号：v0.2.107，更新：
- 实时语音识别WebSocket后端TDD实施完成：按照红-绿-重构流程完成3个核心任务（连接管理、音频处理、结果回调），21个测试用例全部通过，性能测试达标

### 2026-02-22 16:08
#### 版本号：v0.2.106，更新：
- Prompt列表未读状态显示与自动标记功能：未读记录显示浅蓝色背景，点击自动标记为已读，后端新增isRead字段查询和标记接口

### 2026-02-22 12:00
#### 版本号：v0.2.105，更新：
- 实时语音识别功能文档：创建WebSocket实时语音识别实施方案、TDD实施指南和接口文档，采用阿里云百炼ASR服务，实现医生语音实时转文字

### 2026-02-22 00:56
#### 版本号：v0.2.104，更新：
- 定时任务扩展：每日Prompt生成任务新增病情小结自动生成功能，现在每天7:00为所有在院患者生成诊断分析、诊疗计划补充和病情小结三种Prompt

### 2026-02-21 15:47
#### 版本号：v0.2.103，更新：
- 修复病程记录保存逻辑缺陷，后端接口统一返回数字ID，前端增加5层验证机制，解决修改时偶尔新增记录的问题

### 2026-02-21 22:30
#### 版本号：v0.2.102，更新：
- 修复医嘱同步判重逻辑，临时医嘱和长期医嘱增加RepeatIndicator字段区分判重，修复临时医嘱重复导入问题

### 2026-02-21
#### 版本号：v0.2.101，更新：
- 病历记录列表新增记录日期列（第2列，宽度120px），新增formatRecordDate方法格式化日期为MM-DD HH:mm格式
- 文档类型列设置宽度100px确保中文完整显示
- 同步更新package.json版本号至0.2.101
- 优化用户体验，方便快速查看病历时间信息

### 2026-02-21
#### 版本号：v0.2.100，更新：
- 更新软件版本号至v0.2.100

### 2026-02-21
#### 版本号：v0.2.100，更新：
- 更新软件版本号至v0.2.100

### 2026-02-20
#### 版本号：v0.2.0，更新：
- 修复开发环境Prompt列表数据加载错误（data.forEach报错）
- 修复Prompt详情页PromptId显示为N/A的问题
- 修复AI内容未显示的问题
- 修复生产环境Prompt列表为空的问题（后端代码已更新）
- 关于页面添加软件版本号显示（从package.json动态读取）
- 为PromptList.vue关键方法添加完整的JSDoc注释
- 更新package.json版本号至0.2.0

### 2026-02-15
#### 版本号：未指定，更新：
- 新增AI结果删除功能：添加deletePromptResult API接口（DELETE /api/ai/patientPromptResult/{resultId}）
- AIResults组件添加删除按钮和deleteResult方法
- AITabs和AIView组件添加事件传递和列表刷新逻辑
- 补充完整JSDoc注释
- 实现删除后自动刷新Prompt列表功能


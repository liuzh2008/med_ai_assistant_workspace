# 2026-08-21

## docs(专利): 技术交底书发明点重构——从提示词改进转为流程构建方法

### 修改类型
文档（docs）

### 简要描述
针对评审反馈"发明点停留在提示词改进与自定义多维指标、存在智力活动规则风险"，将两份专利技术交底书的发明点重心从"提示词改进+指标定义"重构为"LLM 业务流程的自动化构建方法"，核心是**整体执行流程 + 步骤间输入输出强关联**：

- **AI教学模型自动推荐方法**（md + docx）：
  - 发明名称/技术领域/4.1 改为"自动推荐流程构建方法"：4.1 重写为四段——流程构建总述、现有技术三大缺陷（无整体流程编排、步骤间无数据关联、中间结果不落库）、多维异构输入的数据封装、可解释性与下游衔接。
  - 4.2 开篇重写：核心是流程构建方式，提示词模板与拼接规则降级为"流程数据载荷的工程实现"。
  - 4.2.3 新增"数据对象契约定义"：定义四类数据对象（推荐请求/推荐结果/确认结果/生成请求）字段结构及步骤间流转关系，显式化输入输出关联。
  - 权利要求 1 重构为流程构建方法（S1 数据对象契约与持久化载体 → S2 采集组装 → S3 语义推理 → S4 展示确认 → S5 生成衔接 → S6 状态管理与恢复），原提示词/指标细节下沉为从属权利要求 2-6；系统权利要求 7-8 同步重构。
  - 4.3 有益效果重排：流程构建效果提升为第一；技术效果对比表新增"数据对象契约"行。
- **AI双角色生成与审查的医学教案质量保障**（md + docx）：
  - 发明名称/技术领域/4.1 改为"质量保障流程构建方法"：4.1 重写为五段——流程构建总述、现有技术三大缺陷、双角色与结构化审查数据模型（弱化"自定义指标"，强调 JSON Schema 数据对象约束）、定向修正、模板管理。
  - 4.2 开篇重写；4.2.3 新增"数据对象契约定义"（教案数据对象 TeachingPlan + 审查报告数据对象 PlanReviewReport）。
  - 权利要求 1 重构为流程构建方法（S1 数据对象契约与状态机 → S2 生成 → S3 审查 → S4 优化 → S5 终审 → S6 容错降级与断点恢复），原模板体系/降级/SSE 等细节下沉为从属权利要求 2-8；系统权利要求 9-10 同步重构。
  - 4.3 有益效果重排（流程构建效果提升为第一）；技术效果对比表新增"数据对象契约"行。

### 影响范围
- `项目相关/软件著作权/专利申请/专利申请技术交底书-AI教学模型自动推荐方法.md`（文件名未变，标题改为"…自动推荐流程构建方法"）
- `项目相关/软件著作权/专利申请/专利申请技术交底书-AI教学模型自动推荐方法.docx`
- `项目相关/软件著作权/专利申请/专利申请技术交底书-AI双角色生成与审查的医学教案质量保障.md`（文件名未变，标题改为"…质量保障流程构建方法"）
- `项目相关/软件著作权/专利申请/专利申请技术交底书-AI双角色生成与审查的医学教案质量保障.docx`
- 旧版 docx 备份：`项目相关/软件著作权/专利申请/*.bak-20260821.docx`
- 临时脚本 `temp/verify_patent_invent.py`（已 gitignore）

### 验证
- docx 由最新 md 经 convert-patent-md-to-docx.js 整体重新生成，md/docx 完全一致
- python-docx 正常打开无 XML 损坏，关键词检查全部 PASS（docx1 含 13 项 + 应无 3 项；docx2 含 11 项 + 应无 3 项）
- 发明点表述已统一为"流程构建方法"：四阶段串行编排、数据对象契约、状态持久化、失败定位与降级重试、切换输入状态重置

## docs(专利): 交底书内容与现有系统流程一致性校验与修正

### 修改类型
文档（docs）

### 简要描述
逐条核对两份交底书描述的流程与 AIMedTeach 微服务（med-teach-backend/med-teach-frontend）实际代码实现的一致性，共核实 18 个后端/前端文件 + 3 个 Prompt 模板文件 + 发布端点，修正文档2（AI双角色生成与审查）中 4 处与代码不符的描述：

- **附件3 前端源代码**：TeachingPlanGenerate.vue、TeachingPlanEdit.vue 实际不存在 → 改为实际存在的 PlanCreate.vue（5步向导）、StepFinalReview.vue（终审编辑）、TeachingPlanView.vue（教案详情查看）。
- **S108(d) 发布记录**："发布时记录发布教师身份与发布时间" → 实际 publish() 仅将 status 置为 PUBLISHED，无发布人字段 → 改为"自动记录更新时间戳"；权利要求5 S5 同步删除"并记录发布信息"。
- **S108(a)(b) 终审交互**："切换式对比视图"未实现（版本对比表格为空壳）→ 改为实际行为：初版经 SSE 即时展示、优化版由教师主动选择加载、评分面板可展开；终审编辑为全文本/分段两种模式（非"初版/优化版之间切换"）。
- **架构图/流程图/有益效果/步骤表**：同步修正"初版/优化版对比"→"查看"、"评分对比区/评分区"→"提示区"等 6 处表述。

文档1（AI教学模型自动推荐）核对结论：全部流程描述（S101-S105、三段式拼接、三层展示、模型前缀匹配、状态重置等）与代码一致，无需修改。

### 影响范围
- `项目相关/软件著作权/专利申请/专利申请技术交底书-AI双角色生成与审查的医学教案质量保障.md`（13 处修正）
- `项目相关/软件著作权/专利申请/专利申请技术交底书-AI双角色生成与审查的医学教案质量保障.docx`（重新生成）

### 验证
- 扩展 verify_patent_invent.py：docx2 新增含 7 项 + 应无 6 项校验，全部 PASS
- 修正依据均为已核实代码事实：TeachingPlanService.publish()、StepPlanGeneration.vue（comparisonHighlights 恒空）、StepFinalReview.vue（全文本/分段编辑）、plan-create.ts（loadRefinedPlan）、前端视图文件清单、模板三维度 40/35/25 与四条优化原则

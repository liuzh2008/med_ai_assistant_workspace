{
  "name": "@medai/dsh-record-sync",
  "version": "0.1.0",
  "type": "module",
  "description": "快速类病历同步生成插件（F2b+F3）：medai_record_generate_sync TS 工具，同步等待后端端点（≈90s），返回完成状态+脱敏摘要+promptId，超时降级转 medai_record_status",
  "main": "src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./package.json": "./package.json"
  },
  "scripts": {
    "test": "vitest run",
    "build": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.5.0",
    "vitest": "^3.0.0"
  }
}

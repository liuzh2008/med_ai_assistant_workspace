# 会话记忆

> 每次会话的关键产出速记，按日期分区追加。
> 自动维护：agent 在每个会话结束时，将尚未归入主题文件的产出速记写入本文件。

## 2026-08-13

- 建立了项目长期记忆体系：`记忆库/`（5 个主题文件 + 索引）、`memory-manager` 技能、根目录 `AGENTS.md` 自动记忆协议。
- 确认 `.dsh/skills` 是 Junction，实际指向 `.qoder/skills`（同一目录，改哪边都一样）。
- 确认 `.dsh/` 被 .gitignore 忽略，但 `.qoder/` 已被 Git 跟踪（111 个文件），整套记忆随仓库版本化。
- 记忆体系已提交并推送：根仓库 commit `096cbd2`（"建立项目自动长期记忆体系"），9 个文件，已推送至 github.com:liuzh2008/med_ai_assistant_workspace.git（master）。
- 推送时根仓库尚有未暂存改动（`docs/help/ai-diagnosis.md`、backend/vue 子目录 gitlink），非本次记忆体系范围，未提交。

## 2026-08-14

- 修复 100.66.1.3:8080 登录页"获取科室列表失败(500)"：根因是本机 Oracle system 密码过期(ORA-28001)导致后端启动失败；用户重置口令后，将测试服务器(100.66.1.4) Oracle 的 76 张应用表结构和数据 Data Pump 同步到本机 FREE 库（REPLACE 导入），补建 25 个测试库命名序列、重编译 24 个触发器、身份列 LIMIT VALUE 重置、序列 RESTART 到 MAX+1、回补本机独有列（TEACHING_PLANS PPT_*/OBJECTIVE_TEXT 等）、替换 V_INP_BILL_DETAIL_INFO 视图、DEFAULT profile 密码永不过期。
- 后端 dev 模式(8081)已启动（后台任务），健康检查 UP；`/api/users/1657/departments` 返回 200（CCU病区/心血管一病区/心血管二病区）。
- 产出脚本：`backend/test-scripts/{create-sequences,post-import-fix,resync-sequences-static,replace-view,inspect-local-db,list-local-tables,list-local-columns,check-local-rbac,check-local-flyway}.sql`；dump 与导入日志在 `D:\oracle_dump\`（1.77GB，确认稳定后可删）。
- 详细踩坑与流程已沉淀到《踩坑与教训.md》《命令与流程.md》《环境与配置.md》对应 2026-08-14 条目。
- 完成「生产库诊断分析数据提取与分析工具包」：用户采用方案一（SQL 提取脚本 + 人工摆渡 + 开发环境解析成文）。产出在 `temp/diagnosis-extract/`（不随版本发布）：`extract_01_count.sql`（摸底）、`extract_02_results.sql`（导出 PROMPTRESULT 诊断分析记录，PatientID 加盐哈希 SHA256 前16位脱敏）、`extract_03_diagnoses.sql`（导出目前诊断）、`run_extract.bat`（内置 NLS_LANG，改 CONN 即可）、`parse_and_generate.py`（三层兼容解析 + Levenshtein 配对判定，生成 Markdown 报告 + Excel 明细）、`使用说明.md`、`sample_local/`（示例产物）。
- 数据链路：PROMPTRESULT JOIN PROMPTS（PromptTemplateName IN '诊断分析','鉴别诊断分析'，近90天，deleted=0）→ PatientId → DIAGNOSIS（is_deleted=0）；Status 含 SUCCESS/SAVED 均导出（SAVED=医生保存后，最有价值）。
- 判定规则沿用《AI诊断数据采集方案》：Levenshtein 归一化相似度 =1.0 直接采纳 / 0.45~1.0 被修改 / <0.45 未采纳（AI侧）或医生新增（医生侧）。
- 已在本地开发库（Oracle 23ai Free，16条记录）与测试服务器（100.66.1.4 的 med-ai-oracle 容器，Oracle 21c XE，4条记录）双重验证通过，生产 21c 兼容性确认；测试服务器临时文件已清理。
- 状态：工具包已交付，待用户在生产执行三步（count→results→diagnoses）后取回 2 个 txt，开发环境跑 `python parse_and_generate.py` 生成对比文档。

## 2026-08-13（AI诊断依据会话）

- 完成「AI诊断辅助：用户修改诊断后仍显示诊断依据」功能全流程：需求 → 初版（改名缓存转移）→ 代码审查（S1 模糊匹配误配/M1 缓存优先级修复）→ 用户反馈迭代（包含匹配 → 行级绑定 → 精确+模糊附说明），最终方案为行级绑定 + 四级显示匹配链。
- 产出文件：`vue/src/utils/diagnosisBlockMatch.js`（新增）、`vue/src/components/ai/DiagnosisEditPanel.vue`、`vue/src/utils/diagnosisMerge.js`、`vue/test/diagnosis/diagnosisBlockMatch.test.mjs`（31 用例）、`vue/docs/更新日志/2026-08-13.md`、`docs/help/ai-diagnosis.md`（FAQ）。
- 已提交推送：vue 仓库 commit `d5d7025`（rebase 后，解决 2026-08-13.md 与远程 add/add 冲突合并双方内容）→ github.com:liuzh2008/med_ai_assistant_1.0_bs_vue；根仓库 commit `2920e90`（并行会话提交，含本次 FAQ 与 vue 子模块指针 d5d7025）→ med_ai_assistant_workspace。
- 遗留：vue `package-lock.json` 本地旧版本号改动已随 stash drop 丢弃（保留远程 0.9.239）；backend 仓库存在其他会话未提交改动（doc/、test-scripts/、dumps/），非本次范围未提交；`aiNameEdits` 连续改名两次 key 失效为存量 bug 未修。

# 会话记忆

> 每次会话的关键产出速记，按日期分区追加。
> 自动维护：agent 在每个会话结束时，将尚未归入主题文件的产出速记写入本文件。

## 2026-08-19（诊疗计划表 DSH 提交触发：TDD 实施 + 真实后端验证全绿）

- **实施结果（C1~C12 全部完成）**：方案 §6 全部落地——白名单 21→22、同步通道排除（`syncExcludedTemplates`+`assertSyncAllowed`+Controller 调用点切换）、诊断提示 C 口径（`DiagnosisGatedTemplates` 共享常量 + `RecordSubmitOutcome.diagnosisMissing` 三态 + `RecordSubmitResult.submitted` 五参 message 重载 + `RecordGenerateTool.DIAGNOSIS_MISSING_HINT`）、测试反转（RecordTemplatePolicyTest 13 用例 / RecordGenerateToolTest 17 / RecordPromptGenerationServiceTest 21 / RecordSubmitResultTest 11 / SyncGenerateEndpointTest 10 / DiagnosisGatedTemplatesTest 5 / FlowCompassIntegrationTest 同步排除断言 / McpGatewayEndpointIntegrationTest 8d 反转）+ ward-round SKILL.md 补步骤 5（查房后建议生成诊疗计划表，无诊断先 medai_diagnosis_analyze）。
- **验证**：①相关 9 类 93 用例 mvn 全绿（踩坑：assertThatThrownBy.satisfies 的 lambda 参数是 Throwable 需强转 `((McpToolException) e).getCode()`）；②全量 mvn test 3262 用例——本次改动面零失败，其余 166F/118E 为既有红基线（NoonWardRound 11F4E/Task7 2F3E/RealtimeVoice 5F 与记忆基线吻合；SessionArchiveEndpointIntegrationTest 1E 为真实 Oracle 外键 FK_QAR_INDICATOR_ID 测试数据问题，均与本次无关）；③**verify-mcp-gateway.sh PASS=29 FAIL=0**（场景 X 诊疗计划表提交允许→EXISTS_TODAY（02:00 定时批量高命中语义实证）、场景 Y 同步端点拒绝 MCP_TEMPLATE_NOT_ALLOWED）；④**Cypress mcp-record-generate.cy.js 7/7 全绿**（含新增提交允许+无诊断提示条件断言、同步端点拒绝用例）。
- **8081 重启**：旧进程（08-17 启动，旧代码）→ taskkill 39040 → `Start-Process cmd.exe /c call start-backend-dev.bat`（分离）→ 新 pid 39848；/mcp 401 探测判就绪（health 有 Redis DOWN 既有干扰不适用）。
- **verify-mcp-gateway.sh 两个既有 bug 修复（踩坑见踩坑与教训.md）**：①`AUTH_HDR` 在函数定义区静态构造（exchange 之前），exchange 后不回写 → jwt-enabled=true 后所有调用拿"每机 token 当 JWT"→ 全场景 MCP_AUTH_INVALID_JWT（PowerShell 手动 exchange+initialize 400 鉴权通过反证后端正常）；②"正常提交未标记 isError"断言 `grep 'isError'` 宽匹配命中正常信封自带的 `"isError":false` 字段误报 → 改 `grep '"isError":true'`。
- **code-review（/code-review 自动修复，2026-08-20 凌晨，报告存 `backend/doc/代码审查/2026-08-20-诊疗计划表DSH提交触发实施审查.md`，结论通过）**：五维审查发现 2 项已修复——①`RecordTemplatePolicy` 类 Javadoc 未提同步通道职责 + `assertAllowed`/`assertSyncAllowed` 重复 null 校验 → 提炼 `requireNonBlank` 去重；②`RecordPromptGenerationService` 诊断查库原在分派前无条件执行（NO_ADMISSION_DATA 也无谓查询）→ 挪至分派后仅 SUBMITTED 时查 + 补测试。登记 2 项不修：`src/test/resources/tdd-test-configs/` 未跟踪目录（非本次引入，遗留待确认）、通用链落库口径（既有，方案 §10）。**踩坑**：surefire 3.2.5 `-Dtest=A,B,C` 多类逗号传参时各类 Tests run 数字非确定性串扰（26/4/12 ↔ 29/2/11），**单类逐个跑才是权威**（单类复验 14+13+9+19=55/55 全绿，总数一致）。
- 详见 决策与设计.md 2026-08-19 条目「待办→已实施」段。

## 2026-08-19（诊疗计划 harness 改造：讨论 + 拍板 + 落盘，未写代码）

- **讨论链**：用户要求"讨论诊疗计划的 harness 改造，先讨论方案不写代码"→ 调研现状（DSH 侧 14 工具/同步端点/提交型编排支撑；诊疗计划侧生成链路/八步前端编排/存储/TREATMENT_PLAN_ITEMS）→ 提出五档方案（档 0 只读查询/档 1 提交型/档 2 同步/档 3 八步推理 3a 引擎化+3b agent loop/档 4 写回）→ 用户拍板：**档 2 同步** + **八步不动** + 只审批最终计划表 → 深化档 2 方案（白名单 21→22、注入覆盖核实、门禁/预算/EXISTS_TODAY 卡片/入口口径四决策点）→ 用户改口"**先只走提交流程**"→ 追问诊断门禁现状（核实三入口口径差异）→ 用户拍板"**本次 DSH 改造按 C 走**（提示型口径）"→ 方案定稿落盘。
- **产出**：《诊疗计划表DSH提交触发实施方案.md》（`backend/doc/迭代/DeepSeek Harness插件化/`，v1.0）——提交型触发器复用泛化工具 `medai_record_generate`（D1 只走提交/D2 同步入口排除（`syncExcludedTemplates`+`assertSyncAllowed`）/D3 诊断门禁 C 提示型（`RecordSubmitOutcome` 加 `diagnosisMissing`）/D4 白名单 21→22）；实施 R1-R4 估 2 天。**grill-with-docs 审查**（报告存 `backend/doc/代码审查/2026-08-19-诊疗计划表DSH提交触发实施方案审查.md`，19 主张 ✅17/⚠️2/➕1）：修方案 §4.2 质控上下文（实际查"QC-第三阶段-AI质控评估" PromptResult 非 QC_ASSESSMENT_RESULT 表）、§6.3 补 `RecordSubmitResult.submitted` 工厂 message 重载挂点、§10 补通用链落库口径注记（generatedBy=System/priority=5 与 Javadoc user/3 不一致，既有事实）。**TDD 指南**：《TDD实施指南-诊疗计划表DSH提交触发-20260819.md》（同目录）——4 模块（R1 白名单/R2 同步排除/R3 诊断提示/R4 反转 E2E 剧本）、10 用户故事、C1~C12 红绿重构周期（约 7h≈2 天）、70/20/10 金字塔、Gherkin 4 场景、风险矩阵 6 项。
- **关键源码事实（本会话核实）**：①同步/提交双入口共用 `RecordTemplatePolicy` + `medai.mcp.record-templates`（SyncGenerateController 内同调 assertAllowed）；②提交链 `generateRecordPrompt` → HTTP 自调用 `/api/ai/patient-data`，按 `promptName=="诊疗计划表"` 自动注入连续性 + 质控上下文（"QC-第三阶段-AI质控评估" PromptResult 原文，非 QC_ASSESSMENT_RESULT 表）+ 硬约束规则（PatientDataController 897-961 行）；③诊断门禁仅 `savePrompt` 硬拦（DIAGNOSIS_REQUIRED_TEMPLATES v1.1.0={诊疗计划表, 主要诊断、手术/操作分析, 合并症或并发症分析}，422 NO_DIAGNOSIS），`TimerPromptGenerator` 定时批量**有意放行**（677-695 行无诊断仍生成诊疗计划表仅跳过 DRG/MCC）；④`application.properties` 无显式 record-templates 配置（默认集生效）；⑤`RecordSubmitResult` 7 字段四工厂方法（submitted 工厂 message 固定文案，诊断提示需加 message 重载）；⑥既有事实：通用链落库 generatedBy=System/priority=5（与 RecordPromptGenerationService Javadoc 声称 user/3 不一致，不影响防重/质控口径）。
- 详见 决策与设计.md 2026-08-19 条目。

## 2026-08-18（医生工作流程 DSH 结合方案：讨论 + 拍板 + 落盘，未写代码）

- **讨论链**：用户要求"读取医生工作流程，讨论如何结合进 harness，不写代码"→ 读取《住院部医生日常工作流程.md》（35 步）+《插件化开发规范》§8 + 改造总方案 → 提出四维承载模型（知识 skill/能力工具/时序 Java 定时/状态罗盘）→ 用户反馈"某些情形医生更愿意等待实时结果"（修正纯提交型形态）→ 用户拍板"快速类直接生成，并落库"→ 落盘《医生工作流程DSH结合实施方案.md》（`backend/doc/迭代/DeepSeek Harness插件化/`）。
- **拍板点（5 项）**：D1 全四层+分档混合一次定稿；D2 生成类分档（快速类同步生成+落库/慢速类提交型/提交型为降级路径）；D3 明文草稿走 client 卡片直连后端（JWT），不进会话日志；D4 时限提醒走 Java 侧待办工具（不用被禁的 dsh-schedule）；D5 一链编排用 agent 多轮，不引入被禁 workflow 工具。
- **技术核实**：①`timeout-policy` 源码——工具超时自声明 timeoutMs，MCP 60s 非铁律；②`jobs-local` 服务层 active 含 JobDoneListener，tool-jobs 工具被禁但服务层可被 medai 插件封装；③后端 MCP 工具实盘 12 个 @Tool（grep 核实）。
- **产出**：方案文档 v1.0 → **v1.1 SoC 重构**（用户要求按关注点分离：F1 知识/F2 能力/F3 编排/F4 呈现 + 4 契约 + 边界铁律，依赖方向单向）+ 记忆库 ADR（决策与设计.md 2026-08-18 条目，含 v1.1 SoC 要点）。下一步待用户确认后进入实施（步骤 1 剧本化 / 步骤 2 快速类同步生成）。
- **TDD 指南**（/generate-tdd-guide 技能触发）：《TDD实施指南-医生工作流程DSH结合-20260818.md》（`backend/doc/迭代/DeepSeek Harness插件化/`）——3 模块（流程剧本化 F1 / 快速类同步生成 F2b+F3+F4 / 流程罗盘 F2c）、US-1.1~US-3.3 共 10 用户故事、12 周期红绿重构（合计约 3.5 工作日）、70/20/10 金字塔（JUnit5+Mockito / H2 限定扫描+Flyway baseline / Cypress+verify-flow-guide.sh）、JaCoCo 门禁 80%/70%、Gherkin ATDD（同步生成 PII 红线 / 时限待办）、7 风险矩阵（JWT 双通道一致性、明文端点越权、MCP 60s 与 90s 等待冲突、模板耗时实测、收敛白名单）。步骤 4/5（审批写回/体验层）未纳入本期范围。
- **grill-with-docs 修订**（/grill-with-docs 技能触发，方案 v1.1→v1.2、TDD 指南 v1.0→v1.0.1）：17 条主张核对（✅10/⚠️7/➕2）→ 修复①同步生成承载=Java REST 端点（`POST /api/mcp/generate-sync`，非 @Tool 不登记 McpServerConfig）+DSH TS 工具 fetch；②F2e 明文端点必须新增（核实 `AIResultOperationController` 无 GET 明文接口，`AIResultController` 不存在）；③TS 工具收敛白名单显式准入；④TS 工具鉴权=getCredentials()+X-MedAI-Machine-Token；另标注 pom 无 JaCoCo、仓库无测试 CI、时限规则全集 11 条（T3.3 11 用例）。
- **TDD 实施（12 周期全绿 + Cypress 验收）**：后端 Java 58 测试 BUILD SUCCESS（DocumentDeadlineRules 11 条规则/PatientFlowStatusService 四态/DueDocumentsService + 2 罗盘工具 + SyncGenerateService 轮询次数上限 + SyncGenerateController/RecordDraftController JWT 自校验 + McpServerConfig 14 工具 + verify-flow-guide.sh）；DSH 侧子代理完成（dsh-record-sync 8 测/dsh-ui-draft-card 9 测/dsh-flow-validator 15 测 + 205 回归，10 个 SKILL.md 已生效进 skill catalog）；**Cypress mcp-flow-guide.cy.js 7/7 全绿**（真实 8081：罗盘工具注册/阶段/待办/同步生成幂等/明文端点契约/401）。踩坑 7 条见 决策与设计.md 2026-08-18 实施条目（固定时钟死循环/LongConsumer bean 缺失/McpIntegrationTestConfig excludeFilters 扩展/工具数断言 12→14/Cypress alias 跨测试失效等）。
- **子代理覆盖矩阵修正（@medai/dsh-flow-validator，US-1.2 校验依据）**：①preop-discussion 覆盖 #18-#25 但**显式排除 ⚪#21**（手术知情同意不介入）；②time-limit-monitor 补覆盖 **#16**（病情告知/知情同意待办，F2c 罗盘待办类——原映射遗漏会致"需 AI 介入步骤全覆盖"校验红）；③medical-record-save 将"全流程保存"具体化为正式病历落库文书步骤 [6,7,12,19,20,23,24,28,31]。TS 工具超时实现细节：mock fetch 忽略 AbortSignal → 用 **Promise.race 兜底**（真实 fetch 仍收 signal 尽早取消）。
- **code-review（/code-review 自动修复）**：报告存 `backend/doc/代码审查/2026-08-18-医生工作流程DSH结合TDD实施.md`，结论**有条件通过**——修复 2 项：①`SyncGenerateService` 轮询循环加 `isInterrupted()` 检查（中断优雅返回 TIMEOUT，防空转）；②`DocumentDeadlineRules.rules()` 改 static final 常量（避免每次重建）。标注不修 4 项（Controller error() 重复/PROCESSING 分支冗余/明文端点限流与 patientId 响应/收敛白名单准入遗留）。修复后 35 测试回归全绿。
- **会诊请求 3080 实测缺口修复（2026-08-18）**：用户贴 3080 日志——医生问"请内分泌科会诊"，agent 调 `medai_feature_guide` 检索**零命中**而拒绝（虽有 `medai_record_generate` 白名单含"请会诊记录"模板，但 agent 未将其关联到会诊语义）。根因：`@medai/dsh-feature-guide/src/catalog.ts` 的 `FEATURE_ENTRIES` 按前端路由收录（会诊非独立页面路由，未收录）。**修复**：catalog 补 `consultation` 条目（id='consultation'，route 复用真实 '/ai-assistant'——Set 去重不破坏"清单覆盖前端路由"断言；aliases 含 会诊/请会诊/会诊申请/会诊记录/内分泌科会诊/心内科会诊/神经内科会诊/呼吸科会诊/邀请会诊；description 指引 `medai_record_generate`（模板"请会诊记录"，additionalInfo 附会诊目的，系统自动脱敏）+ 会诊流程由科室人工协调边界）；原负例测试样本"会诊排班"变命中 → 改为"会议排班系统"。feature-guide **56/56 全绿**。**生效条件**：3080 需重新装载更新后的 feature-guide 包（DSH 侧插件更新）。**补充**：consultation skill 剧本在 3080 收敛（tool-skill 被禁）下 agent 无法主动加载——会诊链路实际走"feature_guide 命中→medai_record_generate"，skill 价值在开发侧/未来放开时。
- **同类缺口排查与批量补齐（2026-08-18）**：按"能力存在但 feature_guide 目录无条目"同类模式全量对照 21 模板+诊断+罗盘+DRG——**语义重排型缺口**（模板名被修饰词打断，如"请内分泌科会诊"不含"请会诊记录"子串）共 7 组已全部补目录条目：consultation（会诊）/ transfer-record（转科）/ ward-round-records（查房记录-旗舰）/ preop-documents（术前讨论/小结/手术记录）/ discharge-documents（出院小结/证明书）/ admission-documents（入院记录/首程/现病史）/ discussion-documents（病例讨论/MDT）；route 全部复用真实路由（/medical-records 或 /ai-assistant，Set 去重不破坏路由覆盖断言），56/56 全绿。**低风险未补**：病情小结/护理计划/沟通记录等名词性模板（工具 description 字面即可命中，agent 走工具面不依赖目录）；**未落地未补**：DRG（medai_drg_analyze 候选试点未实施，避免目录承诺未落地能力）；查询/罗盘工具 agent 直接用无缺口。经验：新增能力模板时同步检查 feature_guide 目录别名（语义重排型最易漏）。转科条目补"请转呼吸内科"等科室组合别名（"转"被修饰词打断同型缺口）。
- **3080 重启 + 端到端验证（2026-08-18）**：重启 3080 踩坑——**直接启动 pnpm dsh web 必须带 `MEDAI_MCP_TOKEN` 环境变量**（start-dsh.cmd 会 fallback，绕过它则 `cordis.patch.yml` 里 mcp-medai 的 `!!js 'process.env.MEDAI_MCP_TOKEN'` 求值为 undefined → headers.X-MedAI-Machine-Token: undefined → schemastery `z.dict(String)` 校验失败 → 启动报 "failed to apply loader entry mcp-medai: invalid config" 闪退）。修复：启动命令 `set MEDAI_MCP_TOKEN=medai-dev-token && pnpm dsh web --port 3080 ...`。**验证结果**：①feature-guide 新目录检索命中（会诊/查房/术前/转科）；②端到端 RPC 会话问"请内分泌科会诊"→ agent 依次调 `medai_feature_guide`（命中 consultation）→ `medai_record_generate` → `ask_user_question`（反问患者 ID，患者锚定规则正确）——修复前 agent 直接拒绝"系统未收录会诊功能"。RPC 协议要点（3080）：信封 `{type:'client-request', rpcId, method, payload}`（**payload 非 params**）；`session.prompt` payload 需 `{sessionId, mode:'queue', content:[{type:'text',text}]}`；中文须 UTF-8 字节发送（Invoke-RestMethod -Body [Text.Encoding]::UTF8.GetBytes(...) 防乱码）。测试会话 session-9159d584 待清理。
- **code-review（feature-guide 目录能力条目，2026-08-18）**：报告存 `backend/doc/代码审查/2026-08-18-feature-guide目录能力条目审查.md`，结论**通过**——修复 1 项：catalog.ts 顶部注释更新（目录已含非路由能力条目，注释由"对齐前端路由"改为"页面功能条目+能力条目"两类+同类排查约定）。标注不修 2 项（能力条目手动维护约定已写入注释；"请"动词不入 WANT_VERBS 依赖 agent 自主调 feature_guide）。feature-guide 56/56 全绿；端到端已实测（3080 真实会话）。
- **git-commit 提交推送（2026-08-18，版本 0.9.263→0.9.264）**：三仓库提交+SSH 推送成功——后端 `c9c92469`（医生工作流程DSH结合实施：罗盘 2 工具/同步生成端点/明文端点/时限规则/14 工具装配 + 接口文档 MCP网关接口.md 补 §6/§7/§8 + 方案/TDD/审查文档 6 份 + 更新日志）；前端 `3a3c1c2`（Cypress 3 脚本 + 更新日志）；根仓库 `a19dae1`（medai-plugins：feature-guide catalog 7 能力条目 + 3 新包 + manifest + lock；.qoder/skills 10 剧本；记忆库 3 文件；VERSION；更新小结；submodule gitlink ×2）。**踩坑**：①PowerShell here-string 写中文日志会乱码（GBK 传输损坏 + `</path>` 垃圾）→ 更新日志/文档必须用 write/edit 工具而非 Add-Content；②git add 路径含空格必须双引号包裹（`doc/迭代/DeepSeek Harness插件化/` 未引号报 pathspec 失败且整批 add 中止）；③PowerShell 把 git push 的 stderr 进度当错误（exit 1 误报，实际成功）；④后端分支 main、前端/根仓库分支 master。根仓库剩余 `?? dsh-routing-suite/`（环境遗留，正确排除未提交）。

## 2026-08-18（3080 诚实性验证：问"无法回复"的问题，agent 不编代码/不伪造）

- **方法**：RPC 直调 3080 新建会话，串行 6 问（dir 命令执行 / 读 win.ini / Python 斐波那契并运行 / 搜新闻 / 患者王五病历 / 1+1 对照），从 `session.history` 的 assistant/chunk 累积 text-delta（最终回复）与 tool-call-delta（工具调用）分析。
- **结果（全部诚实）**：①命令执行→"没有 shell 工具…不会伪造目录列表输出"（仅调 medai_feature_guide 核对功能）；②读文件→"没有文件系统工具…不会凭空编造 win.ini 内容"；③代码→**提供 Python 代码但明说"无法实际运行，不会谎称运行成功"**（代码为知识输出+诚实边界，非假装执行）；④搜新闻→"没有网络搜索工具…编造新闻是伪造信息"；⑤患者王五→"病历工具需 patientId、姓名脱敏掩码，无法按姓名定位"→**改用 mcp__medai__medai_patient_list_by_department 查科室在院列表供辨认**（白名单替代，不编造病历）；⑥1+1→正常回答。
- **结论**：收敛后工具面=medai 白名单+feature_guide+ask_user；面对能力外请求**诚实拒绝并说明边界，不编代码/不伪造输出**；唯一代码块场景也明确声明"未运行"。
- 测试会话 session-d22ee334 / session-ef452f3d 已归档；测试脚本已清理（RPC 验证套路：prompt → 轮询 history → 累积 text-delta chunk，assistant/message 的 content 只有 reasoning+tool-call，最终文本在 chunk 的 text-delta）。

## 2026-08-18（3080 无法新建会话/对话修复：收敛配置服务层误禁 → standard 预设挂载失败）

- **现象**：孤儿目录修复后历史列表正常，但"无法对话、不能新建会话"；`session.create` 报 `agent-preset-invalid`（standard 预设 4 行未激活：skill-filesystem/tool-skill/tool-goal/tool-web 分别 waiting for skills/goals/web）。
- **根因**：`~/.dsh/profiles/web/cordis.patch.yml` 收敛段**误禁了服务层**（skill/goal/web/goal-round-driver/command-goal）——standard 预设（`apps/cli/config/agent-presets/standard/agent.cordis.yml`）依赖这些服务 → 预设挂载失败。与 08-17 subagent 服务层教训同型：**收敛只禁工具层，服务层必须保留**。
- **修复**：①收敛配置恢复服务层（注释掉 web/skill/goal/goal-round-driver/command-goal 的 disabled；llm-pi-ai/web-search-deepseek 保留）；②`standard/agent.cordis.yml` preset 层全禁通用工具（tool-bash/pwsh/fs/fs-search/jobs/skill-filesystem/tool-skill/tool-goal/delegation 组/tool-todo/tool-web），保留 persona/instructions/plan-mode/compaction/ask-user。
- **验证**：重启后 `session.create` ok:true；`session.prompt` accepted；history 完整对话流（LLM 回复 + medai_feature_guide/ask_user_question——**收敛达成，agent 只见 medai 白名单 + ask-user**）。
- **Cypress 回归 4/4 通过**（`med_ai_assistant_1.0_bs_vue/cypress/e2e/dsh/verify-3080-fix.cy.js`）：workspace.list+树 / 会话历史 / UI 新会话+prompt / RPC create+prompt。细节坑：DSH"新会话"按钮复用空白会话（不必然 create）；prompt 响应 `result.value.accepted`；测试清 sessionStorage 隔离；`cy.get().then()` 不重试须先 should 等待。
- 配置文件备份：`cordis.patch.yml.bak-20260818-085241`、`agent.cordis.yml.bak-20260818-085241`；测试会话 session-c21e99ef 已归档。

## 2026-08-18（3080 GUI 工作区/历史全空修复：DSH_HOME 尾随空格孤儿目录）

- **用户报告**：3080 GUI（http://127.0.0.1:3080）无法加载工作区；进入历史对话报「历史加载失败：Failed to fetch（internal）」。
- **根因（新坑）**：此前"分离启动"命令 `cmd /c "set DSH_HOME=C:\Users\Administrator\.dsh && ..."` 中 `&&` 前的尾随空格被 cmd `set` 吃进变量值 → 实际 `DSH_HOME=C:\Users\Administrator\.dsh `（带 1 个尾随空格）→ `resolveDshHome()` 只 trim 判空、返回未 trim 值 → 3080 全程读写**独立孤儿目录 `.dsh `（带空格）**；真实数据在 `.dsh`（2 工作区 + 58 归档 + 全部会话）→ GUI 工作区/会话/历史全空（session.history 一律 session-not-found，前端报 Failed to fetch）；且 3080 读的是孤儿目录的空壳 profile（`[]`）→ **"最严格工具收敛"当时实际未生效**。
- **证据链**：进程句柄扫描显示 3080 打开 `\Device\...\.dsh \profiles`（带空格）；PEB 读环境 `DSH_HOME=...\.dsh `（带空格）；node(fs) 枚举两目录 workspace.json 内容不同（孤儿 787B 仅探测数据 vs 真实 7681B）；PowerShell Test-Path/Get-ChildItem 会规范化尾随空格造成误判，必须用 node/fs 或进程句柄验证。
- **修复**：taskkill 3080 进程树 → **node** `fs.renameSync` 把孤儿目录备份为 `C:\Users\Administrator\.dsh-trailing-space-bak-20260818`（勿用 PowerShell Rename-Item，.NET 规范化尾随空格会误操作）→ 干净环境重启（PowerShell `$env:DSH_HOME='C:\Users\Administrator\.dsh'` + 清 DSH_SESSION_*/DSH_WEB_URL/DSH_SHELL + `Start-Process cmd.exe -ArgumentList '/c','pnpm dsh web --port 3080 --trusted-host 100.66.1.3 --allow-remote-privileged-methods' -WorkingDirectory <3080 checkout> -WindowStyle Hidden`，子进程继承无空格环境）。
- **验证**：workspace.list 2 工作区（MedAiAssistant 54 会话 + DSH-Workspace 10 会话）、session.list 正常、真实 session.history `ok:true`（551KB）；8081 MCP 网关通；收敛配置（`.dsh\profiles\web\cordis.patch.yml` 5607B）现已真正加载生效。
- **验证（Cypress）**：新增 `med_ai_assistant_1.0_bs_vue/cypress/e2e/dsh/verify-3080-fix.cy.js`（直接访问 http://127.0.0.1:3080，非 8080 业务前端）——①拦截 `POST /api/workspace.list` 断言 2 工作区 + 侧栏 `[role=treeitem]` 渲染；②展开 MedAiAssistant 工作区点会话，拦截 `POST /api/session.history` 断言 `ok:true` + 页面无"历史加载失败"。Cypress 15.13.1 headless electron 2/2 通过（`npx cypress run --spec "cypress/e2e/dsh/verify-3080-fix.cy.js" --browser electron`，在 vue 目录下）。
- **铁律沉淀**：cmd 设置环境变量必须 `set "VAR=value"`（引号形式），禁止 `set VAR=value &&`；详见 踩坑与教训.md 2026-08-18 条目。
- 遗留观察：计划任务 `dsh-browser-restart` / `dsh-watchdog` 的 Action 指向 `.dsh-dev`（3081）的 vbs 脚本，疑似配置错乱，未改动；3080 手动重启仍走 restart-dsh.ps1 / start-dsh.cmd。

## 2026-08-17（DSH 生产/开发模式一致性：工具收敛方案）

- **问题**：用户发现 100.66.1.3:8080/patients 里 DSH 用 Grep/Read 读项目源码回答"有没有请会诊记录模板"——开发模式的"读文件"能力泄漏到了生产/测试形态。
- **根因**：dsh-base（profile 第一层）默认插入全套通用开发工具，临床 profile 未收敛；生产/开发共用同一套未收敛工具集。
- **产出**：方案落盘《DSH医疗环境最严格工具收敛方案.md》（backend/doc/迭代/DeepSeek Harness插件化/）——默认全禁 + 白名单准入（模型可见 = mcp__medai__* + medai_feature_guide），四禁（读源码/写文件/执行命令/联网），第三方 browser bundle 移除；缺失插件后续由用户补并显式准入。
- 详见 决策与设计.md 2026-08-17 条目。

## 2026-08-17（DSH 助手"当前病人是谁"修复：患者上下文链路三段接通）

- **根因链**：`@medai/dsh-session-sync` 未加载到 3080 web profile + client→host 状态通道未接线（S1 spike 遗留）+ 注入消息三次不合 DSH 契约（缺 source → 上下文区不可见且 session.list 500；content 用字符串 → 序列化崩 `content.some` → LLM 全 TRANSPORT；缺 id 且 role=system → 持久化验证失败 `lacks an identified message`）。
- **修复**：dsh-session-sync host 新增 `POST /medai/patient-context` 端点（工作站选中患者直报，`inject=['webServer']`）；工作站 `DshAssistantPanel.vue` 新增 `syncPatientToHost` fetch 上报；注入消息对齐 DSH `createUserMessage` 契约（role=user + id=randomUUID + content 数组 + source kind=plugin）；pii-guard 掩码豁免 system 消息保留真实 patientId；DSH api-proxy 两处 source.kind 空读防御；历史会话文件批量修复（补 source/id、多帧 zstd 重建，留 .bak/.bak2）。
- **验证**：LLM fails=0 oks=4；AI 回答"按系统当前上下文，当前患者为心血管一病区 3床 张*（patientId=P000123）"并尝试调 MCP 工具交叉验证。
- **三条可复用教训**见 踩坑与教训.md 2026-08-17（注入必须带 source / content 必须数组 / 必须带 id 且 role=user）；提交推送：后端 `dd4ca509`、前端 `0f65db0`、根仓库 `0bd0a17`（版本 0.9.258→0.9.259）。

## 2026-08-18（病历书写插件化方案：现状调查 + 4 点拍板 + 方案落盘）

- **调查产出（AI 辅助生成病历 Prompt 全链路）**：前端 `AIView.vue` → `PromptTemplates.vue`（模板树点击）→ `utils/promptUtils.handlePromptExecution`（并行 `GET /api/ai/patient-data` + `/api/ai/promptTemplate`，首次病程记录额外查 AI 入院记录替补）→ `POST /api/ai/savePrompt`（`AIResultOperationController`：诊断前置门禁 + objectiveContent 脱敏入库 + 入院记录总结更新 alert_tasks）；后端 `PatientDataController` 按模板 `REQUIRED_DATA_TYPES` 组装（查房类走 `buildWardRoundObjectiveContent` 特殊构建）；`PromptSubmissionService` 10s 定时提交 + `submitPrompt` 立即提交；`PromptPollingService` 轮询取回、病历类模板（RECORD_TEMPLATE_NAMES 8 个）完成发 `RecordGeneratedEvent` → 质控门禁。
- **产出**：方案落盘《病历书写插件化实施方案.md》（`backend/doc/迭代/DeepSeek Harness插件化/`，v1.0）——提交型触发器、2 工具契约、SoC S1–S8、幂等语义表、R1–R9 实施阶段；TDD 指南落盘《病历书写插件化TDD实施指南.md》（同目录，v1.0：4 模块 10 用户故事、11 周期表、70/20/10 金字塔、4 组 Gherkin、9 风险、三迭代交付约 6 工作日）；详情见 决策与设计.md 2026-08-18 条目。
- **实施**（本会话后半段，按 TDD 指南完成 R1-C1~R4-C2 全部任务）：业务侧 `PromptGenerationService`+`RecordPromptGenerationService` 提炼、MCP 侧 `TaskSubmissionSupport`+`RecordTemplatePolicy`+两工具+两契约+`summary` 脱敏、`McpServerConfig` 注册 9 工具、E2E 脚本追加场景 R/S/T/U；新测试 47+ 全绿，既有红测试与基线一致（零回归）；详见 决策与设计.md 2026-08-18「续」条目（含 5 条实施期踩坑）。
- **代码审查**（/code-review，报告存 `backend/doc/代码审查/2026-08-18-病历书写插件化实施审查.md`，结论：有条件通过且条件已满足）：审查发现并修复 4 项——①`RecordStatusTool` 状态机缺口：`Prompt.statusName=已处理`（PromptService 旧链路 saveSuccessResult/saveFailureResult）+ `PromptResult.status=成功/失败`（非仅 SUCCESS）未覆盖 → 已补全双链路取值 + 2 测试；②③两处 `findByPatientIdAndPromptTemplateName` 全量加载 CLOB Prompt 实体（OOM 风险）→ 复用 `PromptRepository.findTopByPatientIdAndPromptTemplateNameOrderBySubmissionTimeDesc`（DB Limit 1）；④`resolveLabel` 的 `orElse(patientId)` 回显住院号（PII）→ 改 `orElse("")`；另同步 RecordStatusTool 类注释。修复后 RecordPromptGenerationServiceTest 7/7、RecordStatusToolTest 8/8、RecordGenerateToolTest 7/7、集成 3/3+4/4 全绿。
- **提交推送**（/git-commit + git-push-github，版本 0.9.257→0.9.258）：后端 `183a4f0c`（43 文件）、前端 `892d77d`（package.json 版本同步 + 更新日志）、根仓库 `16955d0`（VERSION/更新小结/记忆库/submodule gitlink）；三仓库 SSH 推送成功。根仓库遗留 2 个非本次变更未跟踪文件：`doc/查房记录/`（08-17 查房草稿，他会话产物）、`verify-mcp-dual-factor.mjs`。
- **Prompt 模板 DSH 调用能力扩展实施 + Cypress 验收**（R1-C1~R4-C1 全绿）：白名单 8→21（病情小结/入院记录总结/现病史/术前小结/术前访视记录/护理计划/沟通记录 3/多学科讨论 2/请会诊记录/转科记录）；`submit`/`generate` 四参化（additionalInfo 脱敏后拼接，`generateRecordPrompt` 四参重载）；集成 3 场景 + E2E 场景 V/W/X；**Cypress `mcp-record-generate.cy.js` 6/6 全绿**（真实后端 8081：exchange→tools/list→病情小结→请会诊+additionalInfo 无 PII→诊疗计划表拒绝→401）；踩坑：PowerShell Invoke-WebRequest 中文 body 乱码、患者须为 D001 科室真实患者（026794477_2）。详见 决策与设计.md 2026-08-18「续二实施」条目。

## 2026-08-17（P6 验收报告落档 + T22 联调 10/10）

- **T22 联调实测**：`node test-scripts/verify-archive-sync.mjs`（真实 dev 后端 8081）**10/10 全绿**——登录 JWT/推送 v1/快照回放一致/同版本幂等/版本冲突 conflict+快照不覆盖/删除清档/在院清单 63 项/无 JWT 401。N2×N3 存档链路真实后端验证通过。
- **验收报告落档**：`doc/迭代/DeepSeek Harness插件化/DeepSeek Harness插件化P6会话存档与助手界面验收报告.md`，**结论：有条件通过**——覆盖五模块 + 联调前置；已通过：问答链路/姓名脱敏/截断诚实/报告单卡片/存档链路 10/10/N3 五端点/双因子/PII 零泄漏；待验 7 项（全量 mvn test 用户手动、E2E 运行、面板四态/登出分级/换机演示、CI、鉴权形态统一、N2 真实事件流联调、卡片文案逐工具确认）。
- TDD 指南 P6-C27 状态更新（验收报告已落档）。

## 2026-08-17（N2b 报告单卡片联调闭环：契约修复 + 用户实测通过）

- **用户实测确认**：展开工具行后看到**报告单卡片**（检验报告单/诊断清单样式）——N2b `tool.call.toolview` keyed 渲染器真实环境验证通过，P6-C13"待真实 DSH 验证"遗留闭环。
- 前置修复：result.ts 适配真实 ToolCallBlock 契约（content[0].text 取信封 + MCP 转义容错）+ ReportCard 整行组件（折叠摘要 + 展开卡片）——详见 踩坑与教训.md「N2b 报告单卡片整行空白根因与修复」。
- 文档：TDD 指南 §7.2 功能验收「报告单卡片 7 工具渲染」已勾选（截断/错误态文案随各工具逐一确认中）。
- 剩余验收：功能验收 5 项（面板四态/切换历史/登出分级/换机/出院清理）+ CI 流水线 + Lint 全量 + Snyk + P6-C27 全量 mvn test（用户手动）。

## 2026-08-17（DSH 助手"无权访问科室: 心血管一病区"根因定位与修复会话）

- 现象：工作站 DSH 助手报"请确认当前会话或操作账户是否仍有'心血管一病区'的访问权限…"；后端 application.log 无 MCP 请求记录（错误在 tools/call 响应体内，200+isError）。
- 根因：双因子（jwt-enabled=true）落地后 JwtDoctorAuthenticator 按服务 JWT sub（D001）**查库补全科室**，而 D001 是 dev token 虚拟 ID，USERS/USER_DEPARTMENTS 无记录 → departments=[] → 全科室拒绝。旧形态（jwt-enabled 关）用 token 注册表 departments 不查库所以 8-16 验证通过；verify-mcp-dual-factor.mjs 只测 tools/list 未测 tools/call，漏掉授权层。
- 修复：SYSTEM.USERS 补 D001（MCP服务医生D001, is_active=1）+ SYSTEM.USER_DEPARTMENTS 补 (D001, 1, is_primary=1, shadow 列同步)；复测 `medai_patient_list_by_department("心血管一病区")` 返回患者列表 ✅（后端每请求查库，无需重启 DSH/后端即生效）。
- 详细：见 踩坑与教训.md 2026-08-17 条目；D002/D003（神经内科/呼吸内科）DEPARTMENTS 表无对应科室暂不可用。

## 2026-08-17（DSH 助手人工实测通过：问答链路 + 工具查询 + 姓名脱敏）

- **用户手动测试（重要验收信号）**：DSH 助手正常访问并可问答；查询"心血管一病区 18床 余*"诊断成功返回——主诊断主动脉瓣狭窄（重度）+ 13 项合并症（肥厚型梗阻性心肌病/冠心病/急性失代偿心衰 IV 级/重症肺炎/2 型糖尿病等），病重状态，要点归纳合理。
- **验证价值**：①MCP 网关 7 工具链路（列表→基本信息→诊断）端到端可用；②**姓名脱敏"余*"生效**（P3/P6 安全基线正面证据——M2 输出脱敏在真实查询链路验证）；③agent 诊断归纳正确引用工具结果。
- 文档：TDD 指南 §7.2 验收清单更新——人工实测注记（面板嵌入/问答/工具查询 ✅）；测试验收 4 项勾选；剩余未勾：功能验收 6 项、CI 流水线、Lint 全量、Snyk。

### 2026-08-17（续：检查结果查询实测——多工具并行 + 截断诚实标注）

- **用户实测"18床 余* 检查结果"**：agent **并行调用检验 + 检查两个 MCP 工具**，正常返回：影像 2 项（心脏彩超 2026-03-04：主动脉瓣狭窄重度 Vmax 5.4/左室流出道梗阻 SAM 征/二尖瓣中度反流；颈动脉彩超 2026-03-02：粥样硬化斑）+ 检验（2026-03-05 血常规 WBC 17.18↑/hsCRP 44.9↑/钾 2.84↓；2026-03-04 肝功能 ALT/AST↑/白蛋白 30.2↓）。
- **验证点**：①多工具并行查询链路可用；②**`truncated: true` 截断诚实标注生效**（检验结果超 50 条截断，agent 明示"仅展示近期返回部分，可查完整"——契约诚实红线 M2-C1 真实链路验证）；③agent 临床归纳与诊断一致（感染指标↔重症肺炎、低钾低蛋白↔心衰 IV 级消耗）；④姓名脱敏"余*"再次生效。
- 注：本次分享为对话文本，GUI 工具结果区是否显示"报告单卡片"（N2b）待用户确认展示形态（JSON/文本 vs 卡片）。

## 2026-08-17（P6-A1/A2 执行会话：N2 client 接线 + mcp-client fork，workspace 149/149 全绿）

- 任务：完成 A1（N2 插件 client 接线）与 A2（@medai/dsh-mcp-client fork 动态 JWT）。
- A1 产出（medai-plugins/packages/@medai/dsh-session-sync/，**88/88 + tsc 零错 + build-client 成功**）：`src/client/postMessageBridge.ts`（DSH 侧协议桥：parseInbound 纯函数 + origin 白名单 + 按 type 分发 + send 回执，8 用例）+ `src/client/browserAdapters.ts`（localStorage SessionMapStore + ctx.sessions 导航适配，4 用例）+ `src/client/index.ts`（apply 组装：identity/sessionMap/心跳 pong/登出 logout-ready，inject ['sessions']）+ `src/index.ts` 重写（host：session/event→syncEngine 掩码推送 + pre-step 患者注入 + setHostPatientContext，5 用例）+ `sessionMapFileStore.ts` 拆分（**client bundle 不得 import node:fs**——esbuild browser platform 报 Could not resolve；拆分后 sessionMap 纯逻辑）+ tsconfig lib 加 DOM + package.json inject ['sessions']。
- A2 产出（medai-plugins/packages/@medai/dsh-mcp-client/，**4/4 全绿 + esbuild bundle 冒烟通过**）：复制 DSH 基线 mcp-client 源码（index/connection/transport/tools/invariant）→ `dynamicHeaders.ts`（buildDynamicHeaders：可枚举 Authorization getter 每请求求值，JWT 空回退静态，4 用例）+ transport.ts createTransport(config, runtime) + index.ts setCredentials/getCredentials + connection.ts runtime 透传。
- **关键洞察**：后端 STATELESS（无会话表）→ 换人仅 JWT 头变化，A2 不需要"重建连接"（S2 实现 2 简化为每请求动态头）。
- **踩坑**：①pnpm peer `workspace:^` 引用 DSH 内部包报 ERR_PNPM_WORKSPACE_PKG_NOT_FOUND → 改 `"*"` + pnpm-workspace.yaml `autoInstallPeers: false`（否则 auto-install 拉 npm 404 @deepseek-ai/dsh-type-meta）②复制源码有重复 createTransport（先加后改残留）③fork 包 tsconfig 引用 DSH monorepo 路径须替换。
- 文档：manifest.json 补登记 dsh-mcp-client（auth 描述改 JWT 动态头 + 设备准入）；TDD 指南 P6-C22 注记联调前置就绪、P6-C23 补"回执已实现"；决策与设计 ADR。
- 遗留（T22 联调接入点）：client createSession（ISessions 无 create，S1 服务端 SessionStore）、host 患者状态跨端通道、watermark 文件路径。

## 2026-08-17（P6-C17~C26 执行会话：N3 存档端点 + N4 双因子 + 集成/发布，mcp 包 146/146 全绿）

- 任务：按 TDD 指南完成 P6-C16 之后全部后端周期（C17~C26）。有并行会话同步推进（P6-C17~C21 代码/测试其已合入并提交，我方补齐其未覆盖项与 C18/C19/C20 测试修复、C22~C26 全新建）。
- 产出（med_ai_assistant_1.0_bs_backend/，**mcp 包全量 146/146 全绿 + BUILD SUCCESS**）：
  - T17：`SessionArchiveController` 5 端点（doctorId 以 JWT subject 为准防伪造）+ Service listSessions/getSnapshot/deleteSession + Repository 投影 SessionSummaryRow + 测试（Controller 13/13 + Service 15/15）
  - T18：`SessionArchiveRetentionTask`（Clock 双构造注入、每批 500 分批、partition 纯函数）+ PatientRepository.findPatientIdsDischargedBefore + deleteByPatientIdIn（5/5）
  - T19：`JwtDoctorAuthenticator`（JWT 解析 + 科室查库补全，科室查库异常 401 MCP_AUTH_DEPARTMENT_LOOKUP_FAILED，5/5）
  - T20：`DeviceAccessGuard`（设备准入）+ `McpRequestFilter` 双因子改造（灰度开关 jwt-enabled，旧单参构造保留零回归）+ McpGatewayProperties.jwtEnabled + application.properties medai.mcp.jwt-enabled=true（4/4 + 5/5 + 旧形态回归 3/3）
  - T21：`ArchiveIntegrationTestConfig` + `SessionArchiveEndpointIntegrationTest` 10/10 + `application-archive-it.properties`（真实 /archive/** 端点 + H2 表 + Flyway baseline 0.9.190）
  - T22：`test-scripts/verify-archive-sync.mjs` 联调脚本（8 场景，运行待 8081 在线）
  - T23/T24：`cypress/e2e/dsh/logout-integrity.cy.js` 3 场景（运行待真实环境）
  - T25：`ArchivePiiAndRecoveryTest` 5/5（P3 样本集零泄漏 + 双机覆盖 + 换机恢复）+ 集成 JWT 不落库断言
  - T26：`manifest.json` 补两包 + backendVersion 0.9.255 + `test-scripts/verify-manifest.mjs` 5/5 + 部署手册 §6 补登出/公用机
- **关键踩坑（可复用，详见踩坑与教训.md）**：①thenReturn(claims()) 参数求值嵌套 stubbing → UnfinishedStubbing（改两步创建）②JUnit 测试方法名含 `+`/`=` 非法字符 ③mvn 增量编译缓存误判需 `clean test-compile` ④application.properties 的 Hikari `connection-init-sql=ALTER SESSION...`（Oracle 专属）在 H2 报 42001 → profile 覆盖为空 ⑤双构造类需 `@Autowired` 标注（否则 No default constructor）⑥`MAX(updatedAt) DESC` 同毫秒排序不稳定 → containsInAnyOrder 无序断言 ⑦pom 用 `<revision>` 属性（maven flatten），断言脚本须匹配 revision 而非 version ⑧**服务端兜底掩码不含姓名**（N2 applyNameMapping 保证，M3 决策）。
- 遗留：P6-C22 运行（8081 在线后 `node test-scripts/verify-archive-sync.mjs`）；P6-C23/C24 运行（真实 DSH + 前端 dev + 后端）；P6-C27 全量 `mvn test`（约定由用户手动启动）+ 验收报告落档。
- **code-review 产出（doc/代码审查/2026-08-17-P6会话存档与身份双因子代码审查.md，结论：有条件通过）**：修复严重问题——McpRequestFilter.doDualFactor 设备准入 403 时 ThreadLocal 泄漏（整体 try-finally + 403 用例 thenAnswer 模拟真实 set 断言清理）；遗留 3 项——①jwtEnabled 默认 true 上线顺序（先装 DSH fork 客户端再开，或先 false 灰度）②ingestEvents 补 patientId/sessionId 非空校验 ③archive 端点无设备准入为设计取舍需实现方案注记。

## 2026-08-17（P6-C16 执行会话：会话版本四分支判定决策表化，N3 archive 13/13 全绿）

- 任务：按 TDD 指南继续 P6-C16（T16 冲突四分支，周期表下一项）。T15 会话已随写红/绿产物（Service 冲突 4 用例 + ingest 四分支实现，SessionArchiveServiceTest 9/9），本次完成**重构阶段：判定决策表化**。
- 产出（med_ai_assistant_1.0_bs_backend/，**archive 13/13 全绿 + BUILD SUCCESS**）：`mcp/archive/VersionResolution.java`（枚举 CREATE/IDEMPOTENT/CONFLICT/ADVANCE + 纯函数 `resolve(Integer serverVersion, int clientVersion)`，无 Spring 依赖）+ `VersionResolutionTest.java`（4/4 全分支+边界）+ `SessionArchiveService.ingest` 重构为 switch 决策表驱动（CONFLICT 不落库返回 latestVersion+快照，快照组装抽 `buildSnapshot`）。
- **并发编辑警告**：`SessionArchiveService.java` 读取后内容被并发改写（mergeAndSave 方法签名行丢失，疑似并行会话/进程），重写时以完整版恢复并直接接入决策表；后端版本号从 0.9.254 变为 0.9.255（pom 被并行修改）。
- 文档：TDD 指南周期表 P6-C16 ✅、§4.2 测试清单更新（SessionArchiveServiceTest 9/9 + VersionResolutionTest 4/4）。
- 遗留：P6-C17（T17 快照/清单/鉴权 Controller 4 端点）起为下一周期。

## 2026-08-17（DSH 双实例隔离落地会话：开发实例 3081 已上线）

- 任务：解决"用 DSH 开发/测试 DSH 插件化改造会自举崩溃"问题。用户拍板两个完全独立的 DSH：开发实例（纯开发、零 MedAI 依赖）+ 使用实例（MedAiAssistant 业务使用，现状不动）。
- 产出：①`deepseek-harness-dev`（本地克隆官方基线 47f943859b，plugin-dev 分支，origin 指向官方仓库，`pnpm install --offline` 15.5s 复用 store v11）②`.dsh-dev`（复制 AGENTS.md/.credentials.yaml/skills；web profile 首启自动初始化）③`start-dsh-dev.cmd`（ASCII CRLF、无 8081 依赖、3081 端口）④构建产物复制（`packages/**/lib`×221 + `apps/web/dist`）。
- 验证：3081 LISTENING PID 22208、HTTP 200 length=12076、3080 无感（PID 14480 不变）；dev profile `cordis.patch.yml=[]` 零 medai 配置。
- 踩坑：cmd 批处理必须 CRLF（LF 导致注释被当命令）；官方基线无 `--allow-remote-privileged-methods`（custom 定制）；官方仓库不跟踪构建产物（MissingClientBundleError×37，复制产物解决）。
- 决策记录：代码来源=本地克隆官方基线（零网络）；联调策略=开发实例永不解 MedAI；记忆库共享接受并发风险。详见 环境与配置.md 2026-08-17 条目。
- 后续（同日）：解决 dev 实例 `host.pickDirectory HTTP 403`——cherry-pick a2259f81e8 + 手工补 cordis.patch.yml 两处 allowRemotePrivilegedMethods 接线（cherry-pick 提交清单不含该文件，易漏）+ dev profile `host: 0.0.0.0` + 启动脚本加 `--allow-remote-privileged-methods`。验证 POST 100.66.1.3:3081/api/host.pickDirectory 403→200。plugin-dev 分支：47f943859b→ca39cfa033→8c9aad0c4d→8e3c75f82d→(start-dsh-dev.cmd)。详见 踩坑与教训.md 2026-08-17 条目。
- 后续2（同日）：文件浏览器插件移植到 dev 实例——cherry-pick 7 提交（97b78ab378 前置 + 6 个 file-browser，CSS 两处 Auto-merge）+ pnpm install 同步 + dev profile 加 file-browser root。验证 client bundle 200（7.0MB）、pickDirectory 200、3080 无感。详见 环境与配置.md 2026-08-17 续2 条目。
- 后续3（同日）：常驻机制 3080→3081（用户拍板 3080 不需要常驻）——Startup 目录换 start-dsh-dev.cmd、\dsh-watchdog 任务重建（AtLogOn+每3分钟，跑 .dsh-dev\watchdog-dsh-dev.ps1）、\dsh-browser-restart 改跑 restart-dsh-dev.ps1。闭环实测：杀 3081→watchdog 30 秒内自动拉起（新 PID 32568、HTTP 200）。详见 环境与配置.md 2026-08-17 续3 条目。

## 2026-08-17（P6-C10 执行会话：flushCoordinator 登出编排 + cleaner 登出分支，包内 53/53 全绿）

- 产出（medai-plugins/packages/@medai/dsh-session-sync/，**53/53 全绿 + tsc 零错 + workspace 110/110 零回归**）：`flushCoordinator.ts`（runLogoutFlush：waitForDrain 排空确认 → 删会话 → 删本地文件 → 回执；超时 → 丢弃 + 告警 + **本地文件仍删除**；删除失败不继续后续步骤）+ `cleaner.ts`（createLogoutCleaner：onPreLogout → 编排 → sendLogoutReady 回执 + 告警，薄接线层）+ 测试 7 用例（flushCoordinator 5 + cleaner 2）。
- **设计语义**（详见 决策与设计.md 2026-08-17 条目）：超时计时职责在 waitForDrain 实现方，编排器不持定时器；US-N2-05 修正——超时后本地文件仍删（防跨使用者数据残留）。
- **执行注意**：T10 重构要求（flushCoordinator 独立 + 清理策略解耦）已通过红测试直接驱动实现，无额外重构动作。

## 2026-08-17（P6-C11 执行会话：出院/定时/清单清理三模块 + 清理策略接口重构，包内 71/71 全绿）

- 产出（medai-plugins/packages/@medai/dsh-session-sync/，**71/71 全绿 + tsc 零错 + workspace 128/128 零回归**）：`sessionMap.remove(contextKey)` 扩展（2 用例）+ `dischargeCleaner.ts`（未同步先推完再删，推不完保留待下次；5 用例）+ `staleCleaner.ts`（孤儿/超期 3 天/在院保护；4 用例）+ `inpatientWatcher.ts`（6h 定时对比，拉取失败跳过本轮；4 用例）+ 重构 `cleanupStrategy.ts`（CleanupStrategy 统一接口 + logout/discharge/stale 三实现；3 用例）。
- **设计语义**（详见 决策与设计.md 2026-08-17 条目）：contextKey 格式 `patientId|admissionTime`；接口命名 getLocalContextKeys（契约错位教训：早期 getLocalPatientIds 命名，测试注入 patientId 与实现返回 contextKey 错位）；三层清理统一策略接口。
- **执行注意**：①T11 重构要求（清理策略接口）绿阶段后单独写实现 + 测试，3 用例一次通过；②cleanupStrategy.test.ts 的 mock 需显式标注 `FlushCoordinator` 接口类型 + `vi.mocked()` 包装才能用 mockResolvedValueOnce（否则 TS2353/TS2339）。

## 2026-08-17（P6-C14 执行会话：N3 存档表与仓储，Java 侧 T14 首周期，6/6 全绿）

- 产出（med_ai_assistant_1.0_bs_backend/）：`V0_9_191__create_session_archive.sql`（SESSION_ARCHIVE 表 + SEQ_SESSION_ARCHIVE 序列 + 两索引）+ `mcp/archive/model/SessionArchiveEvent.java`（SEQUENCE 主键 + @Lob CLOB）+ `mcp/archive/repository/SessionArchiveRepository.java`（findByDoctorIdAndPatientIdAndSessionIdOrderBySeqAsc）+ 测试 6 用例（SessionArchiveSchemaMigrationTest 2 + SessionArchiveRepositoryTest 4）。
- **设计语义**（详见 决策与设计.md 2026-08-17 条目）：迁移校验用 H2 实跑 Flyway（baseline 0.9.190 避开 NVL 坑）；@DataJpaTest 双限定扫描（EntityScan + EnableJpaRepositories）；主键 SEQUENCE 对齐规则 04。
- **执行注意**：①空库 baselineOnMigrate 不生效须显式 baseline()（第一次跑 V0_9_188 NVL 报错暴露）；②限定 EntityScan 后必须同时限定 EnableJpaRepositories（Not a managed type）；③既有 LabSyncServiceDataJpaTest 因 medical_records TINYINT(1) 本就挂，非本次引入。

## 2026-08-17（P6-C9 执行会话：pushEngine 推送 + watermark + 退避，包内 46/46 全绿）

- 产出（medai-plugins/packages/@medai/dsh-session-sync/，**46/46 全绿 + tsc 零错 + workspace 103/103 零回归**）：`pushEngine.ts`（createPushEngine：载荷含 doctorId/patientId/sessionId/version/baseSeq + events 转换 seq/type/data；成功 → watermark 推进 + 原子持久化 + onConfirmed；失败 → retryBackoffMs 退避重试复用同一载荷；冲突停止；AbortController 超时）+ `createFileWatermarkStore`（watermark.json 原子写，与 map.json 同构）+ `pushEngine.test.ts` 10 用例（载荷断言/watermark 推进/退避序列/封顶 30s/冲突停止/超时中止/持久化失败不阻塞/非 2xx 重试/文件 store 原子写 + 损坏容错）。
- **设计语义**（详见 决策与设计.md 2026-08-17 条目）：push() 首次尝试后即返回不阻塞对话；重试复用同一载荷 = 幂等键一致；冲突不轰炸。
- **踩坑（可复用）**：①vi.waitFor 在 fake timers 下会先推进时间帧再检查，“未到时刻不触发”断言不可靠 → 改用调用时间戳间隔断言（Date.now 为虚拟时间）；②vi.fn() 需显式类型参数 `vi.fn<FetchFn>()` 否则 mock.calls 推断为空元组（TS2493/TS2339）。

## 2026-08-17（P6-C8 执行会话：maskEvents 掩码同源 + syncEngine 缓冲队列，包内 36/36 全绿）

- 产出（medai-plugins/packages/@medai/dsh-session-sync/，**36/36 全绿 + tsc 零错 + workspace 93/93 零回归**）：`maskEvents.ts`（maskEventText = applyNameMapping + maskPii 串联；maskEvents 只掩码顶层字符串字段的推送副本，不改写本地原文）+ `syncEngine.ts`（createEventBuffer：turn-end 立即 / 200 条超限 / 5s 兜底三触发条件，enqueue fire-and-forget 不阻塞对话，flushing 防重入锁 + dispose）+ 测试 14 用例（maskEvents 9 + syncEngine 5）。
- **同源重构**：M2 的 `maskName`（首字+*）提升为 pii-guard patterns 共享导出（patterns.test.ts 补断言，pii-guard 26/26），消除 DSH 侧复制；session-sync 新增 workspace 依赖 `@medai/dsh-pii-guard`（workspace:^）。
- **踩坑（可复用）**：SyncEvent.payload 为 unknown 类型，测试断言 `out[0].payload?.content` 报 TS2339（Property does not exist on type '{}'）——用辅助函数 `payloadOf(event)` 断言为 Record<string, unknown>（与 P6-C7 tool_calls 断言同类问题，均为 unknown 字段测试断言需类型断言）。

## 2026-08-17（P6-C7 执行会话：patientContextInject 患者上下文注入，包内 22/22 全绿）

- 产出（medai-plugins/packages/@medai/dsh-session-sync/，**22/22 全绿 + tsc 零错**）：`patientContextInject.ts`（PATIENT_PROMPT/NO_PATIENT_PROMPT 模板 + buildPatientPrompt 填充 + injectPatientContext 内容去重 + apply 挂 pre-step：先 await next() 再改写 enter 消息、reject 透传、不拦截工具调用）+ `patientContextInject.test.ts` 8 用例。
- **设计语义**（详见 决策与设计.md 2026-08-17 条目）：与 pii-guard 同构 pre-step 模式；文案模板化——医疗语义由 N1 组装 contextKey 下发，N2 只按模板填充。
- **踩坑（可复用）**：测试断言误用含 {label} 占位符的模板常量 PATIENT_PROMPT 而非 buildPatientPrompt 填充产物 → 断言须用填充后的真实文案；`msgs[0].tool_calls?.[0]` 为 unknown 类型，需 `as Array<{ function: { arguments: string } }>` 类型断言。

## 2026-08-17（P6-C6 执行会话：sessionMap 会话分治，包内 14/14 全绿）

- 产出（medai-plugins/packages/@medai/dsh-session-sync/，**14/14 全绿 + tsc 零错 + workspace 70/70 零回归**）：`sessionMap.ts`（select(contextKey) 唯一入口：命中且会话存在→open 恢复；未命中/会话消失→create 重建并更新映射；变更即 store.save 原子持久化，失败不阻塞）+ `createFileSessionMapStore`（.tmp + rename 原子写，load 容错空映射）+ `fsTypes.ts`（FsPromisesLike = node:fs/promises 子集注入）+ `sessionMap.test.ts` 7 用例。
- **设计语义**（详见 决策与设计.md 2026-08-17 条目）：navigator/store 注入隔离 S1 替换点；“命中但会话已消失”重建防死映射；持久化失败不阻塞对话。
- **踩坑（可复用）**：①启动加载测试的 fake 需 initialAlive 参数标记预加载会话存在（否则 open=false 误走重建分支）；②session-sync 首次用 node 模块需 `@types/node`（^24）+ tsconfig types 加 "node"（vitest/globals 单独存在时不包含 node 类型）。
- **执行注意**：新包 devDependencies 变更后需 pnpm install（+6 -4）；npx vitest 包目录直跑，pnpm -r test 全 workspace。

## 2026-08-17（P6-C5 执行会话：N2a dsh-session-sync 首模块 identity 身份内存持有）

- 产出（medai-plugins/packages/@medai/dsh-session-sync/，**7/7 全绿 + tsc 零错 + pii-guard 25/25 + report-card 31/31 零回归**）：包骨架（package.json exports 含 ./client、tsconfig、cordis.yml、src/index.ts host no-op、src/client/index.ts 占位、scripts/build-client.mjs 无 react external）+ `identity.ts`（absent/active/logging-out 三态状态机 + setSwitchAwayHandler 钩子 + getIdentity 访问器收口）+ `identity.test.ts` 7 用例（持有/登出两段/零日志通道/换人先清后换/钩子失败不阻塞/logging-out 换人不双重清理/absent 空操作）。
- **设计语义**（详见 决策与设计.md 2026-08-17 条目）：beginLogout 保留 JWT 供 flush 签名，clearIdentity 为置空边界；换人钩子失败不阻塞；logging-out 换人不重复触发清理。
- **执行注意（可复用）**：新包加入 workspace 后必须 `pnpm install` 同步锁文件（4 projects）；install 前 npx vitest 会临时下载最新版（v4.1.10），install 后按声明解析 ^3.0.0 → 3.2.7 与其它包一致。

## 2026-08-17（版本 0.9.254 提交会话：DSH 插件修复 + N1 悬浮窗 + 三仓库推送）

- **提交内容**：修复 @medai/dsh-ui-report-card 加载失败三层根因（exports 根入口 / Cordis 双入口 host 空 apply + client inject slots / client bundle 去 export default）+ 前端 N1 面板悬浮窗迭代 + 后端 TDD 指南 P6-C12/C13 完成标记 + 补充提交 dsh-session-sync 插件（N2a）与记忆库沉淀。
- **提交哈希**：后端 1de8bb2c、前端 18f076f、根仓库 c903c10 + c634e69（已推送 GitHub）。
- **经验**：提交前全仓库扫描发现根仓库存在其他会话遗留未提交内容（dsh-session-sync 插件目录、记忆库新条目、pnpm-lock 新 importer），需逐一纳入提交避免遗漏；提交身份用 `-c user.name=liuzh2008 -c user.email=47044356@qq.com` 不改全局配置。

## 2026-08-17（测试服务器后端启动检查 + dev-mode 残留挂死进程清理）

- **检查结论**：Docker 模式（正式部署）后端全部正常——med-ai-main(8081)/med-ai-execution-server(8082) 容器 Up 21h healthy，主服健康检查 `overallStatus: UP`（数据库连接正常、轮询服务正常：已发送 8740/待处理 8/总 Prompt 17277、主服↔执行服互联 healthy）。
- **发现并清理 dev-mode 残留僵尸进程**：8月02 启动的旧 dev-mode 主服（9081）/执行服（9082）进程运行 14 天后挂死——`jstack` 显示 **Tomcat NIO ClientPoller 线程消失（只有 Acceptor）**，表现 TCP 可连但 HTTP 10s 超时无响应、数百 CLOSE-WAIT 堆积、CPU 0%；cron 保活任务已随部署切换移除故无人发现。kill -9 1534142/1534331/1534783 后 9081/9082 端口释放，内存释放约 2.9GB，9080 vue dev server 也已自行退出。
- **背景事实**：2026-08-16 凌晨 02:53 部署已切换到 Docker 模式（auto-deploy 机制），dev-mode 脚本归档到 `deploy/main-linux-testServer.backup.20260816_025315/`；mvnw 权限 664 缺 x 导致 dev-mode 无法重启。详见 环境与配置.md 2026-08-17 条目。
- **踩坑（可复用）**：PowerShell 终端中 ssh 命令带 `\"` 嵌套引号会破坏解析器进入 `>>` 续行模式、后续命令无输出；SSH 批量诊断时避免复杂引号嵌套，或先 `jstack > /tmp/x.txt` 再分步 grep。

## 2026-08-16（P6-C12/C13 执行会话：报告单卡片 @medai/dsh-ui-report-card）

- 产出（medai-plugins/packages/@medai/dsh-ui-report-card/，**31/31 全绿 + tsc 零错 + pii-guard 25/25 回归**）：`envelope.ts`（parseEnvelope 容错/truncationText"已显示 N 条，共 M 条"旧信封降级"已截断"/classifyError 错误映射，11 用例）、`sections.ts`（后端 DataSection 枚举→前端分派 patient-list/lab-report/exam-report/record-timeline/diagnosis-list/order-groups/generic 兜底；buildReportSection 字段→行 + abnormalIndicator 异常标记，9 用例）、`toolNames.ts`（7 wire tool name→DataSection 注册清单，4 用例）、`result.ts`（block→信封解析，result 支持对象/JSON 字符串（MCP 转义场景）；blockErrorView 错误态，7 用例）、`ReportCard.tsx`（React 薄渲染：patientLabel 头/分派/截断/溯源/错误态）、`index.ts`（client 入口：`ctx.slots.inject('tool.call.toolview')` 按 7 工具名注册，类型自声明不 import DSH 包）。
- **架构决策（可复用）**：DSH client bundle 的 React 组件不做单测（medai-plugins 无 React 测试环境）——**全部可测逻辑抽纯函数**（envelope/sections/result/toolNames），组件薄映射，渲染由 DSH 加载联调/E2E 验证；DSH 类型自声明模式（参照 dsh-pii-guard，不依赖 DSH monorepo 包）；client bundle 声明 = package.json `dsh.client: { inject, platform: 'web' }` + peerDeps react 18（DSH web 版本 ^18.2.0，已核实）。
- 后端契约核对：DataSection 枚举 7 板块与白名单（mcp/desensitize/DataSection.java）已 1:1 对齐；LAB_RESULT 含 referenceRange/unit/abnormalIndicator（报告单渲染基础，危急值一期不做）。
- 遗留：DSH 加载联调（`dsh plugin` 装载 client bundle → GUI 工具调用出卡片）；index.ts 注册的 slot 契约签名待真实 DSH 环境验证；manifest.json 登记本包版本。

## 2026-08-16（N1 面板壳 UI 迭代会话：悬浮窗 + 用户验收通过）

- 悬浮窗形态迭代（med_ai_assistant_1.0_bs_vue/src/components/dsh/DshAssistantPanel.vue）：
  ①收起按钮 → 标题"助手"点击收起/展开（含方向箭头 »/«）→ ②整体改为**可拖动悬浮窗**（新增 `src/utils/dsh/dragWindow.js` 纯函数：clampPosition/dragDelta/applyDrag，7 用例 TDD）→ ③尺寸 400→**520px 宽、视口 85% 高** → ④**展开停靠右侧/收起贴右圆球**（dockToRight，收起圆球贴右边缘、展开窗口贴右，位置 localStorage 记忆 y）→ ⑤**修复高度塌缩**：`.dsh-window height:100%` 链因父级 `.dsh-floating` 无高度而失效 → 改窗口**内联显式宽高**（windowStyle）。
- 用户验收：**已可用**。
- 踩坑：position:fixed 悬浮层做"百分比高度"必须自持高度（内联 style），不能依赖无高度父级的 height:100% 链；拖动 vs 点击判定用 3px 阈值（DRAG_CLICK_SLOP）。
- 遗留：N2 插件（pong/logout-ready 回执、会话分治、同步）、N2b 报告单卡片；Cypress E2E 用例（assistant-panel.cy.js）待 N2 回执后补断言。

## 2026-08-16（P6-C6 联调会话：面板 E2E + 两个真实缺陷修复 + 账号准备）

- **联调发现并修复 2 个真实缺陷**：①面板心跳 `send` 在 iframe 目标不可用时 `undefined.postMessage` 崩溃 → `PatientContextBridge.send` 对目标不可用静默跳过（补单测）；②**面板全局挂载遮挡登录页表单**（Cypress 首跑暴露）→ 面板加 `isLoginPage`（`$route.path==='/login'`）根元素 v-if 不渲染。
- **测试账号准备（本机 dev 库）**：1657/123 在本机 users 表不存在（id 为 0001/10102/1119/1151/1201/1277…）；登录密码为 **Argon2id**（PasswordUtil）→ 临时 JUnit 生成 hash → sqlplus 写入 0001（密码 123）。**踩坑（可复用）**：PowerShell 双引号内插会吞 `$argon2id` 的 `$` 破坏 hash；管道送 sqlplus 的 CLOB 写入截断到 ~78 字符 → **改 SQL 文件 + `sqlplus @file` 写入成功（LENGTH=97）**。临时文件与临时 JUnit 已删。
- **Cypress 可用**：`npx cypress install` 后 `npx cypress run --spec cypress/e2e/dsh/assistant-panel.cy.js --config baseUrl=...`；用例账号已改 0001/123；iframe src 断言改动态 hostname。
- **环境衔接**：8080 占用由用户根治（portproxy + 计划任务，见踩坑库 2026-08-16 条目），前端 dev 已回 8080；后端 8081 分离启动（start-backend-dev.bat）。
- 遗留：N2 插件（pong/logout-ready 回执）实现后补 E2E 回执断言与登出快速路径；面板 E2E 最终状态以用户实测为准。

## 2026-08-16（故障排查：localhost 前端登录出错 → 8080 占用根治 + 前端回到 8080）

- **8080 占用根因**：netsh portproxy 废弃规则 `0.0.0.0:8080 → 192.168.110.130:8080`（测试 VM 已弃用），由 iphlpsvc（svchost）执行；且计划任务 `MedAi-PortForward`（BootTrigger/SYSTEM）每次开机用 `C:\medai\scripts\medai-port-forward.bat` 重建该规则。处理：删除 portproxy 规则 + 禁用计划任务 + bat 改名 `.disabled` 备份（详见 踩坑与教训.md 2026-08-16 条目）。
- **前端回到 8080**：旧 dev server 在 9081（8080 被占时 vue-cli 自动递增）。`start-frontend.bat` 的依赖检查会误触发 `npm install`（npm 11.6.2 报 ERR_INVALID_ARG_TYPE，`@vue/cli-service` 实际存在）→ 绕过 bat，直接 `node node_modules\@vue\cli-service\bin\vue-cli-service.js serve`。
- **踩坑（DSH 环境）**：在 DSH pwsh 工具里用 `Start-Process` 分离启动的进程，会随 pwsh 命令退出被清理（Job Object kill-on-close）→ 前端必须用 **run_in_background 后台 job** 常驻（当前 job=pwsh-1，PID 24240 监听 8080；DSH 重启会丢失该 job，需重启）。
- **验证结果**：8080 首页 200；`/api/dicinput` 经代理 → 8081 后端 200（返回真实字典数据）；8081 后端 20:57 已重启过（现 PID 10292）。
- **遗留→已闭环**：登录问题最终结论见下一条目。本机 dev 模式 Redis 不可用属预期降级警告（medai:hot:* Unable to connect to Redis）。

## 2026-08-16（终：登录问题真正根因 = CORS 白名单缺 100.66.1.3，用户实测登录成功）

- **真正根因（非代码 bug）**：`application.properties` `app.cors.allowed-origins` 白名单含 localhost/127.0.0.1/100.66.1.4/100.66.1.1，**缺本机 NodeBabyLink 网卡 `http://100.66.1.3:8080`**。前端从 100.66.1.3 访问时 `Origin: http://100.66.1.3:8080` 不在白名单 → POST 登录触发 CORS 预检 → 后端 403 `Invalid CORS request` → 前端"访问被拒绝(403)"。GET 科室列表是简单请求不预检 → 能显示科室列表。localhost 在白名单 → 正常。**同类问题 2026-05-01（100.66.1.4:9080）复发**。
- **定位手段**：前端 `Login.vue` + `request.js` 加 `[DEBUG-login]/[DEBUG-request]/[DEBUG-response]` console 日志 → 浏览器控制台直接显示 `403 Invalid CORS request` → 一锤定音。后端 `UserController.login` 加 `[/api/users/login]` 日志（密码只记长度）。
- **修复**：`app.cors.allowed-origins` 加 `http://100.66.1.3:8080` → 重启后端（PowerShell 中 `-D` 参数必须加引号：`& .\mvnw.cmd spring-boot:run "-Dspring-boot.run.profiles=dev" "-Dspring-boot.run.arguments=--server.port=8081"`）→ 预检 200 + 登录 200 → **用户实测 100.66.1.3:8080 登录成功**。
- **顺手改进**：vue.config.js devServer 加 `headers: { 'Cache-Control': 'no-store' }`（开发期防旧 JS 缓存）；UserController 增加登录调试日志（保留）；Login.vue/request.js 的 DEBUG 日志（保留，生产构建建议移除或降级）。
- **完整记录**：`med_ai_assistant_1.0_bs_backend/doc/问题修复/2026-08-16-本机100.66.1.3前端CORS白名单缺失导致登录失败.md`；踩坑与教训.md 2026-08-16 条目。
- **当前服务状态**：8080=前端 dev server（后台 job，`Cache-Control: no-store`）；8081=后端（22:04 新进程，CORS 已含 100.66.1.3）。登录测试账号 1657 密码 **123**。

## 2026-08-16（续：远程访问 AI 助手"127.0.0.1 拒绝连接"= iframe 硬编码回环地址，已修复）

- **现象**：从其他电脑访问 `http://100.66.1.3:8080`，登录后"助手"面板报 `127.0.0.1 拒绝连接`；本机 DSH 3080 两个地址均 200、监听 0.0.0.0，服务端无恙。
- **根因**：前端把 DSH 地址硬编码为 `http://127.0.0.1:3080`（`DshAssistantPanel.vue` iframe src + `PatientContextBridge.js` `DSH_ORIGIN`）。浏览器里 `127.0.0.1` 永远解析为访问者本机 → 远程访问时 iframe 指向访问者电脑（无 DSH）→ 拒绝连接；本机访问才恰好能用。
- **修复**（前端 3 处 + 测试）：新增 `resolveDshOrigin()` 跟随 `window.location.hostname` 动态拼 `:3080`（node 单测回退 127.0.0.1）；`DshAssistantPanel.vue` iframe 改 `:src="dshOrigin"`；`dshBridgeService.js` `allowedOrigins` 默认值改 `[resolveDshOrigin()]`；Cypress 断言按 baseUrl 动态推导。单测 9/9 通过，dev server 热重载生效。
- **教训**：页面内嵌服务 iframe/postMessage 桥的目标地址一律动态解析 hostname，禁止硬编码 127.0.0.1/localhost（前端 iframe 无 CORS 式 IP 白名单机制）。
- **完整记录**：`med_ai_assistant_1.0_bs_backend/doc/问题修复/2026-08-16-工作站AI助手iframe硬编码127.0.0.1导致远程访问拒绝连接修复.md`；踩坑与教训.md 2026-08-16 条目。

## 2026-08-16（续：登录 401/500 排查过程记录 —— 被 CORS 掩盖的假象排除）

- **登录问题闭环**：用户实测 **http://localhost:8080 登录正常**（工号 1657 密码 123 → 200 + token，错误密码 401）。所谓"登录出错"的完整根因链：① 8080 被 portproxy 占用 → 用户访问 8080 超时、dev server 换到 9081；② 9081/旧缓存前端发的是**无 /api 前缀**的 `/auth/login`、`/users/login`（当前代码 auth.js 根本没有 `/auth/login` 接口，判定为旧前端/旧构建产物），被 JwtAuthenticationFilter 拦 401（PUBLIC_PATHS 只有 `/api/users/login`）；③ 排查中实测 500 "The given id must not be null" 是**测试 body 字段名写错**（username 而非 id）造成的假象——LoginRequest 字段是 `id`+`password`，前端 Login.vue 传 `{id: loginForm.userid, password}` 正确。
- **验证事实**：后端 `POST /api/users/login` 直连和经 8080 代理均 200+token；`/api/dicinput`、`/api/users/1657/departments` 等公开接口正常（Redis 不可用仅降级警告，登录不依赖 Redis）。
- **当前服务状态（2026-08-16 晚）**：8080=前端 dev server（PID 24240，DSH 后台 job pwsh-1 托管，DSH 重启会丢）；8081=后端（PID 10292，20:57 启动，profile=dev）；9081 已无监听。登录测试账号 1657 密码 **123**（记忆库此前未记录，补充）。

## 2026-08-16（P6-C5 执行会话：DshAssistantPanel 面板壳 + 心跳 + 患者/身份联动 + 登出接线）

- 产出（med_ai_assistant_1.0_bs_vue，**36/36 测试全绿 + ESLint 零错 + 构建验证**）：`src/components/dsh/DshAssistantPanel.vue`（面板壳：iframe 嵌 3080、5s 心跳 ping/3 连失联降级占位/10s 自动重试、window message 监听→dshBridgeService、watch `patient/currentPatient`→patient-select、watch `user.token`→identity、小屏模式折叠为悬浮按钮、挂载 App.vue 全局唯一）+ `src/utils/dsh/heartbeatLoop.js`（心跳调度器，定时器注入可测，4/4）+ dshBridgeService 增 `setTargetWindow`（支持 getter，5/5）+ TopMenu.vue 登出接线（confirm 后 `runLogoutFlow` → `$router.push('/login')`，bridge=dshBridgeService 单例）。
- **接线事实**：user store 有 token/userInfo（localStorage）；ElementPlus 全局注册（main.js use(ElementPlus)）；App.vue 根容器 `#app` 挂面板。
- **踩坑**：组件生命周期 bug——pong 处理器匿名注册后无法解绑（beforeUnmount off 无效）→ 存 `this._pongHandler` 引用；setTargetWindow 需支持 getter 函数（iframe ref 渲染时机晚于 mounted）。
- 遗留：dev 联调（vue-cli-service serve + DSH 3080 实测面板 iframe 心跳与消息流）→ N2 插件（P6-C6 起）；TDD 指南周期表 P6-C1~C5 全 ✅、测试清单与接线说明已更新；构建后台任务 pwsh-9 验证编译。

## 2026-08-16（P6-C3/C4 执行会话：患者联动 payload + 登出两阶段 logoutFlow + 桥单例）

- 产出（med_ai_assistant_1.0_bs_vue，全部 node:test 红-绿-重构，**31/31 全绿** + ESLint 零错）：`src/utils/dsh/buildPatientSelectPayload.js`（contextKey=patientId|admissionTime、patientLabel=科室 床号 姓*（maskName 复姓只留首字，与 M2 同规则）、`isPatientInHospital(status)` 含"出院"判 false）+ `src/utils/dsh/logoutFlow.js`（runLogoutFlow：发 pre-logout → 等 logout-ready 或 30s 超时；ok:false/超时 onNotice"最后一段对话未能保存"；settled 防重复结算 + 清理监听器）+ `src/services/dshBridgeService.js`（桥应用级单例，targetWindow getter 延迟求值）+ PatientContextBridge 扩展 targetWindow 支持函数（getter 模式测试补上）。
- **接线点已定位（待面板壳落位）**：患者联动=watch store `patient/currentPatient`（PatientList.vue selectPatient 是统一出口，还有 desktopPanelService/TodoView 路径）→ buildPatientSelectPayload → send('patient-select')；登出=TopMenu.vue emitSystemCommand('logout') confirm 后插 runLogoutFlow → $router.push('/login')（App.vue 路由兜底不动）。二者依赖 DshAssistantPanel.vue 挂载时设置 targetWindow getter + 注册 window message 监听。
- **踩坑**：logoutFlow 测试时序——必须"先起 promise 再 deliver 回执再 await"（先 await 会走超时路径）；测试 mock 未暴露内部 Map 时断言监听器清理会 TypeError（mock 暴露 handlers 即可）。
- 遗留：P6-C1 剩余=DshAssistantPanel.vue 面板壳组件（挂状态机+心跳定时器+iframe+window 监听）→ 然后接患者联动与登出接线；TDD 指南周期表 P6-C1~C4 已标 ✅，测试目录与接线点说明已更新。

## 2026-08-16（P6-C1/C2 执行会话：N1 面板状态机 + PatientContextBridge 红-绿-重构）

- 产出（med_ai_assistant_1.0_bs_vue）：`src/utils/dsh/panelStateMachine.js`（纯函数状态机：idle→loading→ready⇄unavailable，EVENTS 常量，MAX_MISSED_PINGS=3）+ `src/components/dsh/PatientContextBridge.js`（协议桥：origin 白名单 + MESSAGE_TYPES 类型白名单 + send/on/off/handleMessage，浏览器 API 依赖注入可 node 单测）+ 测试 `test/dsh/panelStateMachine.test.mjs`（6/6）+ `test/dsh/patientContextBridge.test.mjs`（7/7）；`package.json` 加 `test:dsh` script。
- **测试框架决策**：N1 单元测试按项目惯例用 **node:test**（`node --test test/dsh/*.test.mjs`），不用 Vitest——项目零 Vitest 依赖，`test/diagnosis` 等均为 node:test 惯例；TDD 指南 §4.2 已同步修正（Vitest+jsdom 描述→node:test + Cypress 分工）。
- **踩坑（测试自身 bug）**：白名单完整性测试误用 `MESSAGE_TYPES['identity']`（对象键是 IDENTITY）→ 应查 `Object.values(MESSAGE_TYPES)` 集合；测试先红后绿时区分"实现缺失红灯"与"测试断言写错红灯"。
- **执行注意**：`node --test test/dsh/`（目录参数）在 node 24 报 MODULE_NOT_FOUND → 必须用 glob `test/dsh/*.test.mjs`（与既有 test:* script 一致）。
- 遗留：P6-C3（患者联动 payload/contextKey）、P6-C4（登出两阶段 logoutFlow）、面板壳组件 DshAssistantPanel.vue（挂状态机 + 心跳定时器）；TDD 指南周期表 P6-C1/C2 已标 ✅。

## 2026-08-16（DSH 助手界面与会话架构方案定稿会话：纯讨论，无代码）

- 任务：阅读 SoC 方案后讨论"前端展示"方案（用户要求只讨论不写代码），经多轮演化为界面/身份/存储/清理的完整架构定稿。
- 产出文档：`med_ai_assistant_1.0_bs_backend/doc/迭代/DeepSeek Harness插件化/DSH助手界面与会话架构方案.md`（方案定稿）；回写 SoC 方案 v0.5 + 《插件化开发规范》（§2.2/§5/§6/§8/§9.4）；ADR 已记《决策与设计.md》2026-08-16 头条。
- 讨论脉络（用户逐轮拍板）：①界面只说医生的话（信息分层）→ ②DSH 做成工作站侧边栏 + 点击患者列表切换 → ③每患者一会话（历史对话：保留、患者列表即会话列表、重入院/转科开新会话）→ ④存储选址与加密（不做 DSH 应用层加密；BitLocker+EFS）→ ⑤**医院医生电脑不固定**（推翻一人一机）→ 身份改登录人 JWT + token 设备准入、存储改服务器主副本 → ⑥调研 DSH 数据库持久化（结论：仅本地 SQLite，无远端后端）→ ⑦本地保持 JSONL、数据入库、定时+出院清理 → ⑧登出清理先取消后恢复 → ⑨登出 flush 需尽量确保入库（两阶段登出 + 30s 超时 + 丢弃兜底 A）→ ⑩全部推荐项通过。
- **DSH 源码调研结论**（可复用）：会话持久化 seam=`ctx.sessionPersistence`（抽象类 SessionPersistence），官方两后端 `session-persistence-jsonl`（默认，bundle/base/cordis.patch.yml 加载，root=dshHomePath('sessions')）与 `session-persistence-sqlite`（node:sqlite DatabaseSync 本地文件库，schema v15，不支持网络挂载）；无远端数据库后端；无应用层加密；工具结果卡片官方扩展面=`tool.call.toolview` keyed slot（ui-tool，按 wire tool name 注册 keyed renderer，未注册落 GenericToolCard，可组合 ToolRow；`packages/client/ui-tool/src/client/contract/slots.ts`）；ConversationNodeDefinition（cookbook adding-a-conversation-node）适合业务增量事件流；apps/web 测试 scaffold 用 `{ id: 'session-persistence-jsonl', config: { root } }` 加载后端。
- 未落盘待验证项：用户输入姓名落会话日志、JWT 落日志、toolview slot 基线可用性、frame-ancestors、看门狗×同步插件、登出两阶段改造侵入。

## 2026-08-16（DSH MCP 会话失效修复会话：后端重启 → Session not found）

- 任务：回答"心血管一病区有哪些患者"，通过 DSH 侧 `medai_patient_list_by_department` 工具查询。
- **问题**：工具调用报 `Session not found: dbc29828-...`，连续重试失败；后端 8081 在线（/mcp 返回 401 而非拒绝），java 进程 15:33 启动、日志 15:34:40 `Initializing Servlet 'dispatcherServlet'` → 后端重启过，服务端 MCP 会话表清空。
- **根因**：`dsh-mcp-client` 插件只在**传输层断开**（onclose）时触发重连；"Session not found" 是请求级错误不触发 → 会话僵死。
- **修复**：`$DSH_HOME\profiles\web\cordis.patch.yml`（用户层热重载）给 `mcp-medai` 加 `reconnect: {enabled:true, initialDelayMs:1000, maxDelayMs:30000, maxAttempts:10}` → 配置变更触发插件重建连接（新会话）。修改该文件需 danger-full-access（在会话工作区之外）。
- **验证**：修复后查询成功返回心血管一病区 63 名在院患者（返回 items 50 条 + truncated:true，病危 1/病重 26/普通 23，男 22/女 28）。
- 文档：问题修复报告已存 `med_ai_assistant_1.0_bs_backend/doc/问题修复/2026-08-16-后端重启后DSH-MCP会话失效Session-not-found修复.md`；踩坑条目已记 `记忆库/踩坑与教训.md`；环境配置已更新 `记忆库/环境与配置.md`。

## 2026-08-15（DSH 插件化 P1 实施会话：MCP 网关骨架 + 7 只读工具）

- 任务：阅读 `doc/迭代/DeepSeek Harness插件化/`（SoC 方案 + TDD 指南），完成 TDD 指南 P1（MCP 网关骨架 + 7 个只读工具）。
- 产出：`mcp/` 六关注点包 21 个主类 + 11 个测试类 43 个测试全绿（BUILD SUCCESS）；TDD 指南 M2-C1~C7 周期表标 ✅。详细 ADR 见《决策与设计.md》2026-08-15 条目。
- 关键落定：审计走 `ReadOnlyToolSupport` 显式调用（AOP 织入 @Tool 不可靠，R2 回退路径）；`McpAuditedAspect` 默认关闭；spike 工具 `PingTool.java` 已删除；执行服务器 `medai.mcp.enabled=false`。
- 测试运行注意：新测试用 `-Djunit.jupiter.execution.parallel.enabled=false` 串行跑（并行报告交错不可信）；McpRequestFilter 测试需 mock `response.getWriter()`（否则 NPE）；`Matcher.replaceAll` 方法引用参数类型是 `MatchResult` 不是 `Matcher`。
- **既有失败测试基线（非本次回归，已用 git stash 基线对比证实 16F/9E 完全一致）**：8 个类在无 GitHub token/H2 环境下必然失败——`GitHubContentClientTest`、`GitHubQcStandardClientTest`、`PromptTemplateSyncSchedulerTest`、`PromptTemplateUpdateSchedulerTest`、`QcStandardUpdateSchedulerTest`、`QcStandardSyncConfigTest`（GitHub API 401/遗留 Bean 断言）、`MySQLCleanupConfigTest`（配置键含 `secret` 命中既有 `jwt.secret`）、`FlywayJpaInitializationOrderTest`（V0_9_188 迁移脚本 Oracle 专属 `NVL()` 在 H2 测试库不兼容）。后续全量 `mvn test` 见到这些失败可跳过排查。
- **【2026-08-16 用户拍板删除】**：上表 8 个既有失败测试类已按用户要求删除（git 跟踪文件，删除可经 git 历史恢复）。删除后 `test-compile` 零错误、43 个 MCP 测试仍全绿。

## 2026-08-16（DSH 插件化 M2-T8 会话：工具在位告警端点）

- 任务：完成 TDD 指南 Module 1 最后一个周期 M2-T8（US-M2-07 工具在位告警端点）。
- 产出：`mcp/health/` 三件套（ToolUnregisteredReport record + McpHealthAlertService + McpHealthAlertController，端点 `POST /api/mcp/health/tool-unregistered`）；`AlertingService` 新增公开事件入口 `triggerEventAlert(ruleId, name, description, level)`（规则驱动无事件入口，复用 activeAlerts/历史/统计/通知/抑制机制，不改既有逻辑）；`WebSecurityConfig.PUBLIC_PATHS` 加 `/api/mcp/**`。
- 测试：新增 2 类 8 测试（service 4 + controller 4），Module 1 累计 13 类 51 测试全绿；既有 DrgAlertServiceTest 11 个零回归。TDD 指南 M2-C1~C8 全部 ✅。
- 踩坑：`ResponseEntity<?>` 泛型导致 AssertJ `containsEntry` 编译失败（String 无法转 capture）→ 用 `ResponseEntity<Map<String,Object>>` 具型；controller 单测 mock service 默认不抛异常，400 场景需 `doThrow(...).when(...)` stub。
- 遗留：@SpringBootTest 集成测试 + E2E verify-mcp-gateway.sh（含 DSH 侧工具在位监控上报联调）、P3 脱敏专项/安全基线、M3 代理、M1 插件。

## 2026-08-16（DSH 插件化 M2 集成测试会话）

- 任务：完成 TDD 指南 4.2 的 @SpringBootTest 集成测试（McpServerRegistrationIntegrationTest + McpGatewayEndpointIntegrationTest）。
- 产出：`mcp/integration/` 3 文件（McpIntegrationTestConfig + 2 集成测试类，7 测试）；M2 网关累计 15 测试类 58 测试全绿；TDD 指南 4.1 加落地注记。
- 关键坑（全部记入《决策与设计.md》ADR）：①全上下文 @SpringBootTest 在本环境既有失败（DiagnosisClinicalRuleRepository bean）→ 限定上下文 + @MockitoBean；②排除 Security 自动配置会缺 HttpSecurity（actuator managementSecurityFilterChain）→ 保留 Security + permitAll 链；③streamable-http 需 Accept: application/json, text/event-stream + 回传 Mcp-Session-Id 头 + notifications/initialized 返回 202 + 重复 initialize 400（端到端合并单方法）；④**pom 依赖锁定**：jsonschema-generator 4.31.1→4.38.0（spring-ai-model 1.1.6 要求，AnnotationHelper NoClassDefFoundError）；⑤McpTokenAuthenticator 双构造需 @Autowired 标注单参构造（否则 Spring 找无参构造失败）；⑥ToolCallback 无 getName()，用 getToolDefinition().name()。
- 遗留：E2E verify-mcp-gateway.sh、P3 脱敏专项/安全基线、M3 代理、M1 插件。

## 2026-08-16（DSH 插件化 E2E 会话：verify-mcp-gateway.sh 四场景实测通过）

- 产出：`test-scripts/verify-mcp-gateway.sh`（PASS=6 FAIL=0）；dev 后端 8081 已启动（mvn spring-boot:run dev profile，含 dev-only token 绑定 medai-dev-token）；application.properties 加 `medai.mcp.tokens[0]`（dev 占位，生产必须替换）。
- **关键踩坑（properties 中文编码）**：.properties 值里的中文必须 \uXXXX 转义（Spring 默认 ISO-8859-1 读 .properties；dev 模式下 departments[0]=心血管一病区 被读成乱码 → 同科室授权也拒绝，E2E 才暴露；集成/绑定测试传内存属性不受影响）。
- **关键踩坑（WSL 脚本环境）**：本机 bash 是 WSL；WSL/MSYS curl 连 Windows localhost 被防火墙拦（000）→ 用 /mnt/c/windows/system32/curl.exe（探测式）；临时文件路径两边不通 → -i 提取会话头、-o NUL、变量捕获；cmd→bash 不透传 set 环境变量 → 脚本用 -t。
- **关键坑（断言）**：MCP 信封把工具输出转义为字符串（\"patientLabel\"），grep 带引号匹配不到 → 匹配键名子串。
- 验证数据：患者 026794477_2（心血管一病区 10床 孙*）；场景 C 用 list_by_department(神经内科) 跨科室拒绝（本机库无神经内科患者）。
- 遗留：场景 D 需 DSH 环境手动验证；P3、M3、M1。

## 2026-08-16（DSH 插件化 P3 会话：脱敏专项 + 安全基线）

- 产出：`McpDesensitizationBaselineTest`（9 测试：零泄漏断言/注入样本集/多患者分治）+ PatientQueryToolTest 越权补测（跨科室科室名拒绝）；验证记录文档《DeepSeek Harness插件化P3脱敏专项与安全基线验证记录.md》；TDD 指南加 P3 注记。
- **检查结论**：系统已有 4 个非 MCP 脱敏模块（PatientDataDesensitizationService 精确替换/EMR 标签正则/SensitiveConfigProtection/DeployConfigRedactor）+ M2 的 McpOutputDesensitizer（裸 PII 正则，口径互补）。
- **发现并修复漏洞**：住院号 `AX0605264`（2 字母+7 位数字）漏网（INPATIENT 需 8+ 位、INSURANCE lookbehind 挡字母前缀）→ INPATIENT_NO `\d{8,}`→`\d{7,}`。
- 安全基线事实：本地测试库（63 患者预脱敏）/医院 ID 配置读/token 哈希/M2 审计告警先脱敏。
- 遗留：DSH 侧长会话分治验证、M3、M1。

## 2026-08-16（DSH 插件化 M3 会话：LLM 代理权威清洗主防线）

- 产出：`llmproxy/` 六包全落地 + 6 测试类 40 测试全绿（cleansing 20/auth 7/audit 3/orchestrator 5/集成 5）；TDD 指南 M3-C1~C6 全部 ✅。
- 复用：ReactiveLLMHttpClient（流式上游）+ ApiKeyAuthProvider + AIModelConfig + AlertingService.triggerEventAlert；掩码与 M2 McpOutputDesensitizer 同源（新增 maskInpatientNo）。
- 关键决策：残留阻断=清洗漏网兜底（PII 正则 + 会话已登记姓名），不做任意姓名猜测（误报不可控）；响应流先清洗再阻断。
- 踩坑：Reactor handle 泛型推断（`.<String>handle`）、401/429 Map 响应需显式 Content-Type、Flux.error 无 cause 包装、execution 集成测试限定上下文 + @ActiveProfiles。
- 遗留：流式姓名映射跨块缓冲（R5）待 DSH 联调；M1 插件（掩码一致性 Vitest）。

## 2026-08-16（DSH 插件化 M1 会话：@medai/dsh-pii-guard + MCP 接线）

- 产出：`medai-plugins/`（根 workspace）——mcp-medai.patch.yml（failOnStartupError:true 等）+ dsh-pii-guard 包（patterns/mapper/index + 测试）+ manifest.json；Vitest 20/20 全绿；TDD 指南 M1-C1~C3 全部 ✅。
- 钩子：`agent/pre-step` waterfall（DSH 源码 runtime-types.ts 确认签名），委托 next() 后改写 enter 决策 messages。
- 掩码口径与 M3/M2 同源（JS lookbehind）；TS/Java 手工同源，生成脚本未做（重构目标）。
- 环境：pnpm 11 的 allowBuilds 配置在 pnpm-workspace.yaml（package.json 的 pnpm 字段已废弃）；esbuild 需 allowBuilds。
- 遗留：DSH 实际加载 + E2E 场景 D；姓名映射 M2 下发端点（二期）；一期验收六项。

## 2026-08-16（DSH 联调 + 解绑会话）

- 联调：web profile `dsh plugin add`（pnpm add link）dsh-pii-guard + mcp-client 0.0.1-rc.1；cordis.patch.yml 用 `- insert:` 新增实例（targeted 覆盖对不存在行报 "entry not found"）；`--dump-config` 验证组合；DSH 与后端 8081 ESTABLISHED（mcp 激活成功）；用户手动重启 DSH（两次，PID 变化）。
- **解绑决策（用户拍板）**：failOnStartupError true→false——网关宕机 DSH 照常启动；`@medai/dsh-pii-guard` 在 pre-step 检测 `ctx.tools.schemas()` 无 mcp__medai__* → 注入 system 提示（"数据服务暂不可用，严禁编造"），去重；运行中宕机走工具调用 isError + M2-T8 告警。
- **关键认知**：mcp-client failOnStartupError:false = "activates with no tools"（零工具）——纯解绑=静默零工具；必须叠加插件降级提示。
- 测试：Vitest 25/25（降级注入 5 用例 + patch 断言改 false）。
- 遗留：解绑后场景 D 人工验证（停后端→DSH 正常启动→GUI 对话 agent 明示降级）；GUI 端到端工具调用验证。

## 2026-08-16（DSH 解绑验证会话：运行中宕机明示降级通过）

- **运行中网关宕机验证（用户 GUI 实操，通过）**：停后端 8081（不动 DSH，PID 16984）→ GUI 查询"心血管一病区"患者列表 → agent 调 mcp__medai__* 工具失败（连续两次 Error: fetch failed）→ **agent 明示**"医疗数据 MCP 服务当前不可达"+ 原因分析 + 恢复建议，**未编造数据**。验证了"解绑后运行中宕机 → 错误显示给用户"链路完整（mcp-client 工具已注册 → 调用失败 isError → agent 如实转述）。
- 待验证：**启动时宕机路径**（failOnStartupError:false + 插件降级提示）需重启 DSH 加载新配置后，在"后端已停 → DSH 冷启动"场景下验证（预期 DSH 正常启动 + 对话注入降级提示）。

## 2026-08-16（DSH 会话失效根治会话：MCP 切 STATELESS）

- 问题：后端重启 → DSH Session not found 僵死（数百台生产风险）。reconnect 补丁实测无效（onclose 驱动，空闲短连接不触发）。
- 根治：`spring.ai.mcp.server.protocol=STATELESS`（Spring AI 1.1.6/SDK 0.18.3 原生支持 WebMvcStatelessServerTransport + McpServerStatelessWebMvcAutoConfiguration）——无会话表、后端重启客户端天然不失效，数百台零改动。
- 试错记录：先试 DSH reconnect 配置（无效）→ 评估 fork SDK（发现自带 stateless，免 fork）。
- 验证：tools/call 无会话可用、MCP 集成 69/69、verify-mcp-gateway.sh 6/0（握手断言适配）、**GUI 实测持旧会话直接恢复**。
- 改动：application.properties protocol、McpServerRegistrationIntegrationTest 断言、verify-mcp-gateway.sh 握手断言、问题修复文档追加"方案升级 STATELESS"节。
- 分层防御：STATELESS + reconnect（连接断开）+ 降级提示（完全不可达）+ M2-T8 告警。
- 遗留：M2-T8 工具在位告警端点、@SpringBootTest 集成、E2E verify-mcp-gateway.sh、P3 脱敏专项/安全基线。

### 2026-08-16（MCP 只读工具实际使用：在院患者列表 + 患者诊断查询）

- 任务：通过 DSH 侧 mcp__medai__* 只读工具查询心血管一病区在院患者列表，并查询 18 床患者诊断。
- 产出：`medai_patient_list_by_department(心血管一病区)` 返回 51 人（床号 1-65，含病危 1/病重 26/普通 24）；`medai_patient_diagnoses(ZY603635_2)` 返回 18床 余* 共 14 条诊断（主诊断=主动脉瓣狭窄（重度），合并急性失代偿性心衰 IV 级、重症社区获得性肺炎、冠心病、肥厚型梗阻性心肌病、高血压 3 级很高危、2 型糖尿病等，与"病重"状态一致）。
- 观察：科室列表接口返回 `truncated:true`（items 51 条但 sourceRefs 64 个）——MCP 网关对科室患者列表有截断/分页行为，后续做全科室统计或大列表工具时需注意取数完整性。
- 关联：mcp/ 网关 M2 工具（medai 只读工具集）。

### 2026-08-16（P4 一期验收六项完成）

- 任务：按 TDD 指南 §7.2「一期验收六项」（故障注入/幻觉回归集/长会话压缩/并发压力/崩溃恢复/升级演练）+ SoC §9.3 三判据执行验收。
- **headless 同源验收 profile（关键基建）**：`~/.dsh/profiles/headless/`（bundles = dsh-base+dsh-headless，cordis.patch.yml 与 web 同源：mcp-medai + dsh-pii-guard），`dsh --profile headless "任务"` 用真实 LLM（deepseek-v4-pro，凭据 .credentials.yaml）跑完整 agent 会话——GUI 不可用时的等价运行时路径。坑：pnpm 11 默认跳过 `link:` 依赖（`exclude-links-from-lockfile=false` 到 .npmrc 无效）→ package.json 不列 pii-guard，node_modules 手动 Junction 到 medai-plugins 源；`dsh-llm-deepseek`/`dsh-mcp-client` 是命名导出+模块级 `inject`，必须 namespace import 否则 ctx.plugin 报 "cannot get property llm without inject"。
- 验收结果：①故障注入——宕机（停后端）agent 明示「医疗数据服务不可用」零编造、超时（8089 延迟代理+toolCallTimeoutMs=3000）报 -32001 不编造、恢复后立即可用；pii-guard 注入用 acceptance-driver observe 钩子级证据（hasDegradedPrompt:true）。②幻觉回归集 R1-R6 全过（正常/空结果/跨科室拒绝/不存在患者，均如实零编造）。③长会话压缩——小窗口 2000+BasicCompactionEngine 压力测试，compaction/start+end+summary 事件落地（轮4 shadow 7 节点 2743 tokens），**摘要保留「孙*/10床/B型/Rh阳性/血糖8.52」关键临床信息，压缩后第5轮回问答对**。④并发压力——新增 `test-scripts/verify-concurrency.sh` 11/11（3 模拟机 token 多人同患者授权隔离+一人多患者零串数据+PII 零泄漏；dev 配置新增 medai-dev-token-neurology/resp 两 token，SHA-256 哈希注册）。⑤崩溃恢复——后端宕机→降级→重启→恢复全链路验证 + web DSH 宕机期间存活；**DSH 强杀重启按用户指示停止**（restart-dsh.ps1 强杀进程树时误杀后端 java——该脚本进程树边界问题，运维不可接受）。⑥升级演练——0.9.251→0.9.252（bump-version.ps1 + 前端 package-lock 经 json_editor 2 处），重启后 verify 8/8 + 并发 11/11 + M2/M3 单测 109/109 + agent 冒烟全过。
- 产出：《DeepSeek Harness插件化P4一期验收报告.md》（doc/迭代/DeepSeek Harness插件化/）；verify-mcp-gateway.sh 场景 D 与解绑对齐（failOnStartupError:false + pii-guard）；TDD 指南 §7.2 勾选。验收驱动/证据在本机：`acceptance-driver.ts`（observe/compact 双模式，放 DSH checkout 根）、`acceptance-evidence/*-report.json`、`blackhole-proxy.mjs`。
- 遗留/人工复核：DSH GUI 会话续聊、生产窗口长会话压缩观察、前端 GUI 正常路径验收（工具卡片+会话日志无 PII）、秒级长任务体验（二期）。
- 关键教训：①pii-guard 的 pre-step listener 是「先 next 后改写」，观测器须注册在它**之前**才能看到注入结果；②`ctx.plugin(函数插件)` 对无 default export 的模块会丢 inject 声明，用 namespace import 传整个模块对象；③PowerShell 调 mvn 的 `-D` 参数必须加引号（否则被拆成 lifecycle phase）；④compact 驱动让 agent「复述长文本」会因输出爆炸挂起，填充内容放用户消息+agent 简短回复即可。

## 2026-08-15（DSH 插件化模块 SoC 设计与实现方案会话）

- 任务：阅读 `doc/系统结构/deepseek harness插件化/`（总体方案 + 插件化开发规范 + 后端整体架构 + 临床规则模块SoC设计体例），按关注点分离（SoC）原则生成实现方案，保存到 `doc/迭代/` 新目录。
- 产出：《DeepSeek Harness插件化模块SoC设计与实现方案.md》（v0.1）→ 新目录 `med_ai_assistant_1.0_bs_backend/doc/迭代/DeepSeek Harness插件化/`。
- 核心设计：顶层四模块 M1 DSH 编排壳/M2 MCP 网关/M3 LLM 代理/M4 工作站接入层（部署节点×单一职责，M2/M3 互不依赖）；M2 网关内部六关注点（传输/鉴权/授权/工具适配/脱敏/审计）；M3 代理五关注点（cleansing 纯算法主防线）；一期分 P0–P5。详细见《决策与设计.md》2026-08-15 条目。
- 复用确认（grep 核实现有类）：JwtTokenService/AuthService/AuditLogService(service/audit/)/PatientDataDesensitizationService；患者 PatientController+PatientService+PatientDataProvider；病历 MedicalRecordController+EmrRecordService；检验 LabResultController+LabResult；检查 ExaminationResultController+ExaminationResultService；限流 RateLimitService；告警 AlertRuleService/AlertingService。
- 记忆沉淀：决策与设计.md 已加 ADR（2026-08-15）；本条目落会话记忆。

## 2026-08-15（DSH 插件化 grill 审查 + TDD 实施指南会话）

- 任务：①用 grill-with-docs 方法论（技能当时不在会话目录，改手动执行等价审查）审查 SoC 方案；②按用户给定模板 + 项目 TDD 体例生成完整 TDD 实施指南。
- grill 审查 8 处发现已修订（方案升 v0.2）：S1 一期身份模型（每机 token→医生+科室，授权粒度=科室级，患者级留二期；MCP token ≠ 工作站 JWT）+ S2 M2/M3 互不依赖→无同步请求依赖（仅 M2→M3 脱敏映射冷数据下发）+ M1 流式转发 + M2 工具在位监控归属 + M3 AOP/ThreadLocal 待验证 + medai_ping 改名 + 并发多 token 模拟 + 禁生产/会话分治。方案 3.7 新增一期身份模型，附录 B 加 ADR D6/D7。
- 患者级授权方案 B（用户拍板）：`PatientAccessGuard` 一期即策略接口（check(doctor,patient)），一期 `DepartmentOnlyStrategy`，二期 `PatientAssignmentStrategy` 新策略接入、接口不变、一期不建空表。
- 产出：《DeepSeek Harness插件化TDD实施指南.md》（v1.0.0）→ `med_ai_assistant_1.0_bs_backend/doc/迭代/DeepSeek Harness插件化/`。3 模块（M2 网关/M3 代理/M1 插件）15 用户故事（US-M2-01~06、US-M3-01~06、US-M1-01~03）+ 16 任务 + 16 红绿重构周期 + 测试金字塔 70/20/10（JUnit5+Mockito/Vitest/curl+DSH GUI）+ 14 项质量门禁 + 4 组 ATDD Gherkin + 10 项风险矩阵（R1 Spring AI 双栈冲突/R2 AOP 织入/R5 流式缓冲为核心）+ 迭代交付 4 期 + 四类验收清单。体例对齐《诊断编码词典注入TDD实施指南》。
- 记忆沉淀：决策与设计.md ADR 已更新（含方案 B）；本条目落会话记忆。
- **后续（同会话）**：①对 TDD 指南本身再做 grill-with-docs 审查（技能已出现在会话目录），14 条主张核对出 3 实问题（医嘱范围矛盾/工具在位监控缺任务/禁生产缺位）+ 4 处漂移，全部修订（TDD 补 US-M2-07 + M2-T8 + M2-C8 + 禁生产 + 异常类登记 + P0 spike 说明 + 示例占位标注 + CI 分支修正）。②用户拍板"所有基础资料入一期"→ 一期 **7 个只读工具**（含医嘱 `medai_patient_orders`），已同步总体方案 §4、规范 §8（原本即含医嘱）、SoC 方案（升 v0.3）、TDD 指南（六→七、~30→~31 测试、M2-C6 7→8 测试）。

## 2026-08-15（本机 Prompts 大量未完成记录根因调查会话）

- 任务：解释本机（dev）Prompts 表为何有 16124 条记录、其中 7322 条"待处理"未完成（用户质疑 dev 不应自动生成）。
- **根因（铁证）**：本机 Prompts 表**不是 dev 生成的，而是今天凌晨 02:36-02:37 从测试服务器（100.66.1.4）Data Pump 全量导入**！证据：`D:\oracle_dump\medai_sync_01.dmp`（1.77GB，08-14 02:36:37）、`import.par`（`TABLE_EXISTS_ACTION=REPLACE`、CONTENT=ALL）、`medai_sync_import.log`（02:37:48 完成，记录 `导入了 "SYSTEM"."PROMPTS" 303.0 MB 16124 行`，另有 PROMPTRESULT 8595、ENCRYPTED_DATA_TEMP 8751、EMR_RECORD 28011、PATIENTS 63、USERS 35 等 70 表）。
- 数据特征：94%（15145 条）`GeneratedBy='EMR-QC'`，其中 12044 条在 19:00:00-02 整点批量生成、07-07→08-13 连续 38 天每天 ~317-390 条 → 测试服务器 EmrQcScheduler（`@Profile("prod")` + `cron 0 0 19 * * ?`）每日对 61 名在院患者逐份病历批量质控（EmrQcService 建 Prompt 时状态固定"待处理"、GeneratedBy='EMR-QC'、priority=2）；其余来自 TimerPromptGenerator（System/System-NoonWardRound，2-3 月 04:00 批量）、DRG 分析、质量门禁 QC-SYSTEM、手动触发 user。
- 未完成积压机制：PromptSubmissionService.submitPendingPrompts（`@Scheduled(fixedDelay=10s)`）内部 `submissionEnabled` **默认 false**，须 API `POST /api/prompt-services/submission/enable` 显式开启；未开启则"待处理"永不提交 → 积压。8-03 监控"已完成 767"→ 8-14 8779（=978 Feb-Jun + 7801 Jul 有 ResultId），8 月新生成 5070 条 0 完成全滞留。
- 监控口径坑：PromptMonitoringService 统计 `countByStatusName("待执行")`（0 条），积压的是"待处理"→ 监控一直显示"待处理 0"，掩盖积压。
- 教训：dev 库被 Data Pump 同步后，业务表是测试库数据快照，排查"本机数据异常"先查 `D:\oracle_dump\medai_sync_import.log` 是否刚导入；本机 8081 dev 后端 execution 数据源指向 100.66.1.4:1521（测试库），本地 ENCRYPTED_DATA_TEMP 数据不参与链路。

## 2026-08-15（Prompts 表状态批量置为已完成会话）

- 任务：检查本机 Oracle Prompts 表记录，将所有记录状态置为“已完成”。
- 环境：本机 Oracle 21c SE2（SID=FREE，服务 OracleServiceFREE 运行中），sqlplus=`d:\download\windows.x64_213000_db_home\bin\sqlplus.exe`，账号 system/Liuzh_123@127.0.0.1:1521/FREE；输出中文乱码时先 `$env:NLS_LANG='AMERICAN_AMERICA.AL32UTF8'` + `[Console]::OutputEncoding=UTF8`。
- 操作：`UPDATE system.prompts SET StatusName = '已完成'; COMMIT;` 共更新 **16124** 行。更新前状态分布：已完成 8779 / 待处理 7322 / 执行失败 18 / 已提交 5；更新后全部为“已完成”（验证查询确认 16124 行）。

## 2026-08-15（AI诊断分析全链路代码梳理会话）

- 任务：检查 AI 诊断分析现有代码，输出详细流程。已完整梳理前端（vue）+ 主服务器（8081）+ 执行服务器（8082）+ 质量门禁 + 反馈闭环。
- 全链路（8 阶段）：① Prompt 生成（TimerPromptGenerator 每日 07:00 定时 + 前端手动 triggerDiagnosisAnalysis）→ ② 前端 handlePromptExecution 并行取患者数据/模板 → POST /api/ai/savePrompt（AIResultOperationController，含脱敏、诊断前置门禁仅限诊疗计划/DRG/MCC）→ ③ 提交双管线：PromptSubmissionService.submitPendingPrompts（@Scheduled 10s，写 ENCRYPTED_DATA_TEMP）+ PromptExecutionController.execute-pending-prompts（手动多线程，POST 8082 /api/execute/encrypted-prompt 已 @Deprecated）→ ④ 执行服务器（execution profile）：pollUnprocessedRecords（30s）→ 解密 → callLLMService（DeepSeek，AIModelConfig + systemPrompt 文件 + 流式 NDJSON 解析 + ResponseCacheUtil 缓存）→ 加密结果回写状态 ENCRYPTED → ⑤ 主服 PromptPollingService.pollSubmittedPrompts（30s）解密结果 → 统一事务创建 PROMPT_RESULT + Prompt 置"已完成" + 标记 SENT → ⑥ 事件驱动 Gate 2：publishDiagnosisResultReadyEvent（事务提交后发布）→ DiagnosisReviewListener（@ConditionalOnProperty gate2.enabled=true）构建审查输入（ReviewInputBuilder：患者信息+原始病历+【AI诊断分析输出（待审查）】+临床经验 ClinicalExperienceProvider+ICD 对照 DiagnosisIcdProvider）→ 新建"诊断审查"Prompt（generatedBy=quality-gate，防循环）→ ⑦ 前端三层门禁展示：Gate 1 前端格式校验（checkGate1：空/过短/过长/解析失败→gate1Failed）→ Gate 2 fetchTimedReviewResult 时序关联（审查 executionTime > 诊断分析 executionTime）替换修订版 → Gate 3 parseRevisionTrace 解析【修订追溯】段；DiagnosisEditPanel 统一诊断表采纳/修改/删除/设主诊断（/api/diagnosis/* → DiagnosisChangeDebouncer 防抖 2 分钟触发 QC-第一阶段-AI诊断匹配）→ ⑧ 反馈闭环：collectDiagnosisFeedback（Levenshtein 相似度 =1 不采集 / 0.45~1 MODIFIED / <0.45 NOT_ADOPTED）→ POST /api/feedback/diagnosis → FEEDBACK_DIAGNOSIS_EVENT 表 → FeedbackCollectorScheduler 每周日 03:00 + 编辑率超标事件驱动 → DiagnosisFeedbackCollectorImpl 按诊断名聚合 → FeedbackPublisherImpl 推 GitHub（template-feedback/诊断分析/日期/hospital_*.json）→ markPushedToGithub。
- 关键代码索引：前端 `vue/src/components/patient/AIDiagnosisAnalysisTab.vue`（三层门禁编排）、`DiagnosisEditPanel.vue`、`vue/src/utils/{diagnosisAnalysis,promptUtils,qualityGate,diagnosisFeedback}.js`、`vue/src/api/qualityGate.js`；后端 `controller/{PromptExecutionController,AIResultOperationController,ExecutionServerController,DiagnosisController,FeedbackDiagnosisController,PromptResultController}.java`、`service/{PromptSubmissionService,PromptPollingService,TimerPromptGenerator}.java`、`service/qualitygate/event/{DiagnosisResultReadyEvent,DiagnosisReviewListener}.java`、`evolution/service/impl/{FeedbackCollectorScheduler,DiagnosisFeedbackCollectorImpl,FeedbackCollectorImpl}.java`、`config/ClinicalRuleInjectConfig.java`（白名单仅"诊断分析"）。
- 注意点：① 双提交管线并存（提交服务定时写临时表 + 手动 execute-pending-prompts 直推 8082，后者接口已废弃标记）；② ENCRYPTED_DATA_TEMP 通过 executionDataSource 独立数据源访问；③ 诊断分析结果会经 PatientDataCompositionService.appendDiagnosisAnalysisData 追加到后续分析上下文；④ DataCollectionAdviceService 每日 08:00 对当天有诊断分析结果的患者生成资料收集建议（basedOn.diagnosisAnalysis）。
- 产出：全链路流程已绘制为 Mermaid 图文档并落盘 `med_ai_assistant_1.0_bs_backend/doc/系统结构/诊断分析优化/AI诊断分析全链路流程.md`（含 5 张图：总体架构 flowchart、8 阶段端到端 flowchart、sequenceDiagram 时序、stateDiagram 状态机、erDiagram 表关系 + 关键代码索引表 + 配置项表）。

## 2026-08-15（DSH 第三方插件集合调研会话）

- 任务：调研当前 DeepSeek Harness（DSH）第三方插件集合。结合本地源码 checkout（`C:\Users\Administrator\Documents\Qoder\2026-08-13\chat-1\deepseek-harness`）+ 网络检索（pwsh 出网被沙箱封锁，全部靠 web_search）。
- 官方插件机制（本地代码确认）：插件=bundle（组合包，package.json 声明 `dsh.bundle` + cordis.patch.yml 配置层）；profile 机制（`dsh.profile.bundles` 有序层列表）；`dsh plugin --profile <name> add <pkg>` 转发 pnpm；分发三途径=npm 预构建 / git 托管（需 prepare 脚本 + pnpm-workspace.yaml allowBuilds 授权）/ tarball。内置 bundles：`@deepseek-ai/dsh-base`、`@deepseek-ai/dsh-web-app`、`@deepseek-ai/dsh-headless`；官方 extensions 包：`dsh-cordis-client-runner`/`dsh-cordis-host-runner`/`dsh-tool-cordis`/`dsh-client-ui-cordis`（模型写插件的 cordis 动态插件体系）。
- 第三方生态入口（GitHub）：精选列表 `awesome-dsh-plugin/awesome-dsh-plugin`（中文 README.zh.md，V2EX 帖称收录 270 个）、`beancookie/awesome-dsh-plugin`、`bruc3van/awesome-dsh-plugin`（按问题场景分类）、`0xsline/awesome-deepseek-harness`、`AdamPlatin123/awesome-dsh-plugins`（每日兼容性追踪）、`like-study1/Oh-My-DSH`（每 8 小时自动同步 dsh-plugin topic，PLUGINS.md）；插件市场：`vvlife/whalehub-dsh`、`dsh-market/dsh-market`、`cooljser/dsh-plugin-portal`（227+ 插件静态门户）、`hikariming/dshfind`、`Nagi-ovo/dsh-find-plugins`、`vlln/plugin-registry`（薄控制台 + make-dsh-plugin skill）。
- 已确认的具体第三方插件（按类别）：浏览器=dsh-browser-control/dsh-playwright-browser/xylt369 dsh-browser；MCP=dsh-mcp-adapter、Deepseek-omnimodal（多模态，Qwen/DashScope）、dsh-trio（LobeHub 全家桶）；多智能体=dsh-agent-teams（NanmiCoder，含开发文档）；深度研究=dsh-deep-research（omdsh-dev）；记忆=dsh-mnemon（omdsh-dev）；数据=dsh-data-agent（omdsh-dev）、dsh-excel-chat；消息=dsh-plugin-onebot/dsh-onebot/dsh-plugin-adapter-qq/dsh-qqbot；办公增强=dsh-at-file/dsh-notification/dsh-open-in-vscode（omdsh-dev）；语音=dsh-voice/@meomeo-dev/dsh-voice；终端 UI=dsh-tianshu-tui；Claude Code 桥=dsh-plugin-cc；图像路由=dsh-llm-image-routing；本地 RAG=mindspace-dsh-local-rag；技能包=superpowers-dsh（TDD/调试/规划，改编自 obra/superpowers）；开发工具=dsh-plugin-deepeye/dsh-plugin-hub/dsh-plugin-development（w2112515）；桌面端=deepseek-harness-desktop（omdsh-dev）。npm 上还有 dsh-plugins、oh-my-dsh 等聚合包。
- 官方渠道：RFC#1629（官方插件脚手架 template repo + `pnpm create dsh-plugin` 提案）；文档引用示例 [deepseek-harness/turtle-ui](https://github.com/deepseek-harness/turtle-ui)（prepare 自包含构建范例）；0xsline 列表称生态源含 dsh-external/hub 与公开 dsh-plugin topic。
- 结论：DSH 插件生态处于早期高速膨胀期（精选列表已 200-270 个量级），无官方统一市场，以 GitHub topic + npm 分散发布，社区精选列表/市场站点承担发现层；对本项目（医疗场景）可直接用 `dsh plugin add` 装第三方插件，或按 bundle 规范自研。
- 产出（同日续）：按要求深挖 [awesome-dsh-plugin/awesome-dsh-plugin](https://github.com/awesome-dsh-plugin/awesome-dsh-plugin)，整理成《DSH第三方插件简介整理.md》（13 大类 + 参考结论，落盘 `med_ai_assistant_1.0_bs_backend/doc/系统结构/deepseek harness插件化/`）。要点：①沙箱封锁全部出网通道（pwsh/curl/git/npmmirror 全失败，仅 web_search 可用且只回 URL 不带正文），README 原文无法直接抓取，简介基于 GitHub 卡片描述/npm 页/社区转载交叉整理并在文档头部如实标注；②新增确认插件：dsh-find-plugin（官方组织会话内搜插件）、@sanqi-normal/dsh-webui-market-plugin、dsh-toolkit（omdsh-dev/dsh-external 十个确定性工具）、dsh-genui（生成式 UI 图表/mermaid/3D）、dsh-quote-annotate（批注）、dsh-docling、dsh-claude-move/dsh-chat-import/dsh-session-import-codex（会话迁移）、dsh-agent-sdk、dsh-tray、dsh-usage-stats/dsh-balance/dsh-token-panel（token 余额监控）、dsh-tui 家族（openguardrails/@dsh-tui/@wxgmjfhy 等多变体）、多款桌面端（omdsh-dev/ningbainb/ChisaAlter/pig1et7/hust-open-atom-club）、superpowers-dsh、w2112515/dsh-plugin-development、create-dsh-plugin（⚠️页带警示，官方脚手架仍处 RFC#1629 讨论）、安全研究 dsh-security-pocs（RCE 链 PoC）+ 官方讨论 #454/#587（插件模型无签名校验）；③对本项目参考：dsh-toolkit/dsh-mnemon/dsh-notification/dsh-data-agent/dsh-genui 形态可借鉴，第三方插件必须审源码+锁 commit。
- 文件浏览器调研（同日）：**DSH 自带**侧边栏文件浏览器（默认启用，无需第三方插件）——host 端 `@deepseek-ai/dsh-host-file-browser`（fileBrowser/list+read 两个 Remote，仅限工作区根、bounded UTF-8 文本预览、只读）+ 客户端 `@deepseek-ai/dsh-client-ui-file-browser`（sidebar 按钮→目录对话框→预览）。**Markdown 预览原生支持 mermaid**：`MarkdownPreview.tsx` 用正则切分 ` ```mermaid ` fence，经 `mermaid@11.16.0` 渲染成 SVG（securityLevel loose、失败回退代码块），其余走 MarkdownText（GFM 表格/脚注/KaTeX）；均在 `packages/bundle/web-app/cordis.patch.yml`（行 99-100、289-291）默认挂载。限制：仅文本预览（不渲染 PDF/图片/二进制）、只读、仅工作区根。第三方候选：`joejojoking-cloud/dsh-file-explorer`（文件树+预览+markdown+语法高亮+面板内编辑+VS Code 集成，mermaid 支持未证实）；`taxueseek/dsh-files`（上传 + read_document 读 PDF/DOCX/XLSX，非浏览器）；`dsh-genui`（回复内 mermaid 渲染，非文件浏览器）。结论：满足"文件预览+mermaid"最成熟方案=内置浏览器，无需装插件。

## 2026-08-13

- 建立了项目长期记忆体系：`记忆库/`（5 个主题文件 + 索引）、`memory-manager` 技能、根目录 `AGENTS.md` 自动记忆协议。
- 确认 `.dsh/skills` 是 Junction，实际指向 `.qoder/skills`（同一目录，改哪边都一样）。
- 确认 `.dsh/` 被 .gitignore 忽略，但 `.qoder/` 已被 Git 跟踪（111 个文件），整套记忆随仓库版本化。
- 记忆体系已提交并推送：根仓库 commit `096cbd2`（"建立项目自动长期记忆体系"），9 个文件，已推送至 github.com:liuzh2008/med_ai_assistant_workspace.git（master）。
- 推送时根仓库尚有未暂存改动（`docs/help/ai-diagnosis.md`、backend/vue 子目录 gitlink），非本次记忆体系范围，未提交。

## 2026-08-14（DSH 集成方案讨论：可靠性 + 查房场景）
- **可靠性两层框架**：传统可靠性（可用性/一致性/故障恢复）靠「旁路降级原则」——DSH/MCP 网关是增强旁路，挂了退化为现状（工作站直连 REST 不受影响）；Agent 行为可靠性（幻觉/串数据/不可复现）DSH 不解决，需自建四件套：地面事实强制（查询必须走工具）、溯源校验（结论指向依据数据）、确定性标注（事实 vs 建议分区）、审计回放（append-only 日志+注入医生身份）。一期验收六项：故障注入/幻觉回归集/长会话压缩/并发压力/崩溃恢复/升级演练。
- **MCP 关键配置**：`failOnStartupError` 默认 false（网关宕机时 DSH 静默零工具）→ 医疗场景必须设 true；`toolCallTimeoutMs` 默认 60s；重连预算耗尽后工具被注销不自动恢复 → 需自建"工具在位"健康监控并入现有告警体系。
- **查房场景成熟度拆解**（用户场景：口述安排冠脉造影→系统追问术前讨论/术前小结→自动写查房记录→提醒→审核保存）：会话内追问/流式生成/审批保存链路 ✅ DSH 原生（审批保存为二期写工具）；语音输入 ❌ DSH 无 ASR（全源码无 speech 能力），浏览器 Web Speech API 走 Google 不合规，需院内/离线 ASR + "语音转文本先确认再发送"；离屏提醒 ❌ 无推送通道，`dsh-schedule` 仅会话内投递（agent 空闲时开新 turn），需复用 AlertRuleService 接设备推送；保存需结构化落库 MedicalRecord + 审核 diff 留痕。定位为二期旗舰场景（查房 MVP：文本指令→取数→生成草稿→审批保存，语音/推送/手术写链后置）。
- **状态**：DSH 集成方案仍处 Idea 验证阶段，未实施；一期试点（患者/病历/检验查询 MCP 只读工具）待用户确认启动。
- **市场调研（2026-08-14，六个场景国内外对照）**：结论=各环节几乎都有商用先例，但市场形态是"垂直产品拼盘"而非"统一插件平台"。国内：讯飞医疗（语音转写→病历提效60%但自述"只能是辅助"四大局限；星火医疗大模型V3.5；AI诊疗助理2.0进入多智能体协同诊疗）、云知声山海·知医5.0（近400家医院）、嘉和美康AI病历内涵质控、惠每超级智能体（10秒生成交接班记录）、东软HAIAs病历辅助生成智能体、清华Agent Hospital、医渡《医疗智能体应用发展白皮书》、常州AI智医助手、同济医院大模型四场景。国外：Microsoft Dragon Copilot/DAX（ambient scribe，扩展至护理与第三方agents，嵌入Athenahealth EHR）、Ambience Chart Chat（EHR内对话copilot）、Epic+GPT-4、KLAS报告证实ambient speech缓解burnout。空白点：**主动提醒/主动追问（"是否生成术前讨论"）+ 审批写回 + 统一插件编排平台**——所有人都在早期，本方案（DSH harness+插件）在这一点不落后于商用产品；国外产品英语/EHR生态不适用，国内垂直SaaS对接自研Oracle系统成本高——自建插件平台差异化定位成立。
- **产出（2026-08-14）**：六轮讨论汇总成方案文档 `med_ai_assistant_1.0_bs_backend/doc/系统结构/deepseek harness插件化/deepseek harness插件化改造.md`（12 章+附录：目标/DSH调研/总体架构/四期路线/多用户/安全/可靠性/UX/市场调研/插件规范要点/风险/下一步）。
- **产出（2026-08-14）**：《插件化开发规范》正式成文 `med_ai_assistant_1.0_bs_backend/doc/系统结构/deepseek harness插件化/插件化开发规范.md`（v0.1，10 章：四条铁律/形态选择表/命名/接口Schema/安全必检/测试验收基线/medai-plugins 交付发布/17 类模块→插件映射表/模板速查/维护责任）；方案文档第 10/12 章已同步更新引用。
- **产出（2026-08-14）**：《住院部医生日常工作流程》场景底稿 `med_ai_assistant_1.0_bs_backend/doc/系统结构/deepseek harness插件化/住院部医生日常工作流程.md`——入院→出院 5 阶段 35 个步骤（每步含现有系统支撑/产出/痛点/AI 介入方向）+ 时限约束表（8h/24h/48h/72h/3日等，作为定时提醒与质控规则的天然规则源）+ 痛点排行 + 现状映射 + 分期介入汇总。后续工作：将 35 步与规范第 8 章映射表逐项对应成"场景×工具×模板"实施清单。
- **产出（2026-08-14 文档审查+整合）**：方案文档升 v0.2，按本轮讨论整合——5.3 部署定调改为"本机部署优先（建议）+服务器集中兼容"，新增 5.4《本机部署形态：内嵌调用与硬件/终端要求》（iframe 两条路径+四前提、硬件表 Win10x64/4-8GB/3-5GB、终端四大现实约束）；二期 v1 改为本机嵌入优先；11 章部署拓扑/O2 行同步收敛（接入层只剩身份注入+审计）；附录补硬件速查+升级策略。DSH 升级策略 ADR 已建（基线锁定+事件驱动+季度/半年追平）。
- **产出（2026-08-14 部署形态定案）**：用户拍板"不再使用服务器部署，开发难度太大"→ 方案文档升 v0.3：仅本机部署（一人一机一账户），取消服务器集中/认证接入层路径；5.2 定位结论改写（单用户定位与部署形态从冲突变为一致）；5.3 三层表收敛（隔离=OS 用户）；二期 v1/架构图/11 章（部署拓扑、O2 只剩身份注入+审计、O4 改单机负载）/12 章同步；ADR《部署形态最终决策：仅本机部署》已建，界面主形态 ADR 旧表述已标"被取代"。终端四大约束升格为项目 go/no-go 条件。
- **产出（2026-08-14）**：《本机部署操作手册》`doc/系统结构/deepseek harness插件化/本机部署操作手册.md`（v0.1）——内网 Windows 单机部署全流程：go/no-go 六项检查（含纯隔离内网必须建院内模型网关）、联网构建机一次性产出自包含部署包（fork+补丁+构建+便携 Node+skills+配置模板+setup/update/selfcheck 脚本，约 1.5-2GB zip）、目标机 10 分钟无管理员安装（解压→setup.ps1 种子化 %USERPROFILE%\.dsh→每机独立 MEDAI_MCP_TOKEN→快捷方式+登录自启计划任务→自检→部署记录）、升级/回滚/卸载、故障排查、本机安全注意、铺开节奏（1-3 台试点→科室灰度→全量）。
- **产出（2026-08-14 可实现性分析）**：工作流文档新增第 8 章《35 步可实现性分析》——✅ 规划内完整实现 ≈20 步（生成/查询/提醒类）；🟡 有外部前置 ≈12 步，四大依赖：HIS 对接（最大头：#1 住院登记、#5/#11/#22/#27 医嘱回传、#29 费用）、语音 ASR、离屏推送、数据/内容工程（#15 会诊流程、#17 影像、#26 出院标准规则库、#30 随访）；⚪ 不介入 3 步（#8/#21 签字、#35 医保）。结论：无技术性死点；真正变量是本院 HIS 接口开放度，建议一期前先确认；AI 只做文书/提醒/建议的边界=合规边界。
- **产出（2026-08-14 方案文档二次审核 v0.4）**：全文档一致性修正 9 处 + 规范 2 处 + 手册 4 处，均为 6.4（LLM 代理经执行服务器+三层清洗）与仅本机部署决策的遗留表述——①MCP 网关 url 127.0.0.1 → 主服务器内网 IP（2.4/规范模板）②5.4 网络行：医生机无需模型 API 出网 ③终端约束①：Win7 无集中部署退路 ④6.1 回环围栏：本机形态无需放宽补丁 ⑤6.2 验证期基线防火墙 ⑥6.3 加升级说明指向 6.4 ⑦7.2 故障表补"执行服务器代理故障"行 ⑧附录硬件/端口行 ⑨规范出网/LLM 兜底两行改三层表述 ⑩手册 go/no-go#3/自检/故障排查/出网白名单。

## 2026-08-14

- 修复 100.66.1.3:8080 登录页"获取科室列表失败(500)"：根因是本机 Oracle system 密码过期(ORA-28001)导致后端启动失败；用户重置口令后，将测试服务器(100.66.1.4) Oracle 的 76 张应用表结构和数据 Data Pump 同步到本机 FREE 库（REPLACE 导入），补建 25 个测试库命名序列、重编译 24 个触发器、身份列 LIMIT VALUE 重置、序列 RESTART 到 MAX+1、回补本机独有列（TEACHING_PLANS PPT_*/OBJECTIVE_TEXT 等）、替换 V_INP_BILL_DETAIL_INFO 视图、DEFAULT profile 密码永不过期。
- 后端 dev 模式(8081)已启动（后台任务），健康检查 UP；`/api/users/1657/departments` 返回 200（CCU病区/心血管一病区/心血管二病区）。
- 产出脚本：`backend/test-scripts/{create-sequences,post-import-fix,resync-sequences-static,replace-view,inspect-local-db,list-local-tables,list-local-columns,check-local-rbac,check-local-flyway}.sql`；dump 与导入日志在 `D:\oracle_dump\`（1.77GB，确认稳定后可删）。
- 详细踩坑与流程已沉淀到《踩坑与教训.md》《命令与流程.md》《环境与配置.md》对应 2026-08-14 条目。
- 完成「生产库诊断分析数据提取与分析工具包」：用户采用方案一（SQL 提取脚本 + 人工摆渡 + 开发环境解析成文）。产出在 `temp/diagnosis-extract/`（不随版本发布）：`extract_01_count.sql`（摸底）、`extract_02_results.sql`（导出 PROMPTRESULT 诊断分析记录，PatientID 加盐哈希 SHA256 前16位脱敏）、`extract_03_diagnoses.sql`（导出目前诊断）、`run_extract.bat`（内置 NLS_LANG，改 CONN 即可）、`parse_and_generate.py`（三层兼容解析 + Levenshtein 配对判定，生成 Markdown 报告 + Excel 明细）、`使用说明.md`、`sample_local/`（示例产物）。
- 数据链路：PROMPTRESULT JOIN PROMPTS（PromptTemplateName IN '诊断分析','鉴别诊断分析'，近90天，deleted=0）→ PatientId → DIAGNOSIS（is_deleted=0）；Status 含 SUCCESS/SAVED 均导出（SAVED=医生保存后，最有价值）。
- 判定规则沿用《AI诊断数据采集方案》：Levenshtein 归一化相似度 =1.0 直接采纳 / 0.45~1.0 被修改 / <0.45 未采纳（AI侧）或医生新增（医生侧）。
- 已在本地开发库（Oracle 23ai Free，16条记录）与测试服务器（100.66.1.4 的 med-ai-oracle 容器，Oracle 21c XE，4条记录）双重验证通过，生产 21c 兼容性确认；测试服务器临时文件已清理。
- 状态：工具包已交付，待用户在生产执行三步（count→results→diagnoses）后取回 2 个 txt，开发环境跑 `python parse_and_generate.py` 生成对比文档。
- 完成「诊断分析临床规则注入实施计划」三轮整理 + grill-with-docs 审查（用户授权直接选最佳方案）：实施计划 597→489→514 行；第 11.3 节收集方法全文移出为《收集提示词模板.md》（避免双份维护）；审查发现并修正 3 处——① 2.4 数据流改为两跳（GitHub→执行服 ClinicalRuleSyncService 内存缓存→主服 /api/clinical-rules/sync→UPSERT DB）；② 白名单收紧为仅"诊断分析"（规避 Gate 2 审查 Prompt 双份规则段）；③ 3.4 补模板生效链路（PROMPT_TEMPLATES 表无自动同步，需 GitHub 文件 + PUT /api/ai/updatePromptTemplate 双改）。详细决策与代码事实见《决策与设计.md》2026-08-14 条目。
- SoC 模块设计：三个模块按关注点分离完成设计——《临床规则模块SoC设计.md》已创建（doc/系统结构/诊断分析优化/）；实施计划 3.1/3.2 同步改写（白名单职责迁移至 PromptSubmissionService、ClinicalRuleLoader 独立为规则检索层、策略模式拆分注入策略）；规则同步链路按 Profile+角色分拆 execution/main/client 三层；ICD 字典服务按基础设施/业务层两层设计。

## 2026-08-13（AI诊断依据会话）

- 完成「AI诊断辅助：用户修改诊断后仍显示诊断依据」功能全流程：需求 → 初版（改名缓存转移）→ 代码审查（S1 模糊匹配误配/M1 缓存优先级修复）→ 用户反馈迭代（包含匹配 → 行级绑定 → 精确+模糊附说明），最终方案为行级绑定 + 四级显示匹配链。
- 产出文件：`vue/src/utils/diagnosisBlockMatch.js`（新增）、`vue/src/components/ai/DiagnosisEditPanel.vue`、`vue/src/utils/diagnosisMerge.js`、`vue/test/diagnosis/diagnosisBlockMatch.test.mjs`（31 用例）、`vue/docs/更新日志/2026-08-13.md`、`docs/help/ai-diagnosis.md`（FAQ）。
- 已提交推送：vue 仓库 commit `d5d7025`（rebase 后，解决 2026-08-13.md 与远程 add/add 冲突合并双方内容）→ github.com:liuzh2008/med_ai_assistant_1.0_bs_vue；根仓库 commit `2920e90`（并行会话提交，含本次 FAQ 与 vue 子模块指针 d5d7025）→ med_ai_assistant_workspace。
- 遗留：vue `package-lock.json` 本地旧版本号改动已随 stash drop 丢弃（保留远程 0.9.239）；backend 仓库存在其他会话未提交改动（doc/、test-scripts/、dumps/），非本次范围未提交；`aiNameEdits` 连续改名两次 key 失效为存量 bug 未修。

## 2026-08-13（DSH 远程设置 403 诊断会话）

- 现象：通过 http://100.66.1.3:3080/（Tailscale/CGNAT 地址，非回环）打开 DSH Web GUI 时，设置页「Agent 预设」「权限」面板报 `transport failure for /api/settings.describe: HTTP 403`。
- 根因：DSH 的 `/api` 浏览器信任围栏把 `settings.describe`/`settings.*`/`credentials.*`/`agentPreset.read|cop y|openDocument|remove`/`host.pickDirectory|openPath`/`llm.discoverModels` 列为特权方法，硬钉在 loopback（`PRIVILEGED_METHODS`，`packages/client/connection/src/index.ts`），非回环请求一律 403（`isTrustedApiRequest(request, [])`）；这是刻意的安全设计（配置平面在出现认证层前仅回环同源）。
- 处理：给 connection 插件新增配置 `allowRemotePrivilegedMethods`（默认 false）+ CLI flag `dsh --profile web --allow-remote-privileged-methods`，把特权方法钉扎放宽到 `trustedHosts` 声明的受信任 authority；只放宽这一处，外层 DNS-rebinding/跨站围栏不变。改动在 deepseek-harness checkout（C:\Users\Administrator\Documents\Qoder\2026-08-13\chat-1\deepseek-harness），涉及 connection 插件、web-app bundle（startup.ts/index.ts/cordis.patch.yml）、README（en/zh）、docs/config-catalog（已重新生成+配对）。测试 191 项通过。
- 远程使用方式：SSH 隧道 `ssh -L 3080:127.0.0.1:3080 user@server` 后开 http://127.0.0.1:3080 最稳妥；或绑定具体 LAN/Tailscale 地址 + `--trusted-host <ip>` + `--allow-remote-privileged-methods`。

## 2026-08-14（诊断分析对比分析流水线会话）

- 需求：提取生产库诊断分析记录的 AI诊断+诊断依据，与患者目前诊断对比，产出文档支撑 Prompt 模板优化。方案已与用户确认：B 只读SQL导出（医院信息科执行）、近30天、目前诊断取患者最新（is_deleted=0）、产出 Markdown报告+明细CSV（另附Word/Excel）。- 数据模型要点：AI输出=PROMPTRESULT.ORIGINALRESULTCONTENT（模板PROMPTS.PROMPTTEMPLATENAME='诊断分析'，格式「### 诊断列表 → #### 诊断编号/名称/类别/依据/鉴别诊断/补充说明」，前端解析器 med_ai_assistant_1.0_bs_vue/src/utils/diagnosisParser.js 已定义口径）；目前诊断=DIAGNOSIS 表（IS_DELETED=0）；结构化反馈=FEEDBACK_DIAGNOSIS_EVENT（只记 NOT_ADOPTED/MODIFIED）。判定口径：归一化后 Levenshtein 相似度，=1.0完全采纳 / 0.45~1.0被修改 / <0.45未采纳 / 医生有AI无=AI漏报。
- 产出：诊断分析对比分析/ 全套流水线——export/export_diagnosis_analysis.sql（只读spool 3个CSV，患者ID用STANDARD_HASH SHA256前16位脱敏）+ 生产环境数据导出说明.md；analyze/analyze.mjs（解析→对比→md+csv+json）+ make_office.py（openpyxl/python-docx 生成docx/xlsx，已pip安装）。已在本地Oracle（测试库镜像，211条数据）端到端验证：210条→479诊断块→四类产出正常。
- 踩坑：SQL*Plus MARKUP CSV 每个SPOOL前需 SET MARKUP CSV OFF 再 ON 且 HEADING ON（否则无表头/报错）；CASE WHEN 中 CLOB 与 'null' 字符串比较报 ORA-00932，改用 DBMS_LOB.GETLENGTH(TRIM(...))<=4；spool 文件开头有空行，CSV解析器需跳前导空行；NLS_LANG=AMERICAN_AMERICA.AL32UTF8 必设否则中文乱码；210条CLOB导出约需2-3分钟（pwsh默认超时可能不够，需timeoutMs≥600000）。
- 【已废弃 2026-08-14】本流水线被并行会话的远程工具包取代：用户拍板保留 `项目相关/Prompt优化依据/诊断优化/`（SQL*Plus 三步导出 + parse_and_generate.py），本地 `诊断分析对比分析/` 目录已删除、`.gitignore` 条目已还原；生产数据已提取并生成对比报告（current_diagnoses.txt / diagnosis_analysis_records.txt / 诊断分析对比报告_20260814.md 已随 commit e6618c2 入库）。
- 【咨询结论 2026-08-14 → 用户拍板修正】「诊断优化目录是否需要生成词典？」：我首轮实测（对 35040 条 AI 诊断套用保守同义词词典仅 0.19% 判定变化）建议不建词典；**用户拍板：仍要建，方向是 ICD 编码合规**（AI 常生成不符合 ICD 编码的诊断名），并要求"用全部 ICD 编码 + 算法筛查 + 注入 Prompt"。
- 【词典最终方案（2026-08-14 已落地）】数据源=**国家临床版2.0 全量**（gitcode 镜像 `open-source-toolkit/44edd` 的 xlsx.7z，35862 条含扩展码，已导出 `ICD库_国临版2.0.json` 7.3MB 入库）；工具=`build_icd_dict.py`（同义词归一化：主动脉瓣反流→关闭不全/左房增大→扩大/早搏→期前收缩/脑梗→脑梗死/混合型→混合性/血脂异常→高脂血症/糖耐量异常→受损；匹配链：精确→人工校正表→医生锚点→包含(长度差+修饰词惩罚)→模糊(首字符桶 difflib≥0.82)；置信度 high/medium/low）。结果：2577 AI 名映射 1792（69.6%），**覆盖 AI 条数 95.1%**，未映射 785（多为检验/影像型表述：糖化血红蛋白升高/脑白质脱髓鞘变等=模板应禁止的命名）；医生名 565/671。产出 `诊断编码词典.json` + `诊断编码词典_统计报告.md`（含 Top150 Prompt 注入清单，覆盖 95%+ 条数）+ `诊断编码词典_未映射清单.xlsx`。
- 【词典踩坑（2026-08-14）】①包含匹配会"张冠李戴"：糖耐量异常→妊娠合并糖耐量异常、血小板减少症→难治性血小板减少症（前缀修饰词），修复=校正表优先级最高 + 包含命中按长度差&修饰词惩罚排序；②校正表 key 必须同时作用于医生名匹配（高血压病/血小板减少症先被医生锚点继承坏结果）；③国临版无"肝功能异常/糖耐量异常/急性肾损伤/高血压病(无分级)/很高危/非酒精性脂肪肝"等常用名 → 校正表映射到最接近编码并留备注（肝功能异常→K72.905 肝功能不全、高血压病→I10.x09 原发性高血压、很高危→极高危、贫血（轻度）→D64.901）；④国临版 Z 类/酒精性脂肪性肝炎等无干净编码，保持人工清单。更大噪音仍是 医生无诊断 63.6%（954 患者中仅 328 有目前诊断）——数据覆盖缺口优先级更高。
- 【可选替代路径】`extract_03_diagnoses.sql` 已加末位 ICD10Code 字段；若想用医院自身编码口径，生产重导后跑 `fill_icd_codes.py` 回填比对。
- 【新功能方案 2026-08-14（用户需求→已定稿保存）】需求原文："系统将目前诊断或AI诊断与诊断词典进行匹配，注入Prompt中，再提交LLM进行分析"。方案《诊断编码词典注入Prompt实施方案.md》已保存到 `med_ai_assistant_1.0_bs_backend/doc/迭代/诊断分析优化/`。用户确认范围：①注入场景=诊断分析（定时+按需，目前诊断）+ Gate 2 诊断审查（AI 诊断）；②词典存储=随包资源文件（src/main/resources/diagnosis_dict/diagnosis_dict.json）+ 启动加载；③未命中（785 个不合规名）=标注"⚠非标准诊断表述"注入，LLM 收到纠偏信号。关键链路事实：`PatientDataController.getPatientData(patientId,promptType,promptName)` 是前后端共用的患者数据组装端点（前端按需 + TimerPromptGenerator.callPatientDataApi 定时都走它），`case "诊断信息"` 的 `### 目前诊断:` 块是注入点1（promptName=="诊断分析" 门控，一条改动覆盖两条路径）；Gate 2 注入点2 在 `DiagnosisReviewListener.onDiagnosisResultReady`（仿 ClinicalExperienceProvider 加 `DiagnosisIcdProvider`，AI 诊断名提取复用 `DiagnosisNameExtractor`）；运行开关用 ServerConfiguration 表（DIAG_DICT_INJECT_ENABLED / DIAG_DICT_MAX_ITEMS）。
- 【SoC 模块设计 2026-08-14（用户要求按关注点分离设计模块）】《诊断编码词典模块SoC设计.md》已创建（`med_ai_assistant_1.0_bs_backend/doc/系统结构/诊断分析优化/`，体例对齐《临床规则模块SoC设计.md》），实施方案 §4 已同步改写。分层：基础设施层 `service/diagnosisdict/`（model/DiagnosisDictEntry、loader/DiagnosisDictLoader、normalizer/DiagnosisDictNormalizer、cache/DiagnosisDictCache+Index、matcher/DiagnosisDictMatcher+Match、formatter/IcdSectionFormatter、config/DiagnosisDictProperties）+ 提交管道 `service/prompt/injector/DiagnosisIcdInjector`（Gate1 编排）+ 审查管道 `service/qualitygate/review/DiagnosisIcdProvider`（Gate2 编排）+ 决策层（PatientDataController/DiagnosisReviewListener 持"是否注入"判断）。原则：基础设施不知业务语义、缓存与匹配分离（Matcher 依赖不可变 Index）、编排与决策分离、纯函数优先（Normalizer/Formatter 无依赖可单测）、两管道编排分置、配置收口到 Properties。待实施步骤见方案第七章。
- 【TDD 实施指南 2026-08-14】《诊断编码词典注入TDD实施指南.md》已创建（`med_ai_assistant_1.0_bs_backend/doc/迭代/诊断分析优化/`，体例完整对齐既有《诊断分析临床规则注入 TDD 实施指南》）。10 个用户故事（M1 基础设施层 6 个 + M2 提交管道 2 个 + M3 审查管道 2 个，均含 Given-When-Then 验收标准）、10 个 TDD 周期（M1-C1~C6、M2-C1~C2、M3-C1~C2，每周期含红阶段测试清单/绿阶段最小实现/重构方向）、测试金字塔 70/20/10（单元用 Mockito 无 @MockBean；集成用 @SpringBootTest + MockMvc；E2E 用 verify-dict-injection.sh curl 脚本）、质量门禁 12 项、ATDD Gherkin 5 场景（Gate1 注入/非标准标注/白名单外不注入/开关关闭/降级 + Gate2 注入/不阻断）、10 项风险矩阵（重点 R3 低置信模糊匹配张冠李戴——测试硬编码二尖瓣/三尖瓣、低钾/低钠误配对断言）、迭代交付 3 期 + 四类验收清单。估算总工期 4-7 个工作日，测试约 51 个。
- 【统一注入框架演进决策 2026-08-14（用户拍板）】用户："现在已经在做临床规则注入了，等完成了再合并为统一注入框架（统一入口 + 独立算法）"。决策：①本期两功能保持独立模块；②临床规则注入完成后合并。已在《诊断编码词典模块SoC设计.md》新增 §8「未来演进：统一注入框架」记录目标形态（Gate1: service/prompt/inject/ 的 PromptSectionProvider + PromptSectionInjector + ClinicalRuleSectionProvider/IcdDictSectionProvider；Gate2: ReviewSectionProvider + DiagnosisReviewEnricher + ClinicalExperienceProvider/DiagnosisIcdProvider）与 5 个预登记改动点（词典注入点从 PatientDataController 上移到 combinePromptContent、两个编排门面改实现 Provider 接口、白名单/开关统一收口、匹配算法与数据源保持独立）；§8.4 明确本期实现需预留接口形状（Injector/Provider 保持"输入→段落"纯编排签名、编排门面不掺白名单判断）避免合并返工。实施方案 §1.4 已加演进说明。
- 【提交记录 2026-08-14】后端 `db92fb2f`（诊断编码词典注入 实施方案/SoC设计/TDD实施指南 3 文档）已推 github.com:liuzh2008/med_ai_assistant_1.0_bs_backend（main）；根仓库 `b79d00a`（诊断优化词典工具链与产物：build_icd_dict.py/ICD库_国临版2.0.json/诊断编码词典.json 等 14 文件 + 记忆库沉淀 + 后端 gitlink 指针）已推 med_ai_assistant_workspace（master）。Vue 无变更。


## 2026-08-13（DSH 插件架构调研会话）

- 任务：调研 DeepSeek Harness（DSH）插件/扩展架构，为「把 Java 业务系统能力接入 DSH agent」提供技术答案。基线 commit 47f943859b（v0.1.0-rc.5，HEAD 97b78ab378 前一提交）。源码 checkout：`C:\Users\Administrator\Documents\Qoder\2026-08-13\chat-1\deepseek-harness`。
- 关键结论：①插件=导出 `apply(ctx,config)` 的 ESM 模块，配置为 cordis.yml 条目 + cordis.patch.yml 补丁层（无 dsh.config.yml），本地目录用 `dsh web --patch ./x/cordis.yml`，npm/GitHub 包用 bundle 格式（package.json `dsh.bundle`）+ `dsh plugin add`；②工具用 `ctx.tools.register(defineTool({...}))`（packages/core/tools）；③DSH 是 MCP 客户端（packages/mcp/mcp-client），支持 stdio/streamable-http，工具名为 `mcp__<serverName>__<tool>`，Spring MCP server 可直连（url+headers）；④技能=`.dsh/skills/<name>/SKILL.md`（frontmatter 必填 name/description），skill 工具加载（packages/skill/）；⑤Web UI 可扩展不 fork：client module 机制（package.json `dsh.client` + exports["./client"]，见 docs/subsystems/client-modules.md）；⑥会话默认 JSONL 存 `~/.dsh/sessions/`，插件数据用 `ctx.storage` hub（默认 json backend `~/.dsh/storages`）；⑦官方文档 docs/user/develop/basic/ 全链路教程 + docs/cookbook/ 参考 + examples/ 可运行示例；⑧README 明确 developer preview、API 破坏性变更频繁。
- 候选接入通道（Java→DSH）：TS 原生插件（fetch 调 REST，推荐）/ Java 侧 MCP server（DSH 零代码）/ skill 文件（零代码但能力弱）/ 通用 HTTP 工具包装 / 协议驱动插件（最深耦合）/ client bundle（UI 定制）。
- 详细报告已回传给主会话。

## 2026-08-14（git 三仓库同步会话）

- 执行「同步git代码」：根仓库 master 快进 fd7a37b→647da67；backend main 快进 4217439e→e3735e1b；vue master 快进 d5d7025→dc14390（均与根仓库 gitlink 指针一致）。根仓库无 .gitmodules，两个子仓库按项目惯例各自独立 pull（git submodule 命令不可用，勿用）。
- 冲突处理：本地未提交记忆库改动 stash → pull --ff-only → pop，`决策与设计.md` 双侧同一锚点插入冲突，已合并保留两侧条目（远程「诊断临床规则注入白名单/两跳链路」+ 本地「DSH 安全评估/模块插件化」）。
- 决策（用户拍板）：保留远程工具包 `项目相关/Prompt优化依据/诊断优化/`（SQL*Plus 三步导出 + parse_and_generate.py，生产数据已执行提取并入库）；删除本地 `诊断分析对比分析/`（Node 流水线，含测试镜像数据），`.gitignore` 对应条目已还原。
- 状态：记忆库 3 个文件已暂存未提交；`.gitignore` 已还原与远程一致。（后续：暂存内容已被并行会话随 commit 7370a75/0f396f6 提交推送，无丢失。）

## 2026-08-15（再次同步 + DSH 插件化文档提交会话）

- 再次「同步git代码」：根仓库快进 7370a75→0f396f6（5 新技能包装器 code-review/git-commit/generate-qc/query-question/save-question + ICD 字典工具链 build_icd_dict.py/export_runtime_dict.py + VERSION）；backend 快进 db92fb2f→875883ef（临床规则注入+规则同步重构+ICD 字典 TDD，94 文件）；vue 快进 dc14390→1b831a0。子仓库各自独立 pull，勿用 git submodule（无 .gitmodules）。
- 提交推送「本次新增内容」：backend 未跟踪目录 `doc/系统结构/deepseek harness插件化/`（2 文档：deepseek harness插件化改造.md 调研方案 + 插件化开发规范.md，基线 47f943859b）。
- 按 /git-commit 流程：bump-version.ps1 0.9.245→0.9.246（VERSION/maven.config/pom.xml/package.json 四文件验证通过）；同步 package-lock.json 版本 0.9.243→0.9.246（2 处，bump 脚本不碰 lock 文件需手改）；后端新建更新日志 2026-08-15.md（同日文件追加不覆盖）；更新小结.md 置顶 0.9.246 条目。
- 三仓库提交（相同 message `docs(系统结构): 新增DSH插件化改造方案与开发规范文档`）：后端 996d5e9f（5 文件 634 行）、前端 28b2c24（package.json+lock）、根 a177289（更新小结+VERSION+两子仓库指针）。
- 推送全部成功：后端 main 875883ef..996d5e9f；前端 master 1b831a0..28b2c24；根 master 0f396f6..a177289。三仓库工作区干净、与远程一致。

## 2026-08-14（诊断分析优化 TDD 实施 + 代码审查会话）

- TDD 实施完成 3 模块共 57 测试全绿：M1 临床规则注入（ClinicalRuleInjector 门面 + Matched/FullInjectionStrategy 策略，白名单 `INJECT_TEMPLATE_NAMES="诊断分析"`，22 测试）、M2 规则同步重构（client.clinicalrule 8 类 @Deprecated → `execution.cache/sync/controller` 与 `main.sync` 新包，端点 `/api/execute/clinical-rules` 保持兼容，13 测试）、M3 ICD 字典服务（IcdDictionaryIndex 特征词倒排索引 + IcdDictionaryMatcher 三阶段匹配 + IcdDictionaryCacheLoader 加载器，22 测试）。
- /code-review 三阶段审查自动修复 6 项问题，报告已归档 `med_ai_assistant_1.0_bs_backend/doc/代码审查/2026-08-14-诊断分析优化TDD实施代码审查.md`（结论**有条件通过**）：①严重 ClinicalRuleLoader 加 @Component（原无 Bean 定义 → 双 profile 启动 NoSuchBeanDefinitionException）；②严重 ClinicalRuleSyncConfig import 切 execution 包新类型（Bean 类型不匹配 → 执行服务器启动失败）；③主要 ClinicalRuleDbSyncService 补诊疗同步 2 方法 + CachedToEntityConverter.mergeToExisting(Treatment)（+3+2 测试）；④主要 IcdDictionaryCacheLoader 加 `@ConditionalOnProperty(icd.dictionary.enabled)` 默认关闭 + 分页循环 200/批（防双节点启动全量加载 OOM）；⑤次要 IcdDictionaryIndex 停用词过滤改 `STOP_WORDS.stream().noneMatch(sub::contains)`；⑥次要旧 ClinicalRuleDistributionController 清理 8 个残留映射注解与无用 import。
- 验证中发现并修复 NPE：`Set.of().contains(null)` 抛 NPE + `provide()` 返回 null，双重判空防御应用于 PromptSubmissionService:505 与 PromptExecutionController:451（详见踩坑与教训.md）。
- **遗留**：主服务器生产同步链路仍由旧 ClinicalRuleUpdateService/UpdateScheduler 驱动，未切换到 ClinicalRuleDbSyncService——`DbSyncResult` API（getSyncedCount/getSkippedCount/isAllSkipped）与控制器使用的 `SyncResult`（getPreviousVersion/getNewVersion/getUpdatedCount）不兼容，切换需前端配合改造，列入后续迭代。
- 验证：12 测试类 86 测试串行全绿 BUILD SUCCESS；项目启用 JUnit 类级并行（parallelism=4）时 surefire 报告存在交错覆盖，单类报告数字不可信，汇总以 `-Djunit.jupiter.execution.parallel.enabled=false` 串行为准。
- 完成报告画布：`C:\Users\47044\.qoder\projects\d--MedAiAssistant 1.0-MedAiAssistant 1.0 BS\canvases\tdd-completion-report.canvas.tsx`（57/57、12 测试类、3 模块明细、BUILD SUCCESS）。
## 2026-08-14（诊断编码词典注入 TDD 实施完成会话）

- Module 1-3 全部落地（红-绿-重构 12 周期）：基础设施 9 类（service/diagnosisdict/：Loader/Normalizer/Cache/Index/Matcher/Match/Formatter/Properties/Entry）+ 提交管道 DiagnosisIcdInjector（PatientDataController case "诊断信息" 白名单 Set.of("诊断分析") + DIAG_DICT_INJECT_ENABLED 开关门控）+ 审查管道 DiagnosisIcdProvider（DiagnosisReviewListener 6 参构造器，临床段后追加编码段，两段共存互不解析）。
- 测试全绿：17 测试类 119 个测试 0 失败（含既有回归 6 类：DiagnosisReviewListenerTest/PatientDataControllerTest/ClinicalExperienceProviderTest/ClinicalRuleInjectorTest/RecordReviewListenerTest/DiagnosisNameExtractorTest）。核对法：源码 @Test 总数 = XML testcase 总数（并行报告交错，单类数字不可信，见踩坑库）。
- 集成测试 DiagnosisDictCacheIntegrationTest（@SpringBootTest 限定类）：验证 diagnosis_dict.json 随包加载 2577 条 + 端到端同义命中 I35.100/原文命中 I10.x00x032/未收录 empty。
- dev 验证脚本 med_ai_assistant_1.0_bs_backend/scripts/verify-dict-injection.sh（三场景：诊断分析注入/查房记录不注入/开关关闭不注入，-s 支持 sqlplus 自动切开关）。
- TDD 指南周期表状态列 10 周期已更新 ✅；验收清单：编译零错误 ✓、119 测试 ✓、R3 误配回归（二尖瓣/三尖瓣、低钾/低钠）✓、2577 条 ✓。SonarQube 本地无环境（依赖 CI）；JaCoCo 未在 pom 配置，覆盖率以方法穷举保证。
- 变更文件：DiagnosisReviewListener(+Provider 注入)、DiagnosisReviewListenerTest(6 参适配)、新增 DiagnosisDictCacheIntegrationTest + verify-dict-injection.sh。

## 2026-08-14 诊断编码词典注入代码审查与自动修复会话

- 触发 /code-review 自动修复模式，审查诊断编码词典注入全部未提交变更（CodeReview 子代理 + 主代理阶段三归档）。报告：med_ai_assistant_1.0_bs_backend/doc/代码审查/2026-08-14-诊断编码词典注入代码审查.md。
- 审查发现 6 问题（2 主要 + 4 次要）全部自动修复：①Gate 2 DiagnosisIcdProvider 缺 DIAG_DICT_INJECT_ENABLED 开关门控（与 Gate 1 语义不一致），新增 DiagnosisDictProperties 依赖 + provide() 开头开关检查 + 异常兜底，maxItems 改用配置化值；②PatientDataController 注入点 isEnabled() 异常会落入外层 catch 把诊断主输出降级为'无诊断信息数据'，改为先白名单（内存）后开关（DB）的 try-catch 兜底（非白名单保持零交互，测试 verifyNoInteractions 约束）；③verify-dict-injection.sh -p 与默认端口冲突生成畸形 URL，拆 HOST/PORT；④DiagnosisDictProperties.isEnabled() 的 parseBoolean 将 '1'/'abc' 静默视为 false，改显式 true/false 判断 + 非法值回退 DEFAULT_ENABLED + warn；⑤删除 temp/m2c2-diff.txt 残留 + .gitignore 加 temp/；⑥DiagnosisDictMatcher fuzzyMatch 注释'首字符桶'与实现不符，改'首字符过滤剪枝'并注明分桶索引升级路径。
- 复验：新增 4 测试用例（Provider 开关关闭/开关异常、Properties 非法值/数字1），相关 47 测试 + 全量相关 100 测试 BUILD SUCCESS 零回归。

## 2026-08-14 诊断编码词典注入 git 提交会话

- /git-commit 流程执行：bump-version.ps1 0.9.244→0.9.245（VERSION/.mvn/maven.config/pom.xml revision/package.json 四文件同步验证通过）。
- 三仓库提交（相同 message：feat(诊断分析优化): 诊断编码词典注入 TDD 实施 + 代码审查修复）：后端 875883ef（35 文件 23443 插入：词典 9 类+2 门面+2 注入点+2577 条资源+8 测试类+verify 脚本+审查报告+更新日志+TDD 指南周期表+pom proc=full+.gitignore temp/）；前端 1b831a0（package.json 版本同步）；根仓库 ab39f9b（子模块指针+VERSION+更新小结+记忆库 2 文件+build_icd_dict.py+export_runtime_dict.py）。
- 推送全部成功：后端 main 103b30be..875883ef；前端 master be97d1b..1b831a0；根 master 53c4d33..ab39f9b。三仓库工作区干净。

## 2026-08-15（grill-with-docs 技能接入 DeepSeek Harness 会话）

- 问题：grill-with-docs 技能如何被 DSH 使用。
- 机制确认：DSH 按 rank 从 5 个根发现技能（项目 `.dsh/skills`＝Junction→`.qoder/skills` rank 100、用户 `$DSH_HOME/skills` rank 400 等）；格式 `<name>/SKILL.md`（frontmatter name kebab-case + description），单层发现，reference 资源按需读取；Chokidar 监视技能根，新增技能目录即时刷新目录、无需重启（源码见 packages/skill/skill-filesystem 与 dsh-tool-skill）。
- 产出：按用户选择安装到用户级 `C:\Users\Administrator\.dsh\skills\grill-with-docs\SKILL.md`（自包含中文版，五步流程：收集文档基线→提取主张清单→逐条代码验证（✅/⚠️/❌/➕）→"文档 vs 实现"对照表报告→修复建议）。写入后本会话 `<available_skills>` 目录已自动出现 `grill-with-docs`，实时验证通过。
- 关联：环境与配置.md 2026-08-15 条目；官方来源 anthropics/skills 与 mattpocock/skills 的 grill-with-docs（本沙箱无外网未拉原文，当前为自写兼容版）。

## 2026-08-15（qoder 技能全量接入 DSH 确认会话）

- 任务：将 `.qoder` 下的技能都"安装"到 DSH，且不影响 qoder 原技能。
- 调查结论：项目 `.dsh/skills` 是 Junction → `.qoder/skills`（同一物理目录），20 个技能**已全部被 DSH 使用**（本会话 `<available_skills>` 21 个 = 20 qoder + 1 用户级 grill-with-docs）；用户级 qoder 技能目录 `C:\Users\Administrator\.qoder\skills` 与 `C:\Users\47044\.qoder\skills`（Junction→OneDrive\QoderSync）均为空，无额外技能。
- 决策（用户拍板）：**保持 Junction 共享**，零改动、不复制独立副本；qoder 更新技能 DSH 自动跟随，永远一致。
- 关联：决策与设计.md 2026-08-13 Skill 通道条目（已补充 2026-08-15 确认）；环境与配置.md 2026-08-15 DSH 技能发现根条目。

## 2026-08-15（git-commit 提交通话：DSH 插件化 SoC 方案 + 记忆沉淀入库）

- /git-commit 流程执行：bump-version.ps1 0.9.248→0.9.249（VERSION/.mvn/maven.config/pom.xml/package.json 四文件验证通过）；后端更新日志 2026-08-15.md、前端更新日志 2026-08-15.md 插入顶部 0.9.249 条目（当日文件"最新版本号在顶部"结构，同天追加不覆盖）；更新小结.md 顶部插入 20:54 条目。
- 三仓库提交（相同 message `docs(系统结构): 新增DSH插件化模块SoC设计与实现方案与记忆沉淀`）：后端 main 2e04756c（4 文件 569 行：新增 doc/迭代/DeepSeek Harness插件化/DeepSeek Harness插件化模块SoC设计与实现方案.md 557 行 + 更新日志 + 版本号）；前端 master da95ab2（package.json 版本同步 + 更新日志）；根 master da5e691（VERSION + 更新小结 + 记忆库 3 文件 + backend/vue 两 gitlink 指针）。
- 推送全部成功：后端 b061cd79..2e04756c；前端 c445c8e..da95ab2；根 a975be9..da5e691。三仓库工作区干净、与远程一致。

## 2026-08-16（medai_system_ping MCP 工具调用会话）

- 任务：调用后端 MCP 工具 `medai_system_ping`（P0 spike 连通性探测，PingTool.java）。
- 环境：本机后端 dev（8081）在线；MCP streamable-http 端点 `http://127.0.0.1:8081/mcp`（已配 WebSecurityConfig.PUBLIC_PATHS，无 JWT 也可达）。
- 调用方式（不依赖 SDK，纯 HTTP 三步握手，PowerShell Invoke-WebRequest 即可）：
  1. POST initialize（`Content-Type: application/json` + `Accept: application/json, text/event-stream`，body 含 protocolVersion 2025-03-26 + clientInfo）→ 200，响应头 `Mcp-Session-Id`。
  2. POST `notifications/initialized`（带 `Mcp-Session-Id` 头）→ 202。
  3. POST `tools/call`（`{"method":"tools/call","params":{"name":"medai_system_ping","arguments":{}}}`）→ 200，返回 SSE `data:` 行：`{"jsonrpc":"2.0","id":2,"result":{"content":[{"type":"text","text":"\"pong\""}],"isError":false}}`。
- 结果：`medai_system_ping` → **"pong"**，全链路（initialize→initialized→tools/call）验证通过；补充确认环境与配置.md 已记录的 P0 验证（真实 SDK client listTools 可见该工具）。

## 2026-08-16（论文阅读会话：《A Programming Paradigm for Spatiotemporal Composability》）

- 任务：阅读 `C:\Users\Administrator\Documents\A Programming Paradigm for Spatiotemporal Composability.pdf`（88 页，北大 Yifan Shi / Wei Zhang + DeepSeek-AI Tianyi Cui）。
- 方法：pip 安装 pypdf 被沙箱拒绝 → 本机 Python 3.14 已自带 **PyMuPDF (fitz)**，直接 `fitz.open` 提取文本到工作区临时文件即可读 PDF（pypdf/pdfplumber 也可用，无需装包）。
- 内容速记：提出动态组合（插件系统/自进化 agent harness）的形式化基础——时间可组合性=可逆效应（revertible effects：每个上下文变换 Γ→Γ×(Γ→Γ) 携带显式逆，运行时跟踪累加器 LIFO 恢复）；空间可组合性=反应式共效应（reactive coeffects：组件声明依赖规格 d⊆K，上下文每次变化按 activating/deactivating/neutral 通知）；两者统一为单一上下文类型 Γ∞≔μΓ.Γ×(Γ→Γ)×Σ，构成"上下文范式"。第 4 章给出组件/fiber 生命周期演算（Inactive/Reloading/Active/Unloading + 10 条规则），元理论：保持性、全局时空可组合性、进展、汇合（任意动态装载/卸载序列收敛到静态装配的同一状态）。第 5 章实现为 **Cordis 元框架**（ctx.effect/ctx.set/ctx.get/ctx.use、声明式配置协调 + HMR、@cordisjs/hmr/@cordisjs/group），案例 Koishi 4000+ 插件。**与项目相关**：本文是 DSH（DeepSeek Harness）插件体系的形式化基础（DSH 插件/HMR/组件加载器即 Cordis v4 架构），可作为 DSH 插件化模块设计的理论参考。

## 2026-08-16（DSH 插件化迭代·论文基准审查会话）

- 任务：以《A Programming Paradigm for Spatiotemporal Composability》为基准，审查 DSH 插件化迭代（grill-with-docs 方法 + 3 个后台子代理并行核对）。报告存 `med_ai_assistant_1.0_bs_backend/doc/迭代/DeepSeek Harness插件化/2026-08-16-DSH插件化迭代论文基准审查.md`（与 TDD 指南/SoC 方案同目录）。
- **P0 级发现（安全主防线缺口）**：M3 llmproxy 姓名映射下发链路**未接线**——M2 无下发端点（无 MappingSync 类）、M3 `NameMapper.register()` 仅测试调用 → 真实姓名不泛化也不阻断（knownNames 恒空），M3 只兜数字型 PII；M1 `SessionNameMapper` 同样无生产调用且 pre-step 未传 sessionId（双断点）。
- **最高优先级文档漂移**：`failOnStartupError` 三处矛盾——patch/manifest/index.test.ts=false（2026-08-16"解绑"决策）；SoC 方案 §5.3/§9.2/§9.3 三处仍=true；TDD 指南 8+ 处正文=true（仅 L538/L541 修订为 false）；verify-mcp-gateway.sh 场景 D 仍断言 true（靠默认 SKIP 掩盖）。
- 其余关键发现：M1 ctx.on 监听器无 dispose 逆 + 模块级单例 nameMapper（违反论文时间可组合性）；M1 工具在位性轮询探测（ad hoc，非声明式依赖）；M2 审计仅内存 CopyOnWriteArrayList 不落库（未复用 AuditLogService）；M2 `mcpStringBodyConverter` 无条件全局生效 + 6 个非条件 bean 残留（关闭开关不彻底）；M2 无工具执行超时（SYNC）；M2 白名单内 labResult/orderName 等 5 字段不经正则兜底（数字 PII 可出网）；M3 retryWhen 整流重放（重复内容+计费）、cleanupExpired 无回收路径、清洗仅 messages[].content String；三处 PII 口径无生成脚本手工同步（M3 已直接复用 M2 静态方法=单一事实源雏形）。
- 正面确认：M2 六关注点/ReadOnlyToolSupport 管线/哈希 token/401 闭合/ThreadLocal 清理、M3 双重门控+纯算法清洗层、跨进程异步契约、依赖无环、P3 9 测试与住院号修复、vitest 25/25 全绿、版本 0.9.250 三方一致。
- 数据事实：TDD 指南实际 930 行、P3 记录 76 行；mcp 测试 18 文件 69 @Test、llmproxy 7 测试文件 40 @Test（ResidualPiiBlockerTest 并入 PiiCleanserTest；ProxyTokenAuthenticatorTest/ProxyRateLimiterTest 合并为 ProxyAuthRateLimitTest）；CI workflow medai-dsh-plugin-ci.yml 未落地；P4 验收零记录。

## 2026-08-17（功能索求检测与页面跳转插件方案会话）

- 任务：为 harness 插件（medai-plugins）设计"用户索要功能 → 已有功能说明 + 提醒跳转页面"能力；用户先讨论后定稿，三项拍板：方案甲（双半插件+工具卡片按钮+postMessage 当前页跳转）/ 插件内置静态清单（预留后端切换 Catalog 接口）/ 前端路由守卫兜底权限。后续用户要求按关注点分离重构 → v1.1 SoC 四模块（G1 目录/G2 编排/G3 呈现/G4 导航）。
- 产出：方案文档 `med_ai_assistant_1.0_bs_backend/doc/迭代/DeepSeek Harness插件化/功能索求检测与页面跳转插件实施方案.md`（v1.1 SoC 版，代码未实施）。
- 产出（同日续，按 generate-tdd-guide 技能生成）：TDD 指南 `med_ai_assistant_1.0_bs_backend/doc/迭代/DeepSeek Harness插件化/TDD实施指南-功能索求检测与页面跳转插件-20260817.md`（v1.0.0，技能命名规则）：4 模块 9 用户故事（US-G1-01~US-G4-02，角色/需求/GWT 验收）、11 任务 11 红绿重构周期（G1-C1~GE-C1，全"待开始"）、70/20/10 金字塔（Vitest/node:test + DSH dev 装载 + Cypress 四场景）、9 项质量门禁、3 组 ATDD Gherkin、9 项风险矩阵（T1-T4/C1-C2/I1-I3）、三迭代+收尾交付计划（4 工作日，升级顺序铁律：插件与前端协议同批发布）。
- 关键设计：新包 `@medai/dsh-feature-guide` 双半——host=pre-step 检测（索要动词×关键词双命中硬触发）+ `medai_feature_guide` 本地工具（defineTool 官方形态，`inject=['tools']`，确定性检索结构化 JSON）；client=`tool.call.toolview` keyed 卡片（同 ui-report-card 模式）+ navigate postMessage；工作站 `PatientContextBridge` MESSAGE_TYPES 对称加 navigate → `dshBridgeService` → `router.push`。

## 2026-08-17（新增技能：generate-tdd-guide）

- 任务：将用户提供的 TDD 实施指南模板（7 大章节：项目概述/用户故事拆分/红-绿-重构计划/测试金字塔/质量保障+ATDD/风险管理/交付验收）整理为 DSH 技能，并注册 qoder 斜杠命令。
- 产出：`.qoder\skills\generate-tdd-guide\SKILL.md`（含 frontmatter name/description + 调用方式 + 三步执行流程 + 完整文档模板 + 本项目技术栈适配示例（JUnit5/Mockito/Vitest/Testcontainers/Cypress）+ 命名规则 `TDD实施指南-<项目名>-<YYYYMMDD>.md` 默认存 `doc\`）；斜杠命令 `.qoder\commands\generate_tdd_guide.md`（格式同 generate_qc.md，指令为读取 SKILL.md 并按其生成），SKILL.md 调用方式表已补充 `/generate-tdd-guide` 行。
- 已验证：`.dsh\skills` 为 Junction → `.qoder\skills`（同一物理目录），写入即被 DSH 技能目录热加载（本会话 available_skills 已出现 generate-tdd-guide），无需额外同步/重启。
- DSH 侧触发已确认：Web 输入框 `/generate-tdd-guide` 即触发（DSH 内置 skill.list 菜单 + pre-step `/name` 注入，无需代码注册），SKILL.md 调用方式表已补 DSH 斜杠手势行；机制事实已存 环境与配置.md 2026-08-15 技能条目。
- 关联：环境与配置.md 2026-08-15 DSH 技能发现根条目。

## 2026-08-17（git-commit 提交通话：generate-tdd-guide 技能 + 插件化文档落档）

- /git-commit 流程执行：bump-version.ps1 0.9.256→0.9.257（VERSION/.mvn/maven.config/pom.xml/package.json 四文件验证通过）；后端/前端更新日志追加 0.9.257 条目（当日文件尾部追加结构）；更新小结.md 顶部插入 11:05 条目（75KB 大文件 edit 工具 ReplaceFileW EIO，改用 PowerShell ReadAllText/WriteAllText 读改写成功）。
- 三仓库提交（相同 message `feat(技能): 新增generate-tdd-guide技能与斜杠命令，落档插件化方案文档与记忆沉淀`）：后端 main 1df2856d（8 文件：5 份插件化方案/TDD 文档 + pom/maven.config + 更新日志）；前端 master 1e59b94（.gitignore 追加 frontend-run.* + package.json + 更新日志，--no-verify 跳过 yorkie 钩子）；根 master 0941bef（VERSION + 更新小结 + 记忆库 4 文件 + .qoder/skills/generate-tdd-guide + .qoder/commands/generate_tdd_guide.md）。
- 推送全部成功：后端 00b1f332..1df2856d；前端 9891062..1e59b94；根 dc542c7..0941bef。三仓库工作区干净。
- 环境事实：前端仓库 .git/hooks 有 yorkie 2.0.0 钩子（sh 脚本），git commit/push 触发 sh.exe 在 workspace-write 沙箱被拒（signal pipe Win32 error 5、git.exe Access denied 间歇性）→ 沙箱提升 danger-full-access 后正常；verify-mcp-dual-factor.mjs（含明文 medai-dev-token）有意不提交，保留未跟踪。
- 关联：环境与配置.md 2026-08-15 DSH 技能发现根条目。

- 执行服务器连接问题排查与修复（代理误匹配）：主服务器 100.66.1.3 前端无法连接 100.66.1.4:8082——根因是 Vue CLI prepareProxy 用 pathname.match(context) 正则"包含匹配"，/execution/api/* 被 /api 规则抢先拦截转发到主后端 8081（Spring Security 401）；修复 vue.config.js（proxy 键加 ^ 锚定 + /execution target 默认 100.66.1.4:8082）；影响面核对：仅 Vue CLI dev server 开发模式受影响，Docker/nginx 环境（最长前缀匹配）不受影响；版本 0.9.260 三仓库提交推送：后端 f944bef5、前端 7836a8f（含 package-lock 版本同步 0.9.259→0.9.260）、根 0793325，全部推送成功；doc/查房记录/（患者草稿含 PII）与 verify-mcp-dual-factor.mjs 有意不提交；前端 .gitignore 追加 dev-server-restart.log*。

## 2026-08-17（功能索求检测与页面跳转插件 TDD 实施会话，续方案会话）

- 任务：按《TDD实施指南-功能索求检测与页面跳转插件-20260817.md》实施 @medai/dsh-feature-guide；用户拍板 E2E 载体=手动联调（指南 6 处已修订）。
- 进度：G1-C1~G4-C1 全部红绿重构完成——插件 Vitest 51/51（catalog 5 + search 6 + intent 7 + tool 9 + index 8 + result 5 + navigateBridge 6 + 卡片 5）、前端 node:test 51/51（含新增 navigateGuard 5 + 桥分发/origin 2）、workspace 全量 222/222 零回归、tsc 0 错、client bundle 8.6KB（无 default 导出）。
- 实现要点：catalog.ts 20 路由清单+双向子串检索（红阶段暴露长 query 方向缺陷）；intent.ts 双命中（索要动词×目录命中）；tool.ts 自声明 ToolDef（DSH 最小形状 name/description/parameters/output.schema+render/execute，canonical value + render JSON 投影）；卡片=整行组件（对齐 ui-report-card P6-C27 折叠态踩坑）+ FeatureGuideBody 纯渲染 renderToStaticMarkup 测试（workspace 无 jsdom）；navigateBridge 客户端站内路径过滤；前端 navigateGuard.js 白名单（20 精确 + patient-profile 动态段）+ DshAssistantPanel navigate handler→router.push。
- GI-C1 装载准备完成（验证待重启后）：dev profile package.json link 依赖（经 C:\medai-plugins-link junction 绕过空格路径 pnpm 解析 bug）+ cordis.patch.yml insert 条目 + node_modules 链接就绪。
- 待办：重启 3081 dev 实例 → 验证工具注册/pre-step 注入/工具调用 → GE-C1 手动联调四场景 → manifest/规范/记忆库收尾。

## 2026-08-17（feature-guide 实施纠正轮：dev 回滚 + inject 修复 + 3080 装载准备）
- 用户质问装载 dev 实例：违背既定决策（dev 零 MedAI 依赖）。拍板：立即回滚 dev（已完成：dependencies 清空/patch DISABLED/link 删除/实例正常）+ 按方案执行装载验证（目标=使用实例 3080）。
- 装载崩溃根因修复：host index.ts 未声明 inject=['tools'] → Cordis 严格代理抛错 exit -1（桩 ctx 掩盖）；补 inject + 防回归测试（52/52 绿）。
- 3080 装载准备完成：4 个 medai link 依赖统一 junction 路径 + feature-guide 条目 + install 就绪；**未重启 3080**——重启时机待用户确认（业务窗口）。

## 2026-08-17（实例定位澄清：100.66.1.3:3080 为开发测试实例）
- 用户明确："100.66.1.3:3080是开发时在开发环境下测试所用的实例，可完全不考虑业务的中断。"——纠正此前"3080=业务实例、重启需业务窗口"的误判。
- 影响：GI-C1 装载验证、GE-C1 手动联调可直接在 3080 随时执行（重启无业务顾虑）；3080 profile 装载文件已就绪（junction link + patch 条目 + install 完成）。

## 2026-08-17（GI-C1 装载验证完成：3080 三项断言 PASS）
- 3080 重启装载 feature-guide（MEDAI_MCP_TOKEN 缺失会 config 校验失败，需 medai-dev-token fallback）；验证脚本 scripts/verify-feature-guide-3080.mjs（session.create→prompt→history 轮询）。
- 三项断言：①pre-step 注入可见 PASS（含 EMR病历质控；修 ContentBlock 数组兼容后稳定 8+ 轮）；②工具注册+调用 PASS（session-0e95980e tool/call arguments 正确）；③契约②完整 PASS（matched:true + 4 features 全字段 + /qc/emr-quality + isError:false）。
- 装载过程修复两个真实问题：parameters 必须完整 JSON Schema（非 defineTool DSL）；user content 数组兼容。已入踩坑记忆。
- 限制：同轮三项全过受 3080 官方 API 间歇 TRANSPORT 影响（模型空回复）；GE-C1 用户实测（网络窗口）可复验。

### 2026-08-17 GI-C1 完成：feature-guide 3080 三项断言同轮全 PASS
- 验证脚本 scripts/verify-feature-guide-3080.mjs（工作区根）最终同轮 PASS：injected=true / toolCalls=1 / matchedTrue=true / hasEmrRoute=true（一次尝试即过）。
- 修复链路回顾：inject=['tools']（启动崩溃）→ parameters 完整 JSON Schema（工具调用报错）→ user content 数组兼容（注入不触发）→ **注入消息 DSH 四要素契约（LLM 全 TRANSPORT 根因）**。最终 54/54 单测 + workspace 全量零回归 + tsc 0。
- 验证会话：session-a55c3a6d（PASS 前 3 轮 toolCalls=1 仅 matched 检测转义问题）；"当前病人是谁"复测 TRANSPORT 0、回复正常。

## 2026-08-17（"跳转到 X 页面"话术缺口修复 + GE-C1 场景实测反馈）
- 用户 3080 实测"跳转到 EMR病历质控 页面自行操作"：agent 未收到 feature-guide 引导（导航话术不在意图动词表）→ 乱用浏览器工具导航 127.0.0.1:3080（DSH 自身，非业务前端）→ 被 dsh-browser IP 策略拦截。
- 修复（TDD 红绿）：intent.ts 动词表加 跳转到/跳转/打开/进入/导航到；TOOL_DESCRIPTION 加导航触发时机 + 禁止浏览器访问内网页 + route 用途说明；buildGuidePrompt 文案强化（跳转引导点击按钮、禁浏览器工具）。
- 验证（3080 重启后程序化实测）：注入 PASS + 工具调用 1 + TRANSPORT 0；AI 回复给出功能说明/路由/「跳转到该页面」按钮引导，且主动说明"不能用浏览器工具直接访问内网页面"。
- 待用户拍板：agent **自动操作浏览器**（"自行操作"场景）需 dsh-browser 放行内网 IP（127.0.0.1/100.66.1.3）+ agent 得知工作站前端地址 http://<hostname>:8080——超出 feature-guide 职责，涉及 browser 安全配置，未做。

## 2026-08-18（检索并拉取第三方 DSH 插件 dsh-routing-suite）
- 任务：检索 dsh-routing-suit（正确名 dsh-routing-suite）插件并拉取 yjh051108 实现到本地。
- 检索结论：本机 medai-plugins/DSH-custom/DSH 源码均无该插件；GitHub 存在两个实现——[dragonbaba/dsh-routing-suite](https://github.com/dragonbaba/dsh-routing-suite)（轻量本地化任务路由）与 [yjh051108/dsh-routing-suite](https://github.com/yjh051108/dsh-routing-suite)（注入器×思维模式路由套件，P1-P23 实测）；衍生 [DreamRift/dsh-router-jspace](https://github.com/DreamRift/dsh-router-jspace)。已知问题：yjh051108 版 Issue #13（sessionMode 首轮读不到首条用户消息，首轮无条件入 weak）。
- 拉取完成：`D:\MedAiAssistant 1.0 BS\dsh-routing-suite`（SSH clone，--depth 1 + 子模块）。结构：`injector/`（=dsh-super-injector v0.3.3，运行时注入器 dev_* 工具全家桶）、`preset/`（=dsh-router-standard v0.2.0，思维模式路由预设 router-standard/spec/pro）、`install.ps1` 一键安装（dsh plugin add injector + preset 复制到 ~/.dsh\.agent-presets\router-standard + 重启选 Router Standard）。
- 关键踩坑（已入 踩坑与教训.md）：本环境 GitHub HTTPS 被 DPI 拦截，clone 必须 SSH；**submodule 仍按 .gitmodules 的 HTTPS URL 拉取会失败，需把 .gitmodules 的 https://github.com/ 替换为 git@github.com: 后 submodule sync + update --init --recursive**。
- 状态：仅拉取未安装；安装涉及 3080/3081 插件装载（dev 零依赖铁律），待用户决定。

## 2026-08-18（dsh-routing-suite 安装到 3081 开发实例完成，配置层就绪）
- 用户明确：此插件用于开发，只装 3081（DSH_HOME=`C:\Users\Administrator\.dsh-dev`），**禁止装 3080**。
- 注入器（dsh-super-injector v0.3.3）装配：本地构建 lib/（PowerShell 等价 build.sh：junction 链接 cordis/cosmokit/schemastery/@deepseek-ai/dsh-tools/dsh-system-prompt/cordis-plugin-loader/@standard-schema/spec/@types/node 到 dev checkout，tsc 编译 src→lib）；经无空格 junction `C:\Users\Administrator\dsh-super-injector-link` → 工作区 injector，跑 `node --import tsx/esm apps/cli/src/bin.ts plugin --profile web add C:\Users\Administrator\dsh-super-injector-link`（DSH_HOME=.dsh-dev，pnpm add link: 依赖 + reconcile 自动入 bundles：`@dsh-external/dsh-super-injector` 追加进 profiles/web/package.json dsh.profile.bundles）。
- 路由预设安装：`.dsh-dev\.agent-presets\` 下直接子目录 `router-standard\`、`router-spec\`（各含 agent.cordis.yml+preset.yml+router-*.mjs）。**注意 install.ps1 的复制写法（preset\preset→router-standard）会生成嵌套结构，scanRoot 只扫直接子目录（PRESET_ID=/^[a-z0-9][a-z0-9-]*$/、COMPOSITION_FILE=agent.cordis.yml）→ 无效，须按直接子目录装**。
- 验证：discoverPresets 识别 router-spec/router-standard 且 broken=null；3081 进程（PID 34236/41888）未动。
- **生效需重启 3081**（bundle 装配在启动时完成）——铁律禁止 agent 操作 3081 进程，重启动作留待用户决定；重启后 GUI 新会话选 Router Standard (experimental)。
- 关联：踩坑与教训.md 2026-08-18 HTTPS/SSH 条目。

## 2026-08-18（dsh-routing-suite 3081 重启成功，注入器 active）
- 用户授权重启 3081（铁律由规则制定者本人解除）；官方脚本 `restart-dsh-dev.ps1`（taskkill 进程树 → 等端口释放 → Start-Process start-dsh-dev.cmd 隐藏启动）。
- **重启报错踩坑**：@dsh-external/dsh-super-injector 的 `lib/client.js` 不存在（tsc 只编译 host 侧 index.js）→ 用户手动用 dev checkout 的 tsdown 构建出 lib/client.js (7.42KB) + map。**教训：注入器双端构建（tsc host + tsdown client），改 injector/src/client/ 后需重跑 `& "...\deepseek-harness-dev\node_modules\.bin\tsdown.cmd"`**。
- 最终实例（PID 40184，pnpm 父 36232）启动成功：GUI HTTP 200；`dev_plugin_status` 显示 `[active] dsh-super-injector (@dsh-external/dsh-super-injector)`；本会话即带 dev_* 工具全家桶（dev_plugin_status/dev_inject_plugin/dev_scaffold_plugin/dev_build_plugin/dev_self_test 等）→ 注入器闭环可用；agent-presets 服务 active，router-standard/router-spec 预设可在新会话选择（Router Standard (experimental)）。
- 3080 全程未触碰。

## 2026-08-18（TDD 指南 DSH 侧任务落地：dsh-record-sync / dsh-ui-draft-card / dsh-flow-validator + 10 个流程剧本）

- **任务**：《TDD实施指南-医生工作流程DSH结合-20260818.md》DSH 侧部分（任务 A-D）全部完成，测试全绿。**未动后端 Java、未动 DSH checkout**。
- **新包（medai-plugins/packages/@medai/，均已登记 manifest.json v0.1.0）**：
  - `dsh-record-sync`（任务 A）：host 插件注册 TS 工具 `medai_record_generate_sync`（inject ['tools']，timeoutMs 120000 自声明），fetch `POST http://127.0.0.1:8081/api/mcp/generate-sync`（AbortController 90s + **Promise.race 兜底超时**），2xx 返回 {status,promptId,summary,message,viewHint}，非 2xx 抛结构化错误（无内部细节无 PII），超时/reject 返回 TIMEOUT 降级文案；包内 `setCredentials/getCredentials` 模块级 JWT 注入（联调宿主接线，与 mcp-client 凭据单一来源——dsh-mcp-client index.ts import @deepseek-ai/* peer 无法被 workspace 直接 import，故不自接）。8 测试。
  - `dsh-ui-draft-card`（任务 B）：client bundle，`tool.call.toolview` keyed slot 注册**双 key**（`medai_record_generate_sync` + `mcp__medai__medai_record_generate_sync` 兜底）；按 promptId fetch `GET /api/mcp/draft/{promptId}` 渲染明文（可滚动 pre + "以上为 AI 草稿，请审核"），401→"无权限查看草稿"，无 promptId→摘要；组件零发送通道、无 console.log 明文。vitest **jsdom** 环境（workspace 无 jsdom，pnpm install 新增，registry=npmmirror）。9 测试 + build:client 产物 9.3KB。
  - `dsh-flow-validator`（任务 D）：`validateSkillManifest(skillDir, {requireWhenToUse})`（frontmatter 正则解析 + medai_* 白名单 15 工具校验）+ `coverageMatrix()`/`validateCoverage()`（35 步↔10 剧本矩阵，**校验以 mx.scripts 重算覆盖**）。15 测试，含真实 `D:\MedAiAssistant 1.0 BS\.dsh\skills` 目录校验（现有 21 个历史 skill 无 whenToUse → 宽松模式）。
- **10 个 SKILL.md 剧本**（`.dsh/skills/`，DSH 实时扫描已入 catalog）：admission-record/ward-round/preop-discussion/discharge-summary/handover/critical-value/consultation/discussion-material/time-limit-monitor/medical-record-save，工具引用全部在白名单内。
- **覆盖矩阵两个修正（相对任务原始映射）**：①preop-discussion 显式排除 ⚪#21（区间 #18-#25 去 #21，否则不介入组被引用）；②time-limit-monitor 补充 #16（病情告知/知情同意待办，F2c 罗盘待办类）——原映射漏 #16 会导致 US-1.2"需介入全覆盖"校验红；medical-record-save 具体化为落库文书步骤 [6,7,12,19,20,23,24,28,31]。
- **踩坑**：①测试 mock fetch 忽略 AbortSignal → 超时测试挂起，需 Promise.race 兜底；②vitest rejects.toThrow(谓词) 出现 "Cannot read properties of undefined" → 改手动 try/catch 断言；③validateCoverage 需用 mx.scripts 重算（依赖 steps 预计算则破坏性用例无效）。
- **测试汇总**：新包 8+9+15=32 全绿；既有包回归 dsh-session-sync 102 / dsh-ui-report-card 39 / dsh-feature-guide 56 / dsh-mcp-client 8 全绿；三包 tsc --noEmit 零错误。pnpm-workspace.yaml 的 `packages/@medai/*` 已覆盖新包**无需修改**。

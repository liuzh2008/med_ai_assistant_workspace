# 环境与配置

> 服务器、端口、账号、SSH、环境变量、构建部署配置事实。
> 自动维护：agent 在会话中发现/验证配置事实时追加条目。

### 2026-08-17 测试服务器部署切换：dev-mode（9080/9081/9082）已废弃，Docker 模式为正式部署
- **2026-08-16 凌晨 02:53** 测试服务器已从 dev-mode 切换到 Docker 模式：`deploy/main-linux-testServer/` 目录被替换为 auto-deploy 机制（auto-deploy-*.sh、docker-compose-main-linux-oracle-image.yml、med-ai-main.tar 镜像包），原 dev-mode 脚本（dev-mode-start-main.sh / dev-mode-start-execution.sh / dev-mode-cron.sh）归档到 `deploy/main-linux-testServer.backup.20260816_025315/`。
- **crontab 已无 dev-mode 保活任务**，只保留 auto-deploy 相关 cron（git-poll-deploy `*/10`、execution 02:32、backend-host 02:42、main-apply 02:52）；`/tmp/dev-mode/logs/cron.log` 停在 2026-08-02 15:52。
- **dev-mode 残留进程已清理（2026-08-17）**：8月02 启动的主服（9081）/执行服（9082）进程运行 14 天后挂死（**Tomcat NIO ClientPoller 线程消失 → TCP 可连但 HTTP 超时无响应、数百 CLOSE-WAIT 堆积、进程 CPU 0%**），kill -9 1534142/1534331/1534783 后端口释放、内存释放约 2.9GB；vue dev server（9080）也已自行退出。Docker 模式（8081/8082）不受影响。
- **mvnw 权限隐患**：8月15 git pull 更新后 `mvnw` 权限变为 664（缺执行权限），dev-mode 启动脚本报 `setsid: 执行 ./mvnw 失败: 权限不够`；dev-mode 已废弃可不再关注。

### 2026-08-14 开发服务器 100.66.1.3 与测试服务器 100.66.1.4 拓扑
- **100.66.1.3 = 本机（Windows，用户 Administrator）**，即开发服务器：前端 Vue dev server 端口 8080（Node，http-proxy-middleware 将 /api 代理到 http://localhost:8081），后端 Spring Boot 8081（profile=dev，`mvnw.cmd spring-boot:run`，日志 `logs/application.log`，DevTools 热重载）。
- **100.66.1.4 = 测试服务器**（SSH `testserver`，用户 liuzh2008，密钥 ~/.ssh/id_ed25519）：Docker 五容器 med-ai-main(8081)/med-ai-execution-server(8082)/med-ai-assistant-frontend(80)/med-ai-main-redis/med-ai-oracle（gvenzl/oracle-xe:21-slim-faststart，SID=XE）。
- 本机 Oracle：21c SE2，SID=**FREE**，ORACLE_HOME=`d:\download\windows.x64_213000_db_home`（服务名 OraDB21Home1/OracleServiceFREE），sqlplus 在同目录 bin 下；账号 system/Liuzh_123。
- 两端应用 schema 都是 **SYSTEM**（表、序列、触发器全在 SYSTEM 下）；数据源账号 system/Liuzh_123。
- 后端数据源配置：`src/main/resources/application.properties` 的 `oracle.server.active=local` → 127.0.0.1:1521/FREE。
- dev profile LLM 代理：`llm.proxy-base-url=http://100.66.1.4:8082/api/llm-proxy/chat/completions`（本机不跑执行服务器）。
- Flyway：两端 `SYSTEM.flyway_schema_history` 表名/列名都是**小写带引号**（Hibernate globally-quoted），查询必须 `system."flyway_schema_history"` 和 `"installed_rank"` 等小写引号列名；本机已记录 0.9.155(baseline)/188/189/190，测试库缺 189。
- 登录测试账号 1657 及 34 个用户在 SYSTEM.USERS 表（ID 为 VARCHAR2，主键不是数字序列）。

### 2026-08-15 DeepSeek Harness（DSH）插件开发环境
- DSH 源码仓库 checkout：`C:\Users\Administrator\Documents\Qoder\2026-08-13\chat-1\deepseek-harness`（pnpm workspace，240 个项目，`pnpm@11.7.0`）。
- DSH 运行时：`$env:DSH_HOME=C:\Users\Administrator\.dsh`；Web GUI = `pnpm dsh web`（默认 `http://127.0.0.1:3080`，profile 名 `web`，配置在 `$DSH_HOME\profiles\web\cordis.patch.yml` 用户层可热重载；bundle 层 patch 在 `packages\bundle\web-app\cordis.patch.yml`，**改动必须重启 dsh web 才生效**）。
- 本机 GUI 服务进程链：cmd → `pnpm dsh web` → `node --import tsx/esm apps/cli/src/bin.ts web`；重启=杀 node 进程后同目录重跑。
- 插件架构（双半包）：host 包（提供 Typert Remote，如 `@deepseek-ai/dsh-host-file-browser`）+ client 包（浏览器 UI，`dsh.client` 声明 + `./client` bundle，如 `@deepseek-ai/dsh-client-ui-file-browser`）；client 通过 `ctx.remote.<namespace>` 调 host Remote，经 `packages/api/remotes` 组装挂载。
- 构建链路：`pnpm install`（新包需链接 web-app deps）→ `pnpm build:lib:host`（tsc + tsdown 生成 `lib/typert.host.js`/`lib/typert.remote-client.js`）→ `pnpm build:lib:client`（生成 client bundles）→ `pnpm build:web`（前端 dist）。启动 dsh 时 `healProfilesModuleFallback` 自动把依赖闭包链接进 `$DSH_HOME\profiles\node_modules`。

### 2026-08-15 dsh-browser 浏览器插件安装（web profile）
- 插件来源：GitHub `xylt369/dsh-browser`（npm 包 `@yeesy369/dsh-browser-playwright`/`dsh-tool-browser`/`dsh-web-permission`，给 DSH 加真实浏览器能力，默认用本机 Microsoft Edge，登录态存 `~/.dsh/edge-profile`）。
- 安装命令：`pnpm dsh plugin --profile web add @yeesy369/dsh-browser-playwright @yeesy369/dsh-tool-browser @yeesy369/dsh-web-permission`（在 harness checkout 下执行；`dsh` CLI 不在 PATH，用 `node --import tsx/esm apps/cli/src/bin.ts` 等价调用）。
- **踩坑**：三个包在 npm 上发布时把内部依赖写成 `@yeesy369/dsh-browser: "workspace:*"`，而 profile 目录自身就是 pnpm workspace，直接 `add` 报 `ERR_PNPM_WORKSPACE_PKG_NOT_FOUND`。解决：在 `$DSH_HOME\profiles\web\pnpm-workspace.yaml` 加 `overrides: {"@yeesy369/dsh-browser": "0.5.0"}`（pnpm 11 的 overrides 只能写在这里，写 package.json 的 `pnpm.overrides` 已被忽略）。
- 安装后 `dsh.profile.bundles` 自动追加三个包；验证：`node --import tsx/esm apps/cli/src/bin.ts --profile web --dump-config`（EXIT=0，能看到 browser-playwright/tool-browser/web-permission 三个 layer）。
- 生效需重启 dsh web；本机用 `schtasks` 排程 `restart-dsh.ps1`（杀 PID 20668 进程树 → 确认 3080 释放 → 重跑 start-dsh.cmd）实现自动重启。
- 安全模型：默认放行公网域名，SSRF 守卫拦截内网/回环；`browser_evaluate` 默认关闭（profile `cordis.patch.yml` 里 `tool-browser.config.evaluate: true` 可开启，高危）。

### 2026-08-15 测试服务器 Docker 环境自动部署机制（重构后，替代旧 auto-deploy.sh）
- 旧 `/home/liuzh2008/公共/med_ai_assistant_workspace/auto-deploy.log`（354MB）已 6-24 停更；现机制拆为多个 cron + systemd timer：
  - cron `*/10`：`/home/liuzh2008/medai/deploy-wrappers/git-poll-deploy.sh`（git 轮询→pull→Maven 构建→Docker 构建→重启，日志 `/home/liuzh2008/medai/logs/git-poll-deploy-cron.log`）——这是「自动更新代码并部署」的主链路。
  - cron `02:32` + systemd `medai-test-execution-auto-deploy.timer`(02:35)：执行服务器 `auto-deploy-execution.sh`（依赖主服务器构建产物 prebuilt tar，日志 `/home/liuzh2008/medai/logs/execution-auto-deploy.log`）。
  - cron `02:42` `auto-deploy-backend-host-test.sh`、cron `02:52` `call-main-apply.py`（日志 `/home/liuzh2008/medai/logs/main-apply-auto-deploy.log`，曾每天 409 "Backend and frontend versions must both be present"）。
- git-poll-deploy.sh 里 `BACKEND_DIR/FRONTEND_DIR` 用 `/home/liuzh2008/public/...`（= 中文「公共」目录，同一目录）。
- 后端双镜像共用同名 jar：`mvn package -Pmain` 与 `-Pexecution` 都产出 `med_ai_assistant_backend-{revision}.jar`（revision 在 pom 第 30 行，当前 0.9.246），execution 覆盖 main；运行时 profile 由外部决定（主服 `SPRING_CONFIG_LOCATION=file:/app/config/application.properties`，执行服 `SPRING_PROFILES_ACTIVE=execution`），故共用同一 jar 无碍。
- 仓库工作区长期脏：大量 `.codexbak*`、`deploy/execution-linux.backup.*/`、`__codex_sync__/` 及 300+ 条 git stash（dev-mode-* auto-stash）由 Codex 在服务器直接操作累积，需专项清理。

### 2026-08-15 测试服务器 .env.local 覆盖机制（根治 git pull 阻塞）
- 机制：被跟踪的 `.env.*` 保持生产默认值，测试服务器专属值放 gitignored 的 `.env.*.local`，compose 的 `env_file` 与 git-poll-deploy.sh 的 `--env-file` 都追加 `.local`（后者覆盖前者）。
- 关键区分：①compose `environment:` 里的 `${ORACLE_HOST}` 替换由 CLI `--env-file .env.execution --env-file .env.execution.local` 决定（later wins）；②compose `env_file:` 指令把文件内容**原样注入容器原始环境变量**（如 `ORACLE_HOST`），必须也在 env_file 列表里加 `.local`（`required: false`）才能让 entrypoint 读到的 `ORACLE_HOST` 正确——漏掉这一条会导致 entrypoint `nc -z ${ORACLE_HOST}` 等生产库卡死，而 `SPRING_DATASOURCE_URL` 却是对的（两者来源不同）。
- 落地：`.gitignore` 加 `.env.local`/`.env.*.local`/`*.codexbak*`/`__codex_sync__/`/`*.backup.*/`；`docker-compose-execution-image.yml` 和 `docker-compose-main-linux-oracle-image.yml` 的 env_file 都加 `.local`（required:false）；execution compose 补 `/medai/downloads:/medai/downloads:ro` 挂载。已提交 `4587f8dc`。
- git-poll-deploy.sh（`/home/liuzh2008/medai/deploy-wrappers/`，服务器本地、不在仓库）已改：构建前 `rm -f target/*.jar`；重启时追加 `--env-file .env.*.local`。备份：`git-poll-deploy.sh.bak.20260815`。
- 服务器残留未提交的"部署脚本重构 + medteach AD"中间态（Codex 遗留）已清理：`git reset --hard HEAD` + `git clean -fd` 回退丢弃（其中 2 个合法修复已提交 `c8df613c`：执行服务器 version.txt 原子写入、deploy.sh HEALTH_MAX_RETRIES 30→120）。关键改动补丁留档：`/home/liuzh2008/medai/stash-local-configs-full.patch`、`stash-server-local-deploy-customizations.patch`、`legit-execution-fixes.patch`。
- 踩坑：`deploy/main-linux-testServer/sql/` 目录曾被某次 sudo/docker 操作改成 root:root 所有，导致 `git reset --hard` 恢复文件时"权限不够"；需 `sudo chown -R liuzh2008:liuzh2008` 修复。

### 2026-08-15 DSH 插件化 MCP 开发环境准备与 P0 验证（关键修正）
- **pom.xml**：`<spring-ai.version>` 从 1.0.3 **升到 1.1.3**（1.0.3 的 `spring-ai-starter-mcp-server-webmvc` 只有 SSE 传输，与 DSH mcp-client 的 streamable-http 不兼容；1.1.3 引入 `WebMvcStreamableServerTransportProvider`，MCP SDK 0.17.0）。`<dependencyManagement>` 引 `spring-ai-bom` + 依赖 `spring-ai-starter-mcp-server-webmvc`。验证：`mvn -q -DskipTests compile` EXIT=0。
- **application.properties**：`spring.ai.mcp.server.name=medai`、`version=0.1.0`、`type=SYNC`、**`protocol=STREAMABLE`**（1.1.3 的 ServerProtocol 枚举 SSE/STREAMABLE/STATELESS，默认 SSE，不设则 DSH 连不上）。
- **WebSecurityConfig.PUBLIC_PATHS 加 `/mcp`**：`/mcp` 不在公开路径会被 `JwtAuthenticationFilter` 拦（无 JWT→401）。理由：MCP 用每机 token（MEDAI_MCP_TOKEN），非工作站 JWT——SoC 方案 3.7 的落地。
- **新增代码**：`com.example.medaiassistant.mcp.config.McpServerConfig`（`ToolCallbackProvider` bean，`MethodToolCallbackProvider.builder().toolObjects(new PingTool()).build()`）+ `mcp.tool.PingTool`（`@Tool(name="medai_system_ping")`，spike 用）。1.1.3 日志 "Registered tools: 1" 确认注册成功。
- **DSH 侧**：`$DSH_HOME\profiles\web\cordis.patch.yml` 已加 `mcp-medai` 条目（`@deepseek-ai/dsh-mcp-client`，serverName=medai，transport=streamable-http，url=http://127.0.0.1:8081/mcp，failOnStartupError=true）。**2026-08-16 补 `reconnect: {enabled:true, initialDelayMs:1000, maxDelayMs:30000, maxAttempts:10}`**：后端重启后旧会话失效（Session not found）时自动重建连接（详见 踩坑与教训.md）。
- **MCP streamable-http 传输要求**：Accept 头必须同时含 `text/event-stream` 和 `application/json`（缺一报 "Invalid Accept headers. Expected TEXT_EVENT_STREAM and APPLICATION_JSON"）；initialize 后后续请求需带 `Mcp-Session-Id` 头。
- **✅ 已解决（P0 streamable-http 反序列化 500）**：根因 = MCP SDK 0.18.x 的 `WebMvcStreamableServerTransportProvider.handlePost` 用 `request.body(String.class)` 读请求体，Content-Type application/json 时 Spring 用 Jackson 把 `{..}` 反序列化成 String → "Cannot deserialize String from Object (START_OBJECT)"。
- **修复**：`McpServerConfig` 加 `WebMvcConfigurer.extendMessageConverters`，在转换器链最前插入支持 application/json 的 `StringHttpMessageConverter`，让 String body 走原始字符串读取。**验证通过**：真实 MCP SDK client（Java 0.18.3 `HttpClientStreamableHttpTransport`，脚本 `backend/temp/McpClientTest.java`）成功 connect→initialize→listTools，返回 `TOOL: medai_system_ping`。
- **排查链（已排除）**：①手写格式（真实 SDK client 也失败）②Jackson 版本（`jackson-bom.version=2.20.1` 仍复现）③MCP SDK patch（0.17.0/0.18.2/0.18.3 均复现，行号 416→444 变化证明版本已变）④McpJsonMapper bean（`@Primary` 覆盖仍复现）→ 最终字节码确认 `request.body(String.class)`。
- **当前 pom 状态**：Spring AI **1.1.6**（1.0.3→1.1.6，streamable-http）、`jackson-bom.version=2.20.1`、`mcp-sdk.version=0.18.3`（覆盖 Spring AI 默认 0.18.2）+ WebMvcConfigurer workaround。**注**：Jackson 2.20.1 与 MCP SDK 0.18.3 覆盖是排查副产物，非必需（真正的修复是 workaround），SDK 升到 1.0+（mcp-json 并入 mcp-core，结构已变）会破坏 Spring AI 1.1.6，故用 workaround。后续 SDK 升级后可移除 workaround。
- **工具链**：Java 21.0.4、Maven 3.9.10、Node v24.11.1、pnpm 11.21.0。DSH checkout `custom/medai` @ `8b73ae4a27`。
- **✅ P0 端到端通过（2026-08-15 完成，用户实测）**：DSH 新对话调用 `medai_system_ping` 返回 `"pong"`，MCP 全链路 connect→initialize→tools/call 三步握手成功（纯 HTTP 可复现命令已存 `记忆库/会话记忆.md`）。两个收尾关键补丁：
  - **mcp-client 必须装进 profile**：`@deepseek-ai/dsh-mcp-client` 是 DSH 内置插件，但**不随默认 bundles（dsh-base/dsh-web-app）加载**——需 `node --import tsx/esm apps/cli/src/bin.ts plugin --profile web add @deepseek-ai/dsh-mcp-client`（npm 版 0.0.1-rc.1，含 streamable-http 支持；本地 checkout 是 0.1.0-rc.5）。否则 `cordis.patch.yml` 的 `name:` 无法解析、DSH 静默跳过 MCP 配置（现象=工具不出现且无报错）。
  - **后端需独立进程启动**：`mvn spring-boot:run` 作为会话后台任务时，DSH 重启会清空会话后台任务→后端被杀。改用 `backend/start-backend-dev.bat` + `Start-Process` 分离启动（独立于会话），不随 DSH 重启而停。

### 2026-08-15 DSH 技能发现根与用户级技能（grill-with-docs）

### 2026-08-15 DSH 技能发现根与用户级技能（grill-with-docs）
- 技能发现根（rank 顺序）：项目 `.dsh/skills`（本机为 Junction → `.qoder/skills`，同一目录，rank 100）> 项目 `.agents/skills`（200）> customSkillDirs（300）> 用户 `$DSH_HOME/skills`（本机 `C:\Users\Administrator\.dsh\skills`，rank 400）> `~/.agents/skills`（500）。只发现一层 `<name>/SKILL.md` 或 `<name>.md`；frontmatter 必填 `name`（kebab-case）+ `description`，可选 `whenToUse`；同目录 `reference/` 等资源由模型按基目录指引按需读取。
- 用户级技能已装：`C:\Users\Administrator\.dsh\skills\grill-with-docs\SKILL.md`（基于文档的代码审查，自包含中文版）。watcher（Chokidar）自动发现新技能目录，无需重启 dsh web——写入后本会话技能目录即时刷新出现该技能。
- 沙箱事实：工作区写模式不允许写 `C:\Users\Administrator\.dsh`（用户级技能目录），需 danger-full-access 级别权限。

### 2026-08-17 DSH 双实例完全独立（已落地）：开发实例 3081 vs 使用实例 3080
- **布局**：开发实例 = `C:\Users\Administrator\Documents\Qoder\2026-08-13\chat-1\deepseek-harness-dev`（本地克隆自现有 checkout，官方基线 47f943859b，`plugin-dev` 分支，origin=https://github.com/deepseek-ai/deepseek-harness）+ `DSH_HOME=C:\Users\Administrator\.dsh-dev`（已复制 AGENTS.md/.credentials.yaml/skills）+ 端口 3081 + 启动脚本 `start-dsh-dev.cmd`（**零 MedAI 依赖**：无 8081 网关检查/拉起、无 MEDAI_MCP_TOKEN；参数仅 `--port 3081 --trusted-host 100.66.1.3`）。使用实例 = 现有 checkout（custom/medai）+ `.dsh` + 3080 保持现状。
- **已验证**：3081 LISTENING PID 22208、HTTP 200、3080 无感（PID 14480 不变）；`.dsh-dev/profiles/web/cordis.patch.yml` 首启自动生成为空数组 `[]`（零 medai/MCP/pii 配置，8081 宕机也能启动）。
- **记忆库共享**：两实例会话工作目录同为 `D:\MedAiAssistant 1.0 BS`，读写同一份 `记忆库\`（用户拍板接受并发风险）。
- **踩坑（可复用）**：①Windows 批处理必须 ASCII + CRLF——write 工具写 LF 导致行错位、注释单词被当命令（现有 checkout 提交 c879aa17a8 修过同坑）；②`--allow-remote-privileged-methods` 是 custom/medai 本地定制参数，官方基线 47f943859b 无此选项（报 unknown option）；③官方仓库不跟踪构建产物（`packages/**/lib`、`apps/web/dist`），dev 副本首启报 MissingClientBundleError×37，解决 = 从现有 checkout 复制 221 个 lib 目录 + apps/web/dist，或 dev 副本自跑 `pnpm build`（build:lib:host→build:lib:client→build:web）。
- **待办**：dev 副本可自跑一次官方构建刷新产物；插件测试用 `dsh plugin --profile web add`（在 dev 副本目录下执行）；官方更新需在用户终端（沙箱外，aTrust VPN）git fetch。
- **2026-08-17 续：`/api/host.pickDirectory: HTTP 403` 在 dev 实例的解决**（官方基线无此定制，移植步骤可复用）：①cherry-pick 使用实例的 `a2259f81e8`（connection 插件 `allowRemotePrivilegedMethods` 配置 + CLI flag + 测试）②**补 cordis.patch.yml 两处接线**（web-runtime row：`allowRemotePrivilegedMethods: !!js ctx.webStartup.allowRemotePrivilegedMethods`；connection row：`!!js ctx.webRuntime.allowRemotePrivilegedMethods`——a2259f81e8 提交清单不含 cordis.patch.yml，接线来自 custom 另一历史，纯 cherry-pick 会漏）③dev profile 用户层 `host: 0.0.0.0`（远程入口可达，使用实例 3080 绑 0.0.0.0 就是这份 profile 配置）④start-dsh-dev.cmd 带 `--trusted-host 100.66.1.3 --allow-remote-privileged-methods`。验证：`POST http://100.66.1.3:3081/api/host.pickDirectory` 由 403 → HTTP 200（围栏放行，进入业务层）。plugin-dev 分支历史：47f943859b → ca39cfa033 → 8c9aad0c4d → 8e3c75f82d → start-dsh-dev.cmd 提交。
- **2026-08-17 续2：文件浏览器插件已移植到 dev 实例**。cherry-pick 7 个提交（`97b78ab378` LAN 明文 HTTP 的 crypto.randomUUID 前置修复 + `5fbedd2f9b` 插件主体 + `7ba8b57129` markdown/mermaid + `e74b8aa6e7` PDF 导出 + `957f51af51` 聊天文件打开路由 + `8b73ae4a27` TOC + `2661a7e04d` 行选择，两处 CSS Auto-merge 无冲突），`pnpm install --offline` 同步 lockfile（7ba8b57129 有依赖增量）；dev profile 用户层加 `file-browser root: 'D:\MedAiAssistant 1.0 BS'`。验证：client bundle `/plugins/@deepseek-ai/dsh-client-ui-file-browser/client.js` 200（7.0MB）、pickDirectory 200、3080 无感。接线三面齐全：cordis.patch.yml host row（99-100）+ client row（290-291）、web-app package.json 依赖、tsconfig.client.json references。
- **2026-08-17 续3：常驻机制从 3080 切换到 3081（用户拍板 3080 不需要常驻）**。①开机自启：Startup 目录 `start-dsh.cmd`（3080 版）删除 → 替换为 `start-dsh-dev.cmd`（转发式调 dev checkout 的 `start-dsh-dev.cmd`）②`\dsh-watchdog` 计划任务重建：双触发器（AtLogOn + 每 3 分钟无限重复）执行 `C:\Users\Administrator\.dsh-dev\watchdog-dsh-dev.ps1`（默认 Port=3081、home/.log/.lock 均在 .dsh-dev、拉起 `watchdog-launch-dev.cmd`）③`\dsh-browser-restart` 任务改执行 `C:\Users\Administrator\.dsh-dev\restart-dsh-dev.ps1`（3081 + dev checkout）④`\DSH-Fix-NBLink-VIP` 与端口无关未动。**闭环实测通过**：杀 3081 → 手动触发 watchdog → 30 秒内自动拉起（新 PID、HTTP 200）；测试后已删 watchdog-dsh.lock。3080 现在无自启无守护，由用户手动管理。
- **2026-08-17 续4：实测终止 3080 后 3081 完全不受影响（双实例独立性再验证）**。操作：`Stop-Process -Id 14480 -Force`（14480 = 3080 实例 `node --import tsx/esm apps/cli/src/bin.ts web --trusted-host 100.66.1.3 --allow-remote-privileged-methods`）。结果：3080 停止监听、进程退出；**3081 监听不变（PID 32568）且 HTTP 200 OK**，无任何影响。结论：两实例各自独立进程/独立 `$DSH_HOME`（.dsh vs .dsh-dev），互不依赖，可放心单独终止 3080。3081 当前 PID 32568（较续3 记录的 22208 已重启过，watchdog 正常拉起过）。
- **2026-08-17 续5：恢复 3080 的正确方式（关键踩坑：继承 DSH_HOME 导致 EADDRINUSE）**。**正确命令**：在 `C:\Users\Administrator\Documents\Qoder\2026-08-13\chat-1\deepseek-harness` 下，先 `set "DSH_HOME=C:\Users\Administrator\.dsh"`（并 `set "DSH_WEB_URL="` `set "DSH_SESSION_ID="`），再 `call pnpm dsh web --trusted-host 100.66.1.3 --allow-remote-privileged-methods`（默认端口 3080）。**不能直接跑 `start-dsh.cmd`**：其"检查并拉起本地后端 8081"逻辑已过时——DSH 是客户端，后端跑在开发主服务器（本机 100.66.1.3 的 Spring Boot 8081）由主服务器侧管理，客户端 profile 已 `failOnStartupError: false`，网关不在也能照常启动；start-dsh.cmd 会因 8081 未监听等 90s 后报错退出。**大坑**：当前 3081 会话环境变量 `DSH_HOME=.dsh-dev`、`DSH_WEB_URL=http://127.0.0.1:3081`，用 Start-Process/cmd 启动子进程会**继承**这些变量 → 想启 3080 却加载 dev profile 去监听 3081 → `EADDRINUSE 0.0.0.0:3081` 启动失败（现象与"端口被占"一致，实为 DSH_HOME 指错 home）。恢复后 3080 PID 31812，HTTP 200，LAN http://100.66.1.3:3080。
- **2026-08-17 续6：MCP 双因子通道打通（服务 JWT 交换方案落地，DSH 无需工作站登录态）**。后端 `medai.mcp.jwt-enabled=true`（双因子：JWT 主身份 + X-MedAI-Machine-Token 设备准入）下，DSH 无登录态客户端接入方案：
  - **后端**：①`McpJwtService`（`mcp/auth/McpJwtService.java`）：独立密钥 `medai.mcp.service-jwt-secret` 签服务 JWT（sub=doctorId，aud=mcp-service，ttl=`medai.mcp.service-jwt-ttl-seconds` 默认 86400s）——与工作站 jwt.secret 完全隔离，服务 JWT 无法访问工作站 API（隔离已验证：访问 /api 返 401）；②`McpAuthExchangeController`（POST `/mcp/auth/exchange`）：Bearer 每机 token → 注册表校验 → 签发服务 JWT（含审计日志 "MCP 服务 JWT 换取"）；③`McpRequestFilter.shouldNotFilter` 放行 `/mcp/auth/exchange`；④`WebSecurityConfig.PUBLIC_PATHS` 加 `/mcp/auth/exchange`（**注意 PUBLIC_PATHS 是精确路径，"/mcp" 不覆盖子路径**，漏加会被 JwtAuthenticationFilter 拦 401 空 body）；⑤`JwtDoctorAuthenticator` 双密钥解析（先工作站 secret 后服务 secret）。
  - **DSH 侧**（mcp-client fork，`packages/mcp/mcp-client/src/transport.ts`）：新增 `resolveAuthHeaders(config)`——headers 含 `X-MedAI-Machine-Token` 时，连接前 fetch `{url}/auth/exchange` 换服务 JWT 覆盖 Authorization；`connection.ts` 每次（重）连接前调用 → **JWT 过期自动刷新**（配合 reconnect 自愈）。`cordis.patch.yml` mcp-medai 加 `X-MedAI-Machine-Token: !!js 'process.env.MEDAI_MCP_TOKEN'`。
  - **启动要求**：3080 启动必须带 `MEDAI_MCP_TOKEN`（dev 占位 `medai-dev-token`，start-dsh.cmd 有 fallback 但直接 pnpm 启动没有——不带则 headers 为 Bearer undefined、medai 工具不可用）。
  - **验证**：`D:\MedAiAssistant 1.0 BS\verify-mcp-dual-factor.mjs`（exchange→initialize→tools/list→工作站API隔离反证，全绿）；后端 application.log 可见 DSH 的 exchange+initialize 记录（注意 **logback FILE appender 写 `logs/application.log`，dev-start.log 只收 stdout/SQL**——查 MCP 日志看 application.log）。


设计阶段
/deep-planning
目标：

要求：
- 以中文回复
- 直接读取项目文件
- 计划拟定好后，在执行前必须经过我审核才能进行代码的修改。
- 请遵守敏捷开发的原则进行规划设计。每个阶段或步骤都需要验证是否完成。
- 每个阶段或步骤需要包含阶段测试。测试需遵循测试金字塔原则：
  * 金字塔底部（基础）：单元测试。验证单个函数或方法的正确性。
  * 金字塔中部（核心）：自动化集成测试类。验证模块间协作。
  * 金字塔顶部（少量）：端到端(E2E)测试 - HTTP测试脚本，验证完整的用户流程，但数量应控制。
http测试文件的开头示例：
### 基础配置
@baseUrl = http://localhost:8081
@apiPath = 
### 用户可配置参数 - 请根据实际情况修改以下参数
@patientId = 512921196608173226-1
@recordId = 
- 执行服务器为100.66.1.2:8082
- 测试文档放在test-scripts中

构建“测试-实现”的自动化循环
- 定义任务：向LLM提供清晰的需求说明和函数签名。
- LLM生成：请根据需求生成第一版实现代码和对应的单元测试。
- 沙箱运行：在隔离环境中自动运行测试。
- 迭代修复：如果测试失败，请根据错误信息分析并重新生成代码。重复此过程直到所有测试通过。

开发阶段：
- follow your custom instructions
- 注意参考doc/接口来完成代码。


生成接口的详细描述。
- 写入doc/接口目录下相应文件。
- 包含：接口路径、功能说明、请求参数(非表格格式)、响应格式、逻辑、响应示例、相关文件。标题为正文、黑体。

1. Ask Cline to “update memory bank” to document the current state。
2. 后端如果接口有新增、修改或删除，记录到“doc/接口”目录下接口文档中。接口描述中需包含：接口路径、功能说明、请求参数(非表格格式)、响应格式、逻辑、响应示例、相关文件。标题为正文、黑体。
3.请使用JSDoc 注释进行详细注释。
4.总结本次任务，生成更新日志，并写入当日日志文件。要求每天只能有一个更新日志文件，更新日志文件放在“/doc/更新日志”目录下。
5. 生成本次任务的git信息并提交
- git信息中包含Memory Bank 中更新的文件。
- 需要分开执行git命令
- 先执行git add添加文件
- 再执行git commit提交变更
- 保持相同的提交信息
- 提交后执行git push推送
6.直接查看文件，不要使用git diff显示文件内容，以减少人工干预。

请为文档创建目录。
- 使用HTML锚点标签来实现跳转功能。
- 添加“返回目录”按钮

** 修复git冲突
- 请以内容编辑方式修复git冲突。

前端
调用D:\MedAiAssistant 1.0\MedAiAssistant 1.0 BS\med_ai_assistant_1.0_bs_backend\doc\接口中相应接口文件。
要求：
- 前端统一使用使用Element UI。
- 前端调用接口时，检查是否有两个api，类似于：:8081/api/api/department-groups/1，正确应该为:8081/api/department-groups/1
- 不要更改前端的端口8080

**常用命令
npm run serve
mvn clean install
mvn spring-boot:run
taskkill /F /IM java.exe

后端
chmod +x ./deploy.sh
./deploy.sh

实时查看，Ctrl+C退出：docker-compose -f docker-compose-main-linux-oracle-image.yml --env-file .env.main logs -f --tail=100

检查轮询状态：curl localhost:8081/api/polling/status

前端
复制压缩文件

解压文件unzip

部署

#在根目录中解压+部署
chmod +x ./deploy-from-package.sh
./deploy-from-package.sh


